# Round 42 — 2026-06-26 19:16 PT (Cake cron BATCH)

Atlas IV: in-document Find bar (Cmd+F) in ReaderPanel.svelte -> palette grade.
Explicit round-41 follow-up (parallel treatment to Library Search, for the
OTHER search surface). 5 frontend/UX slices on a new tested pure core.

## Shipped (2 commits)
- 195fc23 feat(reader-find): tested pure core for palette-grade find-in-page
  - new src/lib/readerFindView.ts + readerFindView.test.ts (86 tests)
  - all five capabilities' logic, green standalone
- aff3fb6 feat(reader-find): wire palette-grade find bar into ReaderPanel
  - ReaderPanel.svelte integration (state, find fns, event listeners,
    window keydown, findbar markup, CSS)

## Five slices
1. Find-state model: interpretFindState collapses the two pdf.js find events
   (updatefindcontrolstate + count-only updatefindmatchescount) into one
   discriminated idle/pending/found/not-found status. Key fix: mid-scan zero
   count reads "Searching" not a false "No matches". describeFindStatus +
   findStatusTone + "wrapped" pill.
2. Single dispatch builder: buildFindDispatch is the one source of truth for
   find/again-next/again-prev/options/clear, replacing 3 drifted literals;
   adds matchDiacritics. FIND_OPTION_TOGGLES aria-pressed segmented chips.
3. Recent-search history: pushFindHistory MRU ring (persisted at
   slab.reader.find.history.v1) + suggestFindHistory palette-scored
   combobox/listbox dropdown.
4. Global find-again: classifyFindGlobalKey (F3/Cmd+G, Shift reverses; does
   NOT claim Shift+Cmd+F library search) wired into window keydown;
   classifyFindDropdownKey for in-dropdown arrow/enter/escape; stopPropagation
   so reader keymap never double-fires.
5. SR announcer: announceFindStatus polite aria-live ("Match 3 of 17 for
   ..., wrapped").

## Gates
- readerFindView 86/86
- paletteSearch 253/253, librarySearchView 127/127, beaconCacheView 112/112,
  shortcutsOverlay 95/95, toastStack 254/254, hopper 0-fail — all unchanged
  (reused palette core provably intact)
- pnpm check: 0 errors / 104 warnings (rounds 32-41 baseline held EXACTLY)
- cargo fmt --all --check clean; ZERO Rust files changed
- Pushed to origin/main, verified (20a93b2..aff3fb6)

## Notes
- Frontend-focus override honoured: all TS/Svelte. Zero backend.
- 2 commits not 5: the component integration is one cohesive find-bar surface
  that can't be split into 5 separately-COMPILING commits without pushing red
  intermediate states. Honest 2-commit structure over fake fragments.
- interpretFindState read web/pdf_viewer.mjs (FindState enum + 2-event
  protocol) as ground truth — faithful, not approximate.
- Deferred: thumbnail-rail active-match highlight, scrollbar find mini-map
  (logged as follow-ups in the roadmap).
