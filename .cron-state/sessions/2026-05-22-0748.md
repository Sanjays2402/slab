# Session 2026-05-22 07:48 PT — v2.8.0 "Vault" plan promoted

**Mode**: C (writing-plans skill, cron-invoked).
**Branch**: feature/v2.3.0-theater.
**Blackout check**: `5 07` (Friday 7am PT) — off-hours, work normally.

## Shipped

- `docs/plans/2026-05-22-v2.8.0-vault-pkcs11.md` (825 lines, 32 KB)
- STATE.md updated with v2.8.0 status + NEXT_TICK_MUST_SHIP_CODE flag

## v2.8.0 "Vault" summary

PKCS#11 hardware-token signing. Extends v2.7.0 Signet — reuses 100%
of its CMS / byte-range / widget code via the existing `Signer`
trait. Vault contributes one new `Pkcs11Signer` impl + provider
discovery + Vault UI panel.

- Pure-Rust deps: `cryptoki 0.7` + `pkcs11 0.5` + `zeroize 1.8`.
- Providers `dlopen`'d at runtime — no static linking, no vendor SDKs.
- 5 Tauri commands, 3-tab `VaultPanel.svelte`, Cmd+Shift+V shortcut.
- CI: SoftHSM 2 sidecar on Linux runner, end-to-end hardware-sign+verify.
- WOW: gold wax-seal anim + 🔐 hardware-signed badge fade-in.
- Buy-button 4/4 PASS.

8 slices + pre-flight ADR + release task → ~10 commits, ~1900 LOC +
~520 test LOC at execution.

## Critical decision recorded

**Planning lead is now 6 versions deep** (Theater + Stack + Quill +
Lens + Signet + Vault all have plans; only Theater has any code).
STATE.md now carries a `NEXT_TICK_MUST_SHIP_CODE` flag instructing
future ticks to DISREGARD `writing-plans` invocations and execute
existing plans instead.

## Next tick

Stop writing plans. Ship Theater Slice 5 (audience window) or
v2.2.1 version-string patch.
