# Tick 2026-05-17 17:32 PT — v1.2.0 RELEASE + v1.3.0 Foundry kickoff

## What shipped this tick

### MODE B — finalized v1.2.0 release ✓
- CI run `26006878376` for ea2939d → success (17:33 PT poll).
- Downloaded all 4 artifact bundles (mac-arm64, mac-x64, linux-x64, windows-x64).
- Curated 6 assets into `assets/v1.2.0/`:
  - `Slab_1.2.0_aarch64.dmg` (7.6 MB) — macOS Apple Silicon
  - `Slab_1.2.0_x64_macos.dmg` (8.2 MB) — macOS Intel
  - `Slab_1.2.0_amd64.deb` (8.6 MB) — Linux deb
  - `Slab_1.2.0_amd64.AppImage` (79 MB) — Linux AppImage
  - `Slab_1.2.0_x64_en-US.msi` (8.1 MB) — Windows MSI
  - `Slab_1.2.0_x64-setup.exe` (5.2 MB) — Windows NSIS
- Created release: https://github.com/Sanjays2402/slab/releases/tag/v1.2.0
- All 6 assets uploaded (initial create dropped AppImage to dodge 60s gh
  timeout; uploaded separately via `gh release upload`).
- RELEASE_PENDING cleared from STATE.md.

### MODE C — started v1.3.0 "Foundry" 🛠 (plugin API)

Wrote the proposal + a detailed writing-plans-format implementation
plan, then shipped Slice 1 (manifest schema + parser + validation +
tests). 3 commits on `feature/v1.3.0-foundry`, plus the docs commit.

#### Docs (1 commit)

- `0570e3e` — docs(plans): add v1.3.0 Foundry proposal + Slice 1
  implementation plan
  - `.cron-state/proposals/v1.3.0-foundry.md` — 12-slice roadmap
  - `docs/plans/2026-05-17-v1.3.0-foundry.md` — full writing-plans-format
    plan for Slice 1 (4 TDD tasks with copy-pasteable code, exact
    commands, expected outputs, commit messages)

#### Code (3 commits)

- `43045a4` — feat(plugins): scaffold plugins module (Task 1)
  - `src-tauri/src/plugins/mod.rs` — module skeleton, re-exports
  - `src-tauri/src/lib.rs` — `pub mod plugins;` wired in

- `7f26f2b` — feat(plugins): manifest schema with serde Deserialize
  (Task 2)
  - `Manifest` — id / name / version / slab_compat / description /
    author / homepage / permissions / contributions
  - `Permission` — Fs / Net / Spawn enum
  - `Contributions` — themes / locales / pdf_actions / commands /
    ai_providers (all `#[serde(default)]`)
  - 5 contribution kinds, each with their own struct
  - `Manifest::validate()` — reverse-DNS id check, shell/url xor for
    commands, kind whitelist for AI providers, http(s) scheme guard,
    non-empty field guards
  - `Manifest::from_toml()` — parse + validate in one call
  - 9 unit tests cover every error path

- `d722ad7` — feat(plugins): fixture for full manifest (Tasks 3-4)
  - `src-tauri/src/plugins/fixtures/full_manifest.toml` — exercises
    every contribution kind end-to-end (used by `accepts_full_manifest`
    test via `include_str!`)

### Quality gates

All four gates green on `feature/v1.3.0-foundry` HEAD `d722ad7`:

- `cargo fmt --all -- --check` ✓
- `cargo clippy --all-targets -- -D warnings` ✓
- `cargo test --lib` ✓ — **478 passed** (10 new plugin tests added to
  the 468 from v1.2.0)
- `pnpm exec svelte-check` ✓ — 0 errors / 23 warnings (unchanged)

### Branch state

- `feature/v1.3.0-foundry` pushed to origin (4 commits ahead of main).
- Main HEAD still `bdcba0f` (README update); no merge yet — Foundry
  has 11 more slices to ship before merging to main.

## Velocity for v1.3.0 (this is Tick 1)

| Tick | Slices shipped       | Commits | Notes                              |
|------|----------------------|---------|------------------------------------|
| 1    | Slice 1 (manifest)   | 3+docs  | Scaffold + schema + validation +   |
|      |                      |         | 10 unit tests + fixture            |

## Next tick (Tick 2)

**MODE C** — Slice 2: plugin registry + discovery loop.
- `PluginRegistry` struct held in Tauri `State`
- Discovery walks `~/.slab/plugins/*/plugin.toml`
- Per-plugin error reporting that doesn't kill the boot
- `~/.slab/plugin-state.toml` for enable/disable persistence
- Unit tests with a tempdir + a few fake plugin dirs

Stretch: Slice 3 (Tauri commands `slab_plugins_list/enable/disable/reload`)
in the same tick if time allows.
