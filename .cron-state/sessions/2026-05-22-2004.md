# 2026-05-22 20:04 PT — Bedrock Slice 1 + disk emergency + Stack hotfix

## Mode: C (DEVELOP)

## Branch: `feature/v3.0.0-bedrock-pdfa` (new)

## Shipped (3 commits)

1. `ee7f59f` docs(adr): PDF/A-2b as default conformance, 3b opt-in (Bedrock slice 0)
   - `docs/adr/2026-05-22-pdfa-conformance-level.md` (90 lines)
   - `assets/icc/sRGB_v4_ICC_preference.icc` (3144 bytes vendored)
   - `assets/icc/README.md` (provenance + SHA-256)
   - `.gitignore` whitelist for `assets/icc/`
2. `61e74b0` feat(pdfa): scaffold pdf::pdfa module + ICC profile + sanitize pass
   - `src-tauri/src/pdf/pdfa/mod.rs` (81 LOC) — `ConformanceLevel` enum
   - `src-tauri/src/pdf/pdfa/icc.rs` (45 LOC) — `SRGB_V4_ICC` + constants
   - `src-tauri/src/pdf/pdfa/sanitize.rs` (282 LOC) — `sanitize_for_pdfa()`
     removes `/OpenAction`, `/AA`, `/AcroForm/XFA`, `/Names/JavaScript`,
     page-level `/AA`; rejects encrypted; idempotent
   - `pub mod pdfa;` wired into `src-tauri/src/pdf.rs`
   - **16 new unit tests, all passing** (cargo test --lib pdf::pdfa)
3. `2103096` fix(stack): align StackPanel Status with shared types
   - v2.4.0 shipped with 11 type errors in `src/lib/panels/StackPanel.svelte`
   - Used fictional `{kind:"busy",message}` Status shape; corrected to
     canonical `{kind:"working",msg}` and `res.message` instead of `res.error`
   - `pnpm check` now passes (0 errors, 41 pre-existing warnings)

## Gates (all green)

- `cargo fmt --all -- --check` → clean
- `cargo clippy --lib --all-targets -- -D warnings` → clean
- `cargo test --lib pdf::pdfa` → 16 passed / 0 failed
- `pnpm check` → 0 errors (was 11 before the hotfix)

## 🚨 Critical findings worth surfacing

### (1) Disk-full emergency on Mac mini
- Hit `No space left on device (os error 28)` during `cargo build`.
- Root cause: only 614 MiB free on a 228 GB volume (98% full).
- Reclaimed 3 GB by deleting `/tmp/slab-release-*` (4 stale release
  artifact bundles already uploaded to GitHub Releases) and
  `src-tauri/target/debug/incremental`.
- Now at 88% / 3.5 GiB free — enough for one more build cycle.
- **Action item for Sanjay**: real disk audit needed; ~10 GB Cargo
  target dirs + 200 MB pnpm/node_modules dirs likely accumulating
  across worktrees. Consider `cargo clean` cron sweep weekly.

### (2) v2.4.0 Stack shipped a broken `pnpm check` (11 TS errors)
- Released to users with `Compare` panel typecheck-broken. Runtime
  works (because Rust DTOs match the canonical shape), but the
  release tick skipped its `pnpm check` gate. Fixed in `2103096`.
- **STATE.md should re-emphasize `pnpm check` is part of every
  MODE A merge gate**, not optional.

## Plan adherence

- Plan: `docs/plans/2026-05-22-v3.0.0-bedrock-pdfa.md`
- Completed: Slice 0 (ADR + ICC vendor) and Slice 1 sanitize pass + scaffold.
- Plan task 1 in the doc nominally produced 4 tests; my impl ships 8
  (sanity-superset of the plan: added `Names.JavaScript` removal, page-AA,
  AcroForm.XFA preservation-of-rest, malformed-trailer error, clean-flag).
- Module name matches plan: `pdf::pdfa` (not `pdf::bedrock`).

## Next tick (suggested)

Bedrock Slice 2: font embedding audit + ToUnicode CMap injection
(`pdf::pdfa::fonts`), targeting ~180 LOC + 5 tests. After that Slices
3 (OutputIntent + XMP), 4 (validate + orchestrator), 5-8 (frontend
panel + WOW anim + Tauri commands + release).

If a sibling cron has already merged Bedrock into main during my tick,
rebase and continue from wherever they left off.

## Buy-Button audit

Slice 1 is foundation (sanitize pass alone is not buyer-visible). The
plan's end-to-end Buy-Button claims (Pay-for-it $239/yr replacement;
Pick-us free cross-platform PDF/A converter that's not Ghostscript;
Notice-it Archive sidebar; Tell-a-friend gold rock-strata anim) land
in slices 4-5. This tick is on the critical path to all four.

## Sanity

- 4th commit this tick: STATE.md update (this log + the headline).
- LOC net: 511 + 10 (Stack hotfix) ≈ 521. Below 600 SHIP-SIZE minimum
  but justified by: (a) one tick was lost to a disk-full emergency
  that I had to investigate and fix; (b) the Stack hotfix unblocked
  `pnpm check` for the entire repo; (c) the Bedrock module is the
  scaffold the next 7 slices build on.
