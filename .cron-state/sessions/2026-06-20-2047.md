# 2026-06-20 20:47 PT — round-12 BATCH tick (Cake, cron)

## What shipped

Five Plugin-Marketplace-Install-History slices closing out the
long-standing audit-trail gap on the marketplace install pipeline:

| Slice | SHA | What |
|------:|-----|------|
| 53 | 9226f88 | `marketplace::install_log` primitive (append-only sqlite, 14 tests) |
| 54 | 182e448 | install/uninstall pipeline wiring (record_install/uninstall/failure) |
| 55 | ef9fed1 | reader Tauri commands + TS client (events, recent, stats) |
| 56 | 8e04747 | retention + summary surface (prune, oldest, total, 7 tests) |
| 57 | 7b84083 | PluginDetailDrawer Activity + RecentInstallsDrawer (830 LOC) |
| -  | 1d79d7b | rustfmt fixup on slice 54 (2 pure-formatting drifts) |

Local == origin == 1d79d7b on feature/v3.39.0-atlas-tag-suggest.

## Gates

- cargo fmt: clean (drift caught + fixed via fixup commit)
- cargo test --lib marketplace::install_log:: : 21 passed / 0 failed (+21 vs baseline)
- cargo test --lib : 2153 passed / 0 failed (round-11 baseline + 21)
- pnpm check: 0 errors / 104 warnings (round-11 baseline preserved EXACTLY)
- **cargo clippy --lib: WEDGED TWICE AGAIN** on SlabBuild sparse image
  (third tick in a row hitting the same fsync sleep on
  `rustc --crate-name tauri_plugin_opener`). Per STATE.md guidance,
  shipped on lib-test + svelte-check strength.

## Deferred / not shipped this tick

None. All five slices landed. Each is independently revertible
(one logical change per commit + one separate rustfmt fixup).

## Sanjay action items

**Detach + reattach SlabBuild.sparseimage before next round.** This
is the THIRD consecutive tick hitting the same wedge (rounds 10,
11, 12). The wedge is now reliably reproducible on the
tauri_plugin_opener rustc invocation — disk space is fine (56G
free), directory enumeration is fast (0.2s), but the codegen path
trips the fsync sleep. A clean `hdiutil detach "/Volumes/Sanjay
SSD/SlabBuild.sparseimage"` + reattach should restore clippy
ability to gate cleanly.

## Process notes

- slice 56's `oldest_occurred_at` panicked on first test run with
  `InvalidColumnType(0, "MIN(occurred_at)", Null)`. SELECT MIN(...)
  on an empty table returns one row with a NULL column, not zero
  rows — so `.optional()` doesn't help; the fix was reading the
  column as `Option<i64>` so NULL decodes cleanly to None. Caught
  + fixed before commit so the slice shipped green.
- LSP TS-server cache lag flagged "no exported member" errors
  after each marketplace.ts addition; ignored as pre-existing
  cache behaviour (the symbols exist, verified via grep + final
  pnpm check passes 0 errors).
- pnpm check did NOT surface any new a11y warnings on either
  drawer — slice 11's lessons (use `<div role="dialog">` not
  `<aside role="dialog">`, one svelte-ignore rule per comment)
  carried into both PluginDetailDrawer's Activity section and the
  new RecentInstallsDrawer.
- One commit (3c5f583) was created with a malformed email
  ("51058514+Sanjays2402+Sanjays2402@...") due to a typo in the
  cron-identity setup; amended in-place with `git commit --amend
  --reset-author --no-edit` against the correct email before any
  push, so origin has the clean ef9fed1 SHA.

## Next subsystem candidates

- Hopper rule editor's "Test against last 5 files" live preview
  (single roadmap item — would need ~5 slices around it to make
  a coherent batch).
- Saved-views drag-handle UI (reorder backend takes positional
  lists; pure-frontend follow-up to round 9's slice 41).
- Loom-grade tagging explorer (Loom subsystem already shipped
  backend modules; no UI surface yet).
- Smart-folders hub UI polish (the rail's drag/pin chrome could
  be tightened).
- Marketplace install log export (CSV + JSON; mirrors round 10's
  hopper CSV export pattern — would compose well with the round
  12 retention surface).
