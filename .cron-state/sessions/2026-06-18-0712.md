# Tick 2026-06-18 07:12 PT (Cake, cron)

**Roadmap item:** #9 Tag filter combinator (AND/OR) — last seeded item, DONE.

**Shipped:** Any/All tag-match combinator for the flat library filter.
- Backend `18229a8`: `TagMatch` enum (All default / Any, serde snake_case).
  `tag_match` field on `LibraryFilter` (`#[serde(default)]` => All — legacy
  filters keep intersection byte-for-byte, no migration). `query_documents`
  branches the flat tag path: All = `GROUP BY ... HAVING COUNT(DISTINCT
  tag_id) = N` intersect; Any = `tag_id IN (...)` union (no HAVING). All
  count dedups requested ids so a duplicate can't raise the bar past a single
  doc. 8 new tests.
- UI `522cbe9`: TS `TagMatch` type + `tag_match` mirror; LibraryPanel
  `tagMatch` state (default "all") threaded into flat `refreshDocs` filter +
  reactive `$effect` deps; "All tags"/"Any tag" rail-head toggle shown only
  when >1 tag selected, accent-tinted in the non-default Any state, mirrors
  `.rail-sort` chrome.

**Design call:** flat `tag_match` field over UI-side nested clause groups —
minimal churn, backward-compatible, rail stays a flat toggle list.

**Gates:** cargo fmt clean; cargo test --lib pdf::library:: 234 passed/0
failed (6 new query tests); clippy --lib -D warnings clean (8.73s); pnpm
check 0 errors (LibraryPanel only the 2 pre-existing autofocus + webkit
warnings, none new). No schema bump. Build cache warm — test 1.75s.

**Branch:** feature/v3.39.0-atlas-tag-suggest. Pushed + verified
(local == origin 522cbe9). Parent was 951280e (prior tick's cron-state
chore, already on origin), not ba7a83d.

**Next undone:** #10 saved tag-filter views (round-3 roadmap seeded:
#10 saved views, #11 clear-all, #12 tag descriptions).
