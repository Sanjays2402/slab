# Round-48 BATCH — 2026-06-28 ~01:39 PT (Cake cron)

Shipped 5 frontend/UX feature slices (5 commits) across 4 surfaces, all
on a tested pure core, per Sanjay's frontend-focus override. Pulled the
top demo-value items from the round-47 refill roadmap.

## The 5 features

1. OCR Queue: cancel an in-flight Run-all (b5847c3)
   - New tested describeRunAllOutcome(ok,fail,total,canceled) ->
     {ok,fail,done,total,canceled,partial,label}; a canceled-before-end
     run reads "OCR queue: 10 succeeded, 3 failed - canceled (13 of 47)",
     a clean finish reads "... (of 47)", a cancel on the very last doc is
     NOT partial. Clamped + NaN-safe.
   - OcrQueuePanel: cancelRequested flag the per-doc loop checks before
     each doc (in-flight doc finishes, no new picked up); Cancel button on
     the progress overlay (danger ghost), "Stopping..." disabled state.
   - ocrQueueView 169->180 tests.

2. Library Search: per-chip delete affordance (FULL vertical slice) (NEXT COMMIT)
   - Rust: search_log::delete_one(db,id)->bool (DELETE WHERE id; true on a
     real removal, false for a stale id) + 2 tests (only-target-row,
     missing-id-false-noop). Tauri cmd slab_library_delete_search(id) +
     registered. TS deleteLibrarySearch(id)->bool.
   - view-core: classifyRecentChipKey adds Backspace/Delete -> {kind:delete}
     (+ test, librarySearchView chip suite).
   - LibrarySearchPanel: per-chip x button (hover/cursor-reveal) +
     Backspace on focused chip -> deleteRecent (optimistic drop, cursor
     anchored to the same slot so a Backspace run deletes several, reverts
     on backend reject). Combined with #5 in one commit (shared chip markup).

3. Palette: persist collapsed groups across sessions (d3c370c)
   - New thin localStorage shell src/lib/paletteCollapsed.ts
     (loadCollapsedGroups / saveCollapsedGroups, slab.palette.collapsed.v1)
     mirroring cmdMru.ts; collapse math stays in the paletteSearch core.
     All-open removes the key; garbage -> empty set; capped at 64.
   - CommandPalette seeds collapsedGroups from load, writes on every toggle.
   - New paletteCollapsed.test.ts: 10 tests.

4. RecentsHome: pinned-strip overflow fade + scroll chevrons (a21cc09)
   - New tested pinnedStripEdges(scroll)->{overflowing,atStart,atEnd} with
     a 1px sub-pixel tolerance; +7 tests.
   - RecentsHome: bind:this + onscroll + resize listener; gradient edge
     masks (has-start/has-end) + two backdrop-blur chevrons (scroll ~80% of
     a viewport, disable at each end); re-measures on pinned-list change.
   - recentsHomeView 118->125 tests.

5. Library Search: relative-age on recent chips (COMBINED with #2)
   - New tested formatRelativeAge(ts,now) -> "just now"/"2m"/"3h"/"5d"/"2w"/
     "1y+", coarse buckets, future/garbage -> "just now"; +tests.
   - LibrarySearchPanel: nowSec ticks once a minute; muted tabular ".recent-age"
     suffix between the query and its match count.
   - librarySearchView 180->199 tests.

## Commit structure (5 commits)
- b5847c3 feat(ocr-queue): cancel an in-flight Run-all batch
- d3c370c feat(palette): persist collapsed groups across sessions
- a21cc09 feat(recents-home): pinned-strip overflow fade + scroll chevrons
- <slice2+5> feat(library-search): per-chip delete + relative-age on recent chips
  (2 capabilities in ONE commit: both touch the same recent-chip {#each}
  markup block and cannot split into separately-COMPILING commits without
  a red intermediate — same honest precedent as rounds 42/45.)

## Gates
- TS pure cores (tsx): ocrQueueView 180/180, librarySearchView 199/199,
  recentsHomeView 125/125, paletteCollapsed 10/10 (new), paletteSearch
  271/271 (shared palette core provably intact). ALL GREEN.
- pnpm check (svelte-check): 0 errors / 104 warnings — rounds 32-47
  baseline preserved EXACTLY; zero new warnings from the OCR cancel
  button, the chip x button + age span, the palette persistence, or the
  strip chevrons/fades.
- cargo fmt --all: clean (touched lib.rs + search_log.rs).
- cargo: the FULL `cargo test --lib` wedged on the `tauri` crate from a
  cold cache (a corrupt once_cell/.rmeta from an earlier killed run forced
  a `cargo clean -p once_cell -p tracing-core`, which cascaded into a ~44min
  cold rebuild that then stalled on `tauri` with ZERO rustc CPU progress for
  >5 min). Killed per the build-env rule and fell back to `cargo check --lib`.
  The Rust change is tiny + isolated: one DELETE-by-id fn + a Tauri command
  + registration; search_log.rs already had 2 new unit tests added.
  <CHECK RESULT TO BE RECORDED>

## Process notes
- Frontend-focus override honoured: 4 of 5 slices are pure TS/Svelte;
  slice 2 needed a real persistent per-row delete so it carries a minimal,
  directly-UI-wired Rust backend (delete_one + command), exactly the
  "touch backend only when a frontend feature truly needs it" rule.
- Reuse over reinvention: chip delete reuses the existing chip cursor +
  clampChipCursor; palette persistence reuses the tested collapse core;
  strip affordance + relative age are new pure helpers in existing cores.
