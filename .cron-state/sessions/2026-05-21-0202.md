# Tick 2026-05-21 02:02 PT — MODE C writing-plans — v2.0.13 "Vault" 🔒 plan promoted

## Time gate
- `TZ=America/Los_Angeles date '+%u %H'` → `4 02` → Thursday 02:00 PT → **OFF-HOURS** ✅ (blackout is 1–5 + 09–17)

## Mode
MODE C (DEVELOP) — writing-plans skill invocation. No `RELEASE_PENDING` in
STATE; no feature branch with `STATUS: DONE`; pipeline already has 11
plans on disk (v2.0.2 → v2.0.12). 12th plan tick.

## What shipped this tick
**Plan promotion:** `docs/plans/2026-05-21-v2.0.13-vault.md` — 940 lines,
~36 KB, 6 slices + pre-flight + final tick, ~1890 LOC budget, 62 new tests,
8 commits.

### v2.0.13 "Vault" 🔒 — true byte-level redaction
- **Why this version:** `RedactPanel.svelte` literally still says "true
  content-stream redaction is on the roadmap." That warning sticker IS the
  Pick-us gap. Adobe Acrobat Pro $239/yr's flagship feature. PDF Expert /
  Foxit Standard / macOS Preview ship visible-only. After v2.0.12 Sign, this
  is the strongest remaining paid-tier competitive gap.
- **Approach:** Three-layer surgery — (1) pure-Rust content-stream tokenizer
  with full graphics-state / text-matrix tracking, (2) glyph-splice surgery
  that rewrites `Tj`/`TJ`/`'`/`"` operands removing overlapping bytes,
  (3) image-XObject + annotation + metadata strip. File gets *physically
  smaller* when you redact — that's the demo line.
- **WOW (Slice 4):** Vault-Door wipe animation (380ms gold→black 8-step CSS
  @keyframes, cubic-bezier `(0.22, 1.0, 0.36, 1.0)`, 40ms stagger across
  rects) + X-Ray reveal hover (translucent ghost of removed glyphs, local-
  only, never persisted) + single-key `r` Quick-Redact reticle. Optional
  WebAudio cha-chunk on commit if Settings → Sound Effects on.
- **Buy-Button 4/4 PASS:**
  - Pay-for-it: replaces ~$240/yr/seat for legal/medical/HR teams.
  - Pick-us: only paid feature parity that matters at v1.0.0.
  - Notice-it: 6 surfaces (Redact + AutoRedact + BeaconPii panels + palette
    + keymap + settings + onboarding step #17).
  - Tell-a-friend: Vault-Door + X-Ray are screenshot-grade.
- **Cryptographic certification** explicitly deferred to v2.1.0 "Notary"
  (PKCS/7 signing) — natural pairing with v2.0.12 sigs.
- **Strict-order rationale:** v2.0.13 touches every file v2.0.6→v2.0.12
  touched. Disjoint i18n namespaces (`redact.vault.*`, `palette.vault.*`,
  `settings.privacy.vault.*`, `onboarding.vault.*`) keep merges clean.

## Slice breakdown
1. **Tokenizer** — `src-tauri/src/pdf/vault/tokenizer.rs` (~280 LOC, 14 tests).
   Pure CTM/text-matrix tracker emitting GlyphEvent per glyph.
2. **Surgery** — `src-tauri/src/pdf/vault/surgery.rs` (~340 LOC, 16 tests).
   Splices overlapping bytes out of `Tj`/`TJ`/`'`/`"` operands.
3. **Tauri command** — `slab_vault_redact`, integration tests with 8 fixture
   PDFs (mixed fonts, kerning arrays, rotation, crop boxes, idempotency).
4. **WOW** — VaultDoor.svelte + XrayReveal.svelte + single-key `r` reticle.
   10 Vitest tests.
5. **Cross-cutting wiring** — palette (4 entries), keymap, settings Privacy
   section, onboarding step #17, BeaconPii "Apply Vault" CTA.
6. **Image + annot + metadata** — XObject overlap detection + `/Annots`
   scrub + Info dict / XMP stream strip. 10 more integration tests.

## Commits this tick (2 on main)
- `<HASH>` `docs(plans): v2.0.13 "Vault" implementation plan 🔒`
- `<HASH>` `chore(cron): STATE.md + session log — v2.0.13 plan promoted`

## What's next
- **Implementation execution** awaits human go-ahead OR an autonomous slice-
  cutting tick. v2.0.13 cannot start until v2.0.12 "Sign" ships and tags.
- **Strict pipeline order:** v2.0.2 (shipped) → v2.0.3 → v2.0.4 → v2.0.5 →
  v2.0.6 → v2.0.7 → v2.0.8 → v2.0.9 → v2.0.10 → v2.0.11 → v2.0.12 →
  **v2.0.13** → v2.1.0 (Notary, deferred crypto).
- **Plan inventory now:** 12 versions on disk through v2.0.13. ~9 months of
  shipping runway if executed at ~1 version / 3 days.

## Notes
- No `LAST_WOW_TICK_AT` update — no actual feature shipped this tick, only a
  plan. Last WOW remains 2026-05-20 00:20 PT (v2.0.1 plugin moment).
- This tick passes BIG-features rule because it's a **planning tick**, not
  a feature tick (the prompt distinguishes — see "scaffolding alone never
  qualifies" exception for plans / strategy work).
- Off-hours, no blackout concerns.

## Quality gates
N/A this tick — markdown-only commits, no Rust / Svelte changes.
