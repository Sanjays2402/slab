# Cron tick 2026-05-17 ~10:35 PT (Sunday off-blackout)

## Mode chain: C → A

Started: `feature/v0.15.0-theater` had 4 of 4 planned slices DONE + STATE
suggested "Slice 6: Release prep next tick". Sanjay's mandate: "ship BIG
things every tick". Decided to escalate: ship the harder Slice 5 (Export
Annotated Deck) end-to-end **plus** Slice 6 release prep **plus** MODE A
merge in one tick.

## Commits (this tick)

- `8377fed` feat(theater): pdf::stamp_annotations — bake pen+highlighter strokes into PDF (Slice 5a) [585 LOC, 13 tests]
- `e135625` feat(theater): Save Annotated PDF — Tauri command + presenter export UX (Slice 5b)
- `f034bf6` chore(release): bump 0.14.0 → 0.15.0 + v0.15.0 release notes (Slice 6)
- `494295d` Merge v0.15.0 "Theater" 🎭 — Presenter Mode + Annotated Export (MODE A merge to main)
- `4b0c36b` chore(state): v0.15.0 RELEASE_PENDING — merge 494295d + CI 25997502867

## Slice 5 deep-dive

Built `pdf::stamp_annotations` modeled on `pdf::watermark`:
- `StampStroke { page, tool: Pen|Highlighter, color [r,g,b], width_pt, points (normalized [0,1] top-left) }`
- Two shared ExtGState dicts (SlabAnnOp opaque, SlabAnnHL @ 0.42 alpha) registered once at the document level
- Y-axis flip top-left → PDF user-space applied per vertex: `(nx*W, (1-ny)*H)`
- Round line caps + joins (`J 1 j 1`)
- Validation up front (no partial writes on error)
- 13 unit tests including: parseable output, ExtGState resource, Contents array grew, all 7 error paths, coord-clamping

Wired the frontend with `getAllStrokes()` / `hasStrokes()` exports in
PresenterAnnotations, then added `S` key + ⤓ button + native save dialog +
toast in PresenterOverlay.

## MODE A

- Quality gates re-run ON MAIN: fmt ✓ / clippy ✓ / 404 lib tests ✓ / svelte-check 0 errors
- Tagged v0.15.0 ("Slab v0.15.0 — Theater 🎭")
- Pushed main + tag via token-helper credential
- Merge SHA `494295d`, CI run `25997502867` building 6 installers

## Next tick (MODE B)

Poll CI run `25997502867`. If green:
1. Download 6 installer artifacts via `gh run download`
2. Rename mac x64 dmg to `Slab_0.15.0_x64_macos.dmg` (disambiguate from Windows x64)
3. `gh release create v0.15.0 --title 'v0.15.0 — Theater 🎭' --notes-file docs/releases/v0.15.0.md ...`
4. Clear RELEASE_PENDING in STATE
5. Pivot to v1.0.0 "Glass" planning

## Files changed (cumulative this tick)

```
src-tauri/Cargo.lock
src-tauri/Cargo.toml
src-tauri/src/lib.rs
src-tauri/src/pdf.rs
src-tauri/src/pdf/stamp_annotations.rs   [NEW, 585 LOC]
src-tauri/tauri.conf.json
package.json
src/lib/components/PresenterAnnotations.svelte
src/lib/components/PresenterOverlay.svelte
src/routes/+page.svelte
docs/releases/v0.15.0.md                  [NEW, 5.3 KB]
.cron-state/STATE.md
```
