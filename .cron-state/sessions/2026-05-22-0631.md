# Session 2026-05-22 06:31 PT — writing-plans: v2.5.0 "Quill"

Off-hours Friday 6:31 AM PT. Blackout gate cleared (`5 06`).

User invoked the `writing-plans` skill explicitly. The pipeline has Theater
(v2.3.0) slices 5-7 shipping next, then Stack (v2.4.0) plan from last tick.
Picked **v2.5.0 "Quill"** as the next plan to promote because:

1. AcroForm fill is the single largest table-stakes paid feature still
   greenfield in the repo. `forms/` module does not exist.
2. OCR (`pdf/ocr.rs`) and table extraction (`pdf/table_extract.rs`) already
   have code — Lens is well-scoped but largely already there.
3. The Reddit /r/macapps killerpdf thread had three top comments about
   "fill any tax form offline" — this is a market-confirmed wedge.
4. Buy-Button 4/4 PASS with the highest Pay-for-it score in the pipeline:
   Adobe charges $239/yr largely for this exact capability.

Plan ships in 8 slices, 8 commits, ~1450 LOC, lopdf-only (no new crates),
5 Tauri commands, WOW = gold ink-settle cascade on Save.

No code shipped this tick (writing-plans skill is plan-only). Two commits
expected (plan + STATE/session chore).
