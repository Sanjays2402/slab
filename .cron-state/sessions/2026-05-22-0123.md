# Tick 2026-05-22 01:23 PT — writing-plans skill: v2.0.3 Self-Install plan for #25

## Gate
`TZ=America/Los_Angeles date '+%u %H'` → `5 01` (Friday 1am PT, off-hours ✅).

## Mode
Cron invoked the `writing-plans` skill — task is to PRODUCE A PLAN, not ship
code. Lighter than a normal ship tick, but the deliverable is a 46 KB plan
that unblocks the next 1-2 execution ticks for issue #25.

## Pre-flight
- `gh issue list` → only #25 + #26 + #27 remain in the 5-issue override.
  #25 is lowest-numbered open → picked.
- main at `790cb10`, clean.

## Work
Wrote `docs/plans/2026-05-22-v2.0.3-self-install.md` (1263 lines, 46 KB):

- **Slice 1** (TDD): `first_launch/mod.rs` + `state.rs` (atomic tmp+rename)
  + `should_prompt` decision logic + `MockProbe`. 9 unit tests planned.
- **Slice 2** (per-OS): macOS (~/Applications + lsregister), Windows
  (%LOCALAPPDATA%\Programs\Slab + HKCU `Slab.PDF` ProgID + WScript.Shell
  Start-Menu .lnk — **zero UAC**), Linux (~/.local/bin/slab + .desktop
  + xdg-mime). Each behind `cfg(target_os)`.
- **Slice 3**: 5 Tauri commands (probe/install/skip/set/unset handler).
- **Slice 4** (the WOW): `FirstLaunchModal.svelte` — 3 CTAs, state machine
  (idle→installing→done→error), **420ms cubic-bezier (0.34, 1.56, 0.64, 1)
  settling animation + gold-accent halo drop-shadow** on success.
  prefers-reduced-motion safe.
- **Slice 4.2**: Cmd+K palette entry + Settings panel "First-launch & install".
- **Slice 5**: `docs/features/first-launch.md` with Adobe/PDF Expert/Foxit
  comparison + zero-telemetry promise.
- **Slice 6**: merge to main, close #25, post-push CI/release check.

Buy-Button verdict (3/4 PASS): Pick-us ✅ (no free OSS tool ships no-admin
self-install across all 3 OSes), Notice-it ✅, Tell-a-friend ✅. Pay-for-it
is table-stakes, not anchor. At execution: ~12 commits / ~900 LOC.

## Why a planning tick (not ship)
Skill invoked = `writing-plans`. The point is the spec — bite-sized 2-5min
tasks, exact paths, copy-pasteable code, exact commands w/ expected output.

## Commits
- `ab7645c` docs(plan): v2.0.3 Self-Install — issue #25 (refs #25)

Branch `feature/v2.0.3-self-install` cut from main + pushed.

## Next tick
Execute Slices 1-2 of this plan on `feature/v2.0.3-self-install` →
~6 commits, ~600 LOC, end-to-end backend complete.

## STATE.md update needed
- Active branch: `feature/v2.0.3-self-install`
- Active slice: pre-flight done, ready for Slice 1
- Open override issues remaining: #25 (in-flight), #26, #27
