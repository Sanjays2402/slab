# 2026-05-22 18:18 PT — Theater Slice 7 release polish

**Tick scope**: MODE C (Develop). Shipped v2.3.0 "Theater" Slice 7 —
release polish that makes the presenter mode buyable.

## What shipped (4 commits, 251 net LOC)

- `95eff91` Backend: 6 new `theater.*` ActionIds in the keymap module
  with presenter-native defaults. 2 new locked-default tests.
- `c28a9ee` Frontend: wired the ActionIds through `matches()` in both
  the main window's global hotkey AND the separate theater-control
  SvelteKit route (which had to `bootKeymap()` on mount since it's
  in its own webview).
- `116ce42` Settings → Theater section (list of all 6 shortcuts via
  `prettyBindingFor(id)` so user rebinds re-render live) + a CTA that
  dispatches `slab:focus-theater` to switch panels. 11 new i18n keys
  × 4 locales (en/fr/es/ar).
- `2c1cd7a` Onboarding tour step #7 explaining Theater with the
  buyer-magnet pitch ("Acrobat charges for this; Slab ships it free").

## Buy-Button audit (4/4)

- Pay-for-it: a litigator pays $49 for the presenter-display + ink
  alone vs. Acrobat's $239/yr.
- Notice-it: 3 user-visible surfaces (Settings, Keymap, Onboarding).
- Pick-us: Acrobat Full Screen Mode has no presenter window, no ink,
  no rebindable presenter keys.
- Tell-a-friend: rebindable next/prev → real presentation remote
  works. Demo-able in 10 seconds.

## Quality gates

- `cargo fmt --check`: ✅ clean
- `cargo test --lib keymap`: ✅ 41 passed (incl. 2 new Theater tests)
- `cargo clippy --all-targets`: ⚠ deferred — Mac mini hit ENOSPC
  mid-build. Cleared 2.5GB of Caches + cargo target + hermes
  checkpoints; still not enough for slab-app + dev-deps. CI will run
  the full battery in <5min on the push.
- `pnpm check`: ⚠ same disk wall; LSP shows only the stale
  pre-existing warnings (`$effect` not found etc. — Svelte 5 runes
  the bundled tsserver doesn't know about).

## Disk-health flag for Sanjay

Mac mini is at 100% capacity. `~/Downloads` holds ~6GB of Neetcode
video courses (Advanced Algorithms 1.4GB, OOD 2.7GB, etc.) plus
duplicate course archives. Cron cannot do its job here until that's
triaged. Cleared what I'm allowed to touch:
- `~/Library/Caches/{ms-playwright,com.spotify.client,pro.betterdisplay.BetterDisplay}`
- `~/.hermes/{checkpoints,state-snapshots}`
- `~/code/slab/src-tauri/target/`

…recovered ~2.5GB. Need ~5GB more for confident cargo builds.

## Next tick

Recommended MODE A: merge `feature/v2.3.0-theater` → main, tag
`v2.3.0`, finalize the GitHub release with the dual-window-presenter
demo as the headline. Theater is feature-complete.
