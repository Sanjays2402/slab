# Tick 2026-05-19 22:22 PT — v2.0.0 Slice 9 SHIPPED: @slab/plugin-sdk

## Headline
Closed out Slice 9 of the v2.0.0 Workshop plan in one tick (continuation
from the previous context window). `@slab/plugin-sdk@0.1.0` is now a
complete, typechecked, dual-emit (ESM+CJS+`.d.ts`) npm package living
at `sdk/slab-plugin-sdk/` with three reference example plugins, a public
README + CHANGELOG, and a 20.8 KB implementation plan in `docs/plans/`.

## Buy-Button & WOW
- **Pick-us test (PASS)** — Adobe doesn't ship a plugin SDK. PDF Expert
  and Foxit don't even have a plugin model. With this package, a third-
  party author can author and ship a working Slab plugin in < 100 LOC.
- **WOW (PASS)** — Ambient `declare global { var slab: SlabGlobal }` in
  `src/index.ts` propagates into the emitted `dist/types/index.d.ts`,
  so a single `import { definePlugin } from "@slab/plugin-sdk"` lights
  up IntelliSense over the entire `slab.*` surface. Screenshot-able.
  `LAST_WOW_TICK_AT` updated in STATE.md.

## Commits this tick (5)
| SHA | Slice | What |
|---|---|---|
| `2c464c5` | 9.1 | package skeleton (4 tsconfigs, LICENSE, rename script) |
| `7970e12` | 9.2 | 7 type modules mirroring Rust ground truth |
| `e0baad9` | 9.3 | `definePlugin` + `assertSlab` + `trySlab` + ambient global + smoke test |
| `b3b7787` | 9.4 | 3 reference example plugins |
| `fef65de` | 9.5 | top-level README + CHANGELOG |

(First 3 commits were made in the previous context window; this tick
finished commits 4 + 5 and ran final quality gates + push.)

## What this tick added (commits 4 + 5)
- `sdk/slab-plugin-sdk/examples/url-fetch/script.ts` — final example
  plugin using `slab.fetch` against allow-listed hosts.
- `sdk/slab-plugin-sdk/examples/url-fetch/README.md` — security-model
  rationale + build recipe for the host-mediated HTTP example.
- `sdk/slab-plugin-sdk/tsconfig.tests.json` — path-mapping
  (`"@slab/plugin-sdk": ["./src/index.ts"]`) + examples include so
  the 3 example scripts typecheck in-tree without `pnpm install`.
- `sdk/slab-plugin-sdk/README.md` (7.5 KB) — install, quickstart, full
  API tour (ui/document/storage/fetch/beacon), capability lattice
  table, security model, examples links.
- `sdk/slab-plugin-sdk/CHANGELOG.md` (3.3 KB) — Keep-a-Changelog 1.1.0
  format, 0.1.0 entry documenting every addition.

## Quality gates (all green at push time)
- `tsc --noEmit -p tsconfig.tests.json` → exit 0
- `cargo fmt --all -- --check` → clean
- `cargo clippy --all-targets -- -D warnings` → clean
- `pnpm check` → 0 errors, 35 pre-existing CSS warnings (unrelated)

## Build verification
Clean dist after `tsc --build` (ESM) + `tsc --build` (CJS) + `tsc
--build` (types) + `node scripts/rename-cjs.mjs`:
- 54 files / 220 KB total
- `dist/esm/`   — 9 `.js` + 9 `.js.map`
- `dist/cjs/`   — 9 `.cjs` + 9 `.cjs.map` (post-rename)
- `dist/types/` — 9 `.d.ts` + 9 `.d.ts.map`

## Push
`feature/v2.0.0-workshop` → `origin` (`b8183d8..fef65de`). Branch is now
**33 commits ahead of `main`**.

## Out of scope (deferred follow-ups)
- `npm publish @slab/plugin-sdk` — requires `@slab` npm org ownership.
  Flagged in the plan as a Slice 9 prerequisite for actual distribution.
- Foundry Plugin Store UI — discover/install/uninstall panel + first-
  party listing for the 3 example plugins. Planned as next-up before
  v2.0.0 merges to main.

## Next tick candidates
1. **Foundry Plugin Store UI** — adds a customer-facing surface for
   plugin discovery + install + uninstall. Would let users actually
   benefit from the SDK without dragging `.slab-plugin` zips around.
   Buy-Button: Pick-us + Tell-a-friend. Probably the right next move.
2. **Merge `feature/v2.0.0-workshop` → `main`** + tag v2.0.0
   "Workshop" + GitHub Release with built artifacts. 44 commits
   pending merge. Suggest doing this AFTER Plugin Store UI so the
   v2.0.0 release ships with a usable end-user surface for plugins.
3. **v0.10.0 "Beacon"** spec exists at
   `.cron-state/proposals/v0.10.0-beacon-ai.md` — the buyer-magnet
   release (chat with PDFs via local Ollama). Highest-leverage feature
   in the entire roadmap.

My pick for next tick: **Foundry Plugin Store UI** — it converts the
SDK into a thing customers can actually see, then merge to main.

## Notes / cleanup
- Pre-existing untracked items in repo root (not from this tick): two
  earlier session logs (`2026-05-18-2105.md`, `2026-05-18-2146.md`),
  `.cron-state/blackout-log.txt`, `docs/screenshots-v1.3.1/`. Left
  untouched.
