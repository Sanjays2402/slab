# Round 51 — 2026-06-28 11:40 PT (Cake cron)

5/5 frontend slices shipped, gated once, pushed to origin/main (016c510..d465273).

## Disk emergency at tick open (recovered ~3 min)
Both /Volumes/Projects (repo) + /Volumes/SlabBuild failed to mount:
`mount_apfs: Input/output error` (errno 5); macOS auto-repair failed (0x8);
`fsck_apfs -n /dev/disk6` -> "failed to read container superblock".
ROOT CAUSE: at 11:19:58 (30s before the lock acquired) the exFAT
"Sanjay SSD" fskit_agent (pid 4864) was killed and the drive remounted,
leaving the already-attached SanjayProjects/SlabBuild sparseimages with
STALE block mappings against the old mount. The image files themselves
read fine at the VFS layer (head -c confirmed) — only the device mapping
was stale.
FIX: `hdiutil detach -force /dev/disk5 /dev/disk7` (both stale image
devices) + fresh `hdiutil attach` of each sparseimage against the current
exFAT mount. Both mounted clean, repo intact, on main, in sync. NO fsck
needed (distinct from round-50's genuine superblock corruption).

## Shipped (each = 1 commit, own tested pure core)
1. eac91db  feat(ocr-queue): name the in-flight doc under the bulk progress bar
   describeInFlightDoc(path,action) -> "Running <name>…"/"Re-queuing <name>…"
   reusing ocrBasename; currentDocPath set each loop iteration, cleared in
   finally; aria-live caption under BOTH Run-all + Requeue-all bars.
   ocrQueueView 212->221.
2. a82b520  feat(palette): Alt-click a section header to solo-expand it
   soloExpandGroup(grouped,collapsed,group) folds every OTHER section;
   symmetric (re-Alt-click an already-solo group pops all back open);
   header onclick branches on e.altKey, persists via saveCollapsedGroups.
   paletteSearch 298->308.
3. 60105a3  feat(recents-home): persist the board sort mode across sessions
   NEW recentsView.ts shell (loadRecentSort/saveRecentSort, default
   "recent") gated on tested isRecentSortMode guard; setSort persists every
   pick. FILTER query intentionally NOT persisted. recentsHomeView 155->167.
4. 5493b3e  feat(library-search): drag-to-reorder the Saved-searches strip
   moveSavedSearch(pinned,from,to) splice (movePinned pattern on flat
   string[]), normalize+dedupe first; typed DragEvent handlers + accent
   drop-indicator. librarySearchView 251->261.
5. d465273  feat(library-search): keyboard cursor over the Saved-searches strip
   classifySavedSearchKey (plain arrows MOVE, Alt+arrows REORDER checked
   first, Enter run/Backspace unpin/Esc park); role=listbox/option + cursor
   ring + savedCursor clamp effect. librarySearchView 261->280.

## Gate (once, end of batch)
- tsx: ocrQueueView 221, paletteSearch 308, recentsHomeView 167,
  librarySearchView 280 — all 0 failed.
- pnpm check: 0 errors / 104 warnings — rounds 32-50 baseline EXACT
  (zero new a11y warnings across the in-flight caption, solo-expand header,
  sort persistence, saved-strip drag + listbox cursor).
- ZERO Rust files changed -> round-32 lib baseline (clippy clean, 2620
  tests) carries forward; full Tauri binary build never run (wedges disk).

## Spread: 4 surfaces, 1 new file (recentsView.ts), reuse over reinvention
- describeInFlightDoc reuses ocrBasename; soloExpandGroup mirrors
  collapseAllGroups iteration; recentsView.ts mirrors paletteCollapsed.ts;
  moveSavedSearch mirrors RecentsHome.movePinned; classifySavedSearchKey
  reuses nextChipCursor/clampChipCursor + the palette nav core.
- Slice 5 depended on slice 4's moveSavedSearch -> landed 4 before 5.

## Deferred: none. Refilled the round-51 roadmap (11 frontend items) in STATE.md.
