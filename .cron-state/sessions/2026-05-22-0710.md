# Tick 2026-05-22 07:10 PT — v2.7.0 "Signet" plan promoted

Mode C, writing-plans skill.

- Wrote `docs/plans/2026-05-22-v2.7.0-signet-digital-sign.md` — PKCS#7 digital
  signatures for PDFs. 8 slices + pre-flight ADR + release task. Estimated
  ~1800 net LOC + ~700 test LOC at execution, 9 commits.
- Codename **Signet ✒️** — PAdES-B-B compliant cryptographic signing with
  embedded X.509 cert chain, RSA-SHA256 or ECDSA-P256-SHA256, visible
  signature appearance, tamper detection on verify.
- Pure-Rust crypto: rsa 0.9, p256 0.13, x509-cert 0.2, cms 0.2, pkcs12 0.1.
  Zero C deps. Reuses existing lopdf 0.40 + sha2 0.11.
- 6 Tauri commands (inspect/sign/verify/generate_cert/load_p12/list_certs)
  + 3-tab SignetPanel (Sign / Verify / Manage) + G shortcut + 3 palette
  entries + CLI subcommands (`slab sign`, `slab verify`, `slab cert`).
- WOW: 320ms cubic-bezier (0.34, 1.56, 0.64, 1) gold wax-seal stamp animation
  with radial-gradient glow ripple on successful sign. Reduced-motion safe.
- Buy-Button 4/4 PASS — pay-for-it (Adobe Sign $240-720/yr replaced for
  free), pick-us (only free offline cross-platform PDF signer — Preview's
  signature is bitmap-only, PDF Expert can't sign, Foxit Mac is $159/yr),
  notice-it (new nav + G shortcut + wax-seal anim), tell-a-friend (twitter
  demo: 10-sec sign → Adobe validates ✓ → "macOS Preview can't do this").
- Pipeline position: v2.3.0 Theater → v2.4.0 Stack → v2.5.0 Quill →
  v2.6.0 Lens → **v2.7.0 Signet (this tick)** → v2.7.1 timestamps (B-T) →
  v2.7.2 LTV (B-LT) → v2.8.0 PKCS#11 smart-card.
- Self-signed-cert generator is in-scope for v2.7.0 — every first-run user
  has a working sign flow without buying a $200/yr CA cert.
- Out of scope (deferred): RFC 3161 timestamping (v2.7.1), LTV/DSS (v2.7.2),
  smart-card/YubiKey (v2.8.0), multi-sig workflow (v2.8.0).

Active branch: `feature/v2.3.0-theater` (plan files are version-independent).

Next tick options:
- (a) Theater Slice 5 — audience window rendering. Now 5 versions of plans
  ahead of shipping; ship gap is real.
- (b) v2.2.1 patch — sync package.json/Cargo.toml versions.
