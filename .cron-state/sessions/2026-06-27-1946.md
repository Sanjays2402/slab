# Round 47 — 2026-06-27 19:46 PT (Cake cron, 20-min batch)

Frontend-focus override active. Shipped 5 frontend/UX capabilities,
each with a tested pure core, across four surfaces. Explicit roadmap
follow-ups (the three named "Next FRONTEND candidates" plus the
palette group-collapse item). 5 feature commits + 1 test fixup.

## Shipped (5/5)

1. **OCR Queue — per-reason "Retry all <reason>"** (`af53f7b`)
   - Core: `collectReasonRetryIds(failed, reason)` (projects the faceted
     bucket to ids, same membership `filterByReason` decides) +
     `describeReasonRetry(reason, count)` (the "Retry N · reason" label,
     hidden at zero).
   - Wire: constructive-accent button at the end of the reason-facet row,
     shown only while a facet is active; handler loops `ocrQueueRequeue`
     over exactly those ids, then refreshes once.
   - ocrQueueView 141 -> 154.

2. **OCR Queue — determinate Run-all progress** (`2d48a21`)
   - Core: `describeRunAllProgress(done, total, pagesDone, pagesTotal)` ->
     clamped `{fraction, percent, label, finished}`.
   - Wire: switched Run-all from the blanket `ocrQueueRunAll` to a per-doc
     `ocrQueueRunOne` loop ticking a `role=progressbar` bar after every
     doc; workload snapshotted up front (denominator fixed), per-doc OCR
     failures tallied not thrown. Removed the now-unused ocrQueueRunAll
     import.
   - ocrQueueView 154 -> 169.

3. **Library Search — recent-chips keyboard nav** (`4eaedfc`)
   - Core: `classifyRecentChipKey` (Left/Right -> prev/next, Home/End,
     Enter run, Escape park; modifier-gated; Up/Down deliberately NOT
     claimed) + `nextChipCursor` + `clampChipCursor` (palette-nav adapters).
   - Wire: flat horizontal cursor over the empty-query chip strip, only
     active when the query is empty (no results to collide with); strip is
     now `role=listbox` with aria-selected options + a visible key hint.
   - librarySearchView 158 -> 180.

4. **RecentsHome — pinned-strip horizontal nav + scroll** (`f9e21e3`)
   - Core: `classifyRecentKey` now maps ArrowLeft/Right -> prev/next
     (local to the board; not part of the shared vertical palette nav) +
     `recentCardScrollOptions(section)` -> per-axis scrollIntoView
     alignment (pinned = inline center, grid = nearest).
   - Wire: focused pinned card scrolls horizontally (next card peeks in);
     horizontal arrows inside the filter box stay with the text caret.
   - recentsHomeView 110 -> 117.

5. **Command Palette — group collapse** (`440d72e`)
   - Core: `toggleCollapsedGroup` (immutable set toggle) +
     `partitionCollapsedGroups(grouped, collapsed)` -> `{display, visible,
     starts}` (headers always shown with true count + chevron; folded
     groups drop items from the visible cursor space + the jump heads).
   - Wire: each section header is now a real toggle `<button>`; the cursor
     indexes the `visible` list and group-jump uses the partition's
     `starts`, so arrows + Cmd-arrow never land on a hidden row. Collapse
     disabled while a query is active. Bonus: cursor order now == render
     order (fixes the old `filtered.indexOf` divergence). Removed the
     now-unused `groupStartIndices` import.
   - paletteSearch 253 -> 271.

## Fixup
- `5912210` test(view-cores): dropped two unused `@ts-expect-error`
  directives (NaN is type number, so `describeRunAllProgress(NaN,...)` and
  `clampChipCursor(3, NaN)` are valid TS — svelte-check flagged the stale
  directives as errors). No behaviour change.

## Gates (once, end of batch)
- ocrQueueView 169/169, librarySearchView 180/180, recentsHomeView
  117/117, paletteSearch 271/271 — **74 new tests**, all green.
- `pnpm check`: **0 errors / 104 warnings** — rounds 32-46 baseline
  preserved EXACTLY (new buttons, progressbar, chip listbox, palette
  toggle headers all clean on a11y).
- `cargo fmt --all --check`: clean. **ZERO Rust files changed** this batch
  — the round-32 lib baseline (clippy clean, 2620 tests) carries forward.
- Full Tauri binary build never run (wedges the disk, per STATE.md).

## Notes
- All 5 are TS/Svelte UI work; zero backend. Same pure-core/thin-shell
  discipline — every decision is a pure function unit-tested without a DOM.
- Slice 5 (palette collapse) was the riskiest (re-bases the palette cursor
  onto the visible list); built last, gated green, kept revertible.
- Pushed ad264f7..5912210 to origin/main; verified landed.

## Deferred / next
Refilled roadmap with 4 fresh follow-ups: OCR Run-all cancel button,
Library Search per-chip delete (Backspace on focused chip), palette
collapsed-groups cross-session persistence, RecentsHome pinned-strip
overflow fade/affordance.
