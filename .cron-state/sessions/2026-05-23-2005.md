# 2026-05-23 20:0x PT — v3.13.0 Streamline Slice 1+2+3 + Tauri/UI

Mode: C DEVELOP. Folded Tasks 1+2+3 of the v3.13.0 plan into one tick, plus
the Tauri commands and Svelte panel that make the inspector usable end-to-end
TODAY (the writer ships in subsequent ticks).

## Shipped

4 commits on `feature/v3.13.0-streamline`, 909 net LOC.

- `169907e` docs(adr): 0013 — streamline (PDF linearization / Fast Web View)
  - 88 LOC. Spec subset, reader compat targets, validation strategy.
- `f311ff6` feat(streamline): scaffold module + DTOs
  - New `pdf::streamline` module + LinearizeReport/LinearizationStatus/LinearizeStats DTOs.
- `fb914df` feat(streamline): is_linearized inspector + first-page object-graph walk
  - 374 LOC including 9 unit tests, all green.
  - `inspect::is_linearized` probes first 4 KB for /Linearized + /L + /H + /E.
  - `depgraph::first_page_reachable` walks catalog → pages → resources, bounded depth 64.
- `9268718` feat(streamline): Tauri commands + Svelte panel — end-to-end inspector
  - 374 LOC. `slab_streamline_inspect` + `slab_streamline_linearize` Tauri commands.
  - StreamlinePanel.svelte with Liquid-Glass status pill, per-stat cards, buy-button copy.
  - Sidebar + tools registry entries.

## Quality gates

- cargo fmt --all -- --check ✓
- cargo clippy --all-targets -- -D warnings ✓
- cargo test --lib: 1427 passed ✓
- pnpm check: 0 errors, 62 pre-existing warnings ✓

## Buy-button score

- **Pick-us test:** YES — Acrobat Standard ($179/yr) doesn't ship linearization,
  PDF Expert ($79/yr) doesn't either. Every web publisher and DMS vendor cares.
- **Notice-it test:** YES — new "Streamline (Fast Web View)" sidebar entry.
- **Tell-a-friend test:** the inspector ALONE is screenshot-worthy: "this 12 MB
  PDF forces readers to download all 12 MB before page 1 paints" — that's a
  wedge nobody else exposes in a free tool.

## CI

Push triggered build run 26350495175 on the feature branch. Will check next tick.

## Next tick

1. Poll CI for run 26350495175 — confirm 4-platform bundles green on the branch.
2. Execute Task 4 of the plan: linearization parameter dict builder.
3. Then Task 5: primary hint stream builder. (Fold into same tick if scope allows.)
4. Task 6 (writer) is the big one — likely its own tick.

## Notes

- Disk was tight (200 MiB free at start of tick) so ran `cargo clean` to recover
  4.9 GiB before clippy/test. Current free: ~3 GiB after the test build. Watch
  this; may need to clean again before the writer tick.
- v3.12.0 Atelier release fully shipped (release published 02:46 UTC this tick).
