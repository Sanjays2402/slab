# Round 17 — 2026-06-21 11:31 PT (Cake cron)

5/5 slices shipped, all green, all on origin/main.

## Slices

- **78** — `ddfd6ec` — feat(library): personal-preset row menu in
  Smart Folders Hub (351 LOC, SmartFoldersHubPanel.svelte).
  Six-column grid + ... menu on personal rows + inline rename +
  Notion-style popover (Rename / Duplicate / Delete) + per-row busy
  state + Escape ladder. Closes the round-16 deferred item.
- **79** — `7613923` — feat(hopper): rule coverage analyzer
  primitive (531 LOC, new src/pdf/hopper/coverage.rs +
  src/pdf/hopper/mod.rs). Pure-data compute_coverage with
  first_match + would_match + dead_at_position per rule; 15 tests
  pin conservation invariant + shadow detection + empty edge cases.
- **80** — `4bf519f` — feat(hopper): rule coverage Tauri command
  surface (+261 LOC across cmds.rs + lib.rs). slab_hopper_rule_coverage
  command sources samples from HopperLog::list_recent; 3 testable
  helpers (clamp_sample_limit / sample_over_read / samples_from_runs)
  with 13 tests pinning bounds, over-read ceiling, basename
  reduction, and edge cases.
- **81** — `c9e9b03` — feat(hopper): rule coverage TS client +
  diagnostic helpers (268 LOC, hopper.ts + new hopper.test.ts).
  Wire types + slabHopperRuleCoverage wrapper + 4 pure helpers
  (fallthroughPercent / ruleMatchPercent / ruleCoverageDiagnostic /
  summarizeCoverage). 17 inline-expect tests.
- **82** — `8467fc4` — feat(hopper): coverage panel in
  HopperRulesEditor (422 LOC, HopperRulesEditor.svelte). The
  demo-able payoff. Coverage button in header + section below the
  split with two-layer per-rule bars (would-overlay +
  first-solid), diagnostic chips, fall-through row, sample-size
  number input. Bars reshape live on rule edits via
  scheduleCoverage hooked into scheduleSave.

## Gates

- cargo fmt: clean
- cargo clippy --lib -- -D warnings: PASSED CLEAN in 15.17s (round-16
  baseline ~14.6s preserved; coverage.rs adds pure-data only)
- cargo test --lib: **2261 passed / 0 failed** (round-16 baseline
  2233 + 15 from slice 79 + 13 from slice 80 = 2261; matches exactly)
- pnpm check: **0 errors / 104 warnings** (round-16 baseline preserved
  EXACTLY)
- TS: `pnpm exec tsx src/lib/hopper.test.ts` → 17/17 ok

## Push verified

`git fetch -q origin && git log --oneline origin/main | head -7`
shows the five new commits at the top of `origin/main`:

```
8467fc4 feat(hopper): rule coverage panel in HopperRulesEditor
c9e9b03 feat(hopper): rule coverage TS client + diagnostic helpers
4bf519f feat(hopper): rule coverage Tauri command surface
7613923 feat(hopper): rule coverage analyzer primitive
ddfd6ec feat(library): personal-preset row menu in Smart Folders Hub
b01a584 chore(cron-state): log round-16 batch ...
b74a749 feat(plugins): install-log filter bar in Recent installs drawer
```

## Nothing deferred

All 5 slices landed. No half-finished work, no skipped tests, no
"will polish next tick" notes. The arc is whole.
