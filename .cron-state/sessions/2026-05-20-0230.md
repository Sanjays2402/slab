# Tick 2026-05-20 02:30 PT — v2.0.2 Workshop Marketplace Slices 1–6

## Mode
MODE C (develop). Off-hours, Wed 02:17 PT. Single autonomous tick
landing 6 plan-slices into one big vertical ship.

## What landed (3 commits on `feature/v2.0.2-workshop-marketplace`)

1. **bc895c6** `feat(marketplace): schema_version=2 with category/tag/screenshot/installs`
   - Schema bumped 1 → 2. IndexEntry + IndexEntryUnsigned extended with
     4 optional v2 fields. sign_plugin CLI gained `--category --tag
     --screenshot --installs`. 4 new tests. 923 lib tests pass.

2. **0a83d57** `feat(marketplace): embedded seed-index — Browse never blank, 10 starter plugins offline`
   - assets/marketplace/seed-index.json (7 KB, 10 entries) bundled.
     FetchOutcome::EmbeddedSeed variant. verify_entry "BUNDLED" sentinel
     short-circuit. lib.rs MarketplaceFetchResult.is_embedded_seed.

3. **6e3d634** `feat(marketplace): live-highlighted fuzzy search + category chips + sort dropdown` ⭐ WOW
   - src/lib/marketplace/fuzzy.ts (232 LOC, homemade, NOT Fuse.js).
     Browse toolbar redesign. Category chips. 4 sort modes. Result
     count. Empty-state + clear-filters CTA. Embedded-seed banner.
     Install-count pill + taxonomy chips on cards. 15 i18n strings.

## Plan deviations (documented in code)

- **No `IndexEntryV2` wrapper struct.** Direct field extension on the
  canonical `IndexEntry` with `#[serde(default, skip_serializing_if)]`
  for byte-identical wire compat with v1.
- **No Fuse.js.** Homemade matcher for offline-first ideology + score-
  aware highlights + small surface area. Documented in fuzzy.ts.

## Quality gates (all green)

- cargo fmt --all -- --check ✓
- cargo clippy --all-targets -- -D warnings ✓
- cargo test --lib → **923 passed; 0 failed**
- pnpm check → **0 errors**, 35 warnings (all pre-existing)

## Buy-Button verdict
- Pay-for-it PASS
- Notice-it PASS
- Tell-a-friend PASS (screenshot moment)

## Ship-size verdict
+1175 net LOC across 3 feature commits + 1 follow-up test file =
PASS on ship-size rule.

## WOW moment shipped today (LAST_WOW_TICK_AT bumped)

Type "redact" into Plugin Store search → every char in "Redactor",
"redact" tag, "PII redaction" description glows in accent yellow
across multiple cards simultaneously. Stripe/Linear/Raycast tier.

## Blocked
None.

## Next tick

Slice 7 (featured hero card) + Slice 8 (version bump + CHANGELOG +
release notes + MODE A merge + tag + push) of the v2.0.2 plan.
