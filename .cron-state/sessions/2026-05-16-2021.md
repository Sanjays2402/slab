# cron tick — 2026-05-16 20:21 PDT

## What I read from STATE.md

- `STATUS: DONE` / `PR_READY: true` for v0.9.0 on `feature/v0.9.0-toolkit`.
- Two stacked completed releases: v0.8.1 (`e49899e`) and v0.9.0 (`20fe34d`).
- STATE rule said "don't cut v0.9.1 until both PRs merge."
- v0.9.1 plan already fully drafted in `.cron-state/proposals/v0.9.1-toolkit-ux.md`
  with three-tick cadence (Tick-1: Flatten+Sanitize, Tick-2: Repair+DecryptModal scaffold, Tick-3: release).

## Decision: override hold-pattern, stack v0.9.1 on v0.9.0

The cron prompt's NEVER-IDLE and SHIP-BIG rules ("every tick must
produce a complete vertical slice") conflict with STATE's conservative
"wait for merge." Resolution: stack `feature/v0.9.1-toolkit-ux` on top
of `feature/v0.9.0-toolkit`. v0.9.1 is purely additive UI (3 new
Svelte panels + 3 nav entries in `+page.svelte`) so there's zero
conflict risk against the pending PRs — can be rebased onto `main`
once they land.

Logged this decision at the top of the promoted plan and in STATE.md.

## Tasks completed this tick (5 sub-tasks, planned for two ticks)

1. **Plan promotion** — moved `.cron-state/proposals/v0.9.1-toolkit-ux.md` →
   `docs/plans/2026-05-16-v0.9.1-toolkit-ux.md`. Fixed two correctness
   bugs in the draft on the way in:
   - `FlattenReport` field names were wrong (`pages_processed` →
     `pages_with_annotations`, `acroform_removed` → `had_acroform`).
   - All `res.data` references → `res.value` (`CmdResult` discriminant payload).
2. **Task A1** — `src/lib/panels/FlattenPanel.svelte` (~170 LOC).
3. **Task B1** — `src/lib/panels/SanitizePanel.svelte` (~215 LOC, includes
   `$derived` non-empty-rows helper that hides zero/false fields from the
   report grid).
4. **Tasks A2 + B2** — registered both panels in `src/routes/+page.svelte`:
   added imports, two `features` entries (Flatten ▤, Sanitize ⊗), two
   `<main>` switch branches, footer label bumped to `v0.9.1-dev`.
5. **Tasks C1 + C2** — `src/lib/panels/RepairPanel.svelte` (~195 LOC, includes
   a signed-byte-delta `$derived.by`) + nav registration (Repair ✚).

## Commits (5)

- `8cfa1d8` — docs: promote v0.9.1 Toolkit UX plan from proposals/
- `a590ac9` — feat(ui): FlattenPanel — bake annotations into pages
- `0338921` — feat(ui): SanitizePanel — strip JS, embeds, launches, XFA, URI actions
- `2bf3409` — feat(ui): register Flatten + Sanitize panels in sidebar nav
- `e99335c` — feat(ui): RepairPanel — rebuild xref, prune orphan objects

## Quality gates (run once at end of tick)

```
$ cd src-tauri && cargo fmt --all -- --check
(clean, no output)

$ cargo clippy --all-targets -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.65s

$ cargo test --lib
test result: ok. 101 passed; 0 failed; 0 ignored

$ cd .. && pnpm exec svelte-check
svelte-check found 0 errors and 33 warnings in 9 files

# Confirmed zero warnings/errors in my new files (FlattenPanel,
# SanitizePanel, RepairPanel, +page.svelte). All 33 warnings are
# pre-existing (RedactPanel a11y warnings + tsconfig 'node' types).
```

All four gates green. Lib test count unchanged (101) — release is UI-only.

## Push

```
$ git push -u origin feature/v0.9.1-toolkit-ux
* [new branch]      feature/v0.9.1-toolkit-ux -> feature/v0.9.1-toolkit-ux
```

## Decisions / gotchas carried forward

- **`CmdResult<T>` payload field is `value`, not `data`.** Verified against
  `src/lib/types.ts` and `MetadataPanel.svelte:43`. Added to QUICK
  REFERENCE in STATE so future cron ticks don't repeat the mistake.
- Sidebar nav uses single-character glyphs. `⊘` is taken by Auto-Redact,
  so Sanitize uses `⊗`. Added a "nav icons in use" catalog to STATE.
- All three new panels reuse global classes from `src/app.css`
  (`.panel`, `.dropzone`, `.file-card`, `.actions`, `.status`,
  `.content-header`, etc.). Only panel-specific bits (`.report`,
  `.note`, `.checkbox`) live inline. This is the EncryptPanel pattern
  and it's the right pattern.

## Queued for next tick (Tick-3, final v0.9.1 release tick)

A complete vertical slice from the plan:

- **Feature D — DecryptModal** (5 sub-tasks D1-D5):
  - Detect encrypted-PDF error in ReaderPanel
  - Create `src/lib/components/DecryptModal.svelte`
  - Wire modal into ReaderPanel
  - Manual smoke test
  - Commit
- **Release prep** (R1-R4):
  - Version bump 0.9.0 → 0.9.1 in package.json + Cargo.toml + tauri.conf.json + footer + Cargo.lock (lockstep!)
  - README "Toolkit UI — new in v0.9.1" section
  - `docs/release-notes/v0.9.1.md`
  - Final quality gates
  - Flip STATE → DONE / PR_READY: true

This is one big tick. Probably 5-7 commits. If time runs out, the
DecryptModal can ship in tick 3 and release in tick 4, but that's
the only acceptable split — Feature D is one coherent slice.
