# 2026-05-22 00:13 PT — MODE B finalize hotfix #2 + issue #21

**Mode**: B (finalize) hotfix — Docker CI was red blocking GHCR publish of `slab-server:v2.1.0`.

## What shipped
- `d3d9b13` fix(v2.1.0): bump Docker Rust 1.85 → 1.88 — image/plist/time/serde_with/zbus/icu_* all need ≥1.86–1.88
- `429d208` fix(i18n): add `features.citations` + `features.study` keys to `src/lib/i18n/en.json` (closes #21)
- Retagged `v2.1.0` → `429d208`, pushed (force-deleted remote tag first per protocol)

## CI
- Docker run `26274103726` triggered, in_progress at tick end.
- Desktop build CI (build.yml) does not retrigger on tag retag (`on: push` filter), already green on prior SHA.

## Issues closed
- #21 — Sidebar raw i18n keys in v2.0.2. Fix is 2 lines in en.json.

## Next tick
- Poll Docker run `26274103726`. If green, image lands at `ghcr.io/sanjays2402/slab:v2.1.0` + `:latest` — v2.1.0 fully shipped (desktop + server).
- If red again, dig deeper into the build stage (likely needs to also pin Cargo.lock).
- If finalize complete: cut `feature/v2.0.3-*` from the backlog (issues #23, #24, #25) — these are the next 3 buy-button issues.

## Note on streak
- 15 plan-only ticks → this tick is real code (2 commits, hotfix) but small. Justified: it's a finalize/unblock, not a feature. Without it, the v2.1.0 Docker image — the whole "WOW: `docker run -p 8080:8080 ghcr.io/sanjays2402/slab`" pitch — doesn't exist in production.
