# Session 2026-05-20 07:23 PT — MODE C writing-plans — v2.0.5 plan promoted ★

## Tick gate
- `TZ=America/Los_Angeles date '+%u %H'` → `3 07` (Wednesday 7am PT, BEFORE 9am blackout) → OK to work.

## Mode
- MODE C development tick — user invoked the `writing-plans` skill a fourth consecutive time.
- Pattern: every writing-plans tick promotes the next release plan in the pipeline. v2.0.2, v2.0.3, v2.0.4 already on disk; this tick: **v2.0.5 "Bookmarks" ★**.

## What I shipped
- **1 plan file**: `docs/plans/2026-05-20-v2.0.5-bookmarks.md` (~62 KB, 1932 lines, 6 slices + pre-flight, ~980 LOC end-to-end).
- **1 STATE.md update**: prepended `## TICK 2026-05-20 07:23 PT` section + bumped STATUS block to "Four release plans on disk".
- **This session log**.

## Buy-Button verdict (release-level, baked into the plan)
- **Pick-us PASS** — Adobe / PDF Expert / Foxit all ship per-page user bookmarks. Slab today maps Cmd+D to nothing in the Reader. Launch-blocker territory.
- **Notice-it PASS** — toolbar ★/☆ + ⊕ sidebar toggle + new palette group + new onboarding step all visible on first open after upgrade.
- **Tell-a-friend PASS** — drag-to-reorder bookmark slots with live thumbnails is genuinely uncommon in free PDF readers and an obvious screenshot target.

## Plan structure (one slice = one commit)
1. **Slice 0** — pre-flight: 5 verification checks (namespace, key collision, sidebar pattern, thumbnail surface, surface collision audit).
2. **Slice 1** — `src/lib/bookmarks.ts` (~250 LOC, no deps) — per-path store mirroring v2.0.4's `readingPosition.ts` line-for-line.
3. **Slice 2** — Mod+D action (real ActionId variant + ACTIONS row, NOT hardcoded) + toolbar ★/☆ button + ⊕ sidebar toggle + 8 i18n strings.
4. **Slice 3** — 280px right-rail bookmarks sidebar with 56x72 pdfjs thumbnails (lazy + cached), inline rename, hover-delete, accent-tinted current-page slot + 11 i18n strings.
5. **Slice 4 ⭐ WOW** — HTML5 drag-to-reorder with pointer-Y drop indicators (accent line above/below target slot), 220ms ease-out slot animation.
6. **Slice 5** — three command-palette entries under new "Bookmarks" group (toggle + top-12 jump), gated on new `readerCtx` Prop.
7. **Slice 6** — onboarding step #5 (★ Bookmarks), version bumps (2.0.4 → 2.0.5), CHANGELOG entry, `docs/release-notes/v2.0.5.md`, MODE A merge + tag + push + queue RELEASE_PENDING.

## Codebase discovery (decisions baked into the plan)
- Verified `Mod+D` is not already bound anywhere (`keymap/action.rs`, `keymap.ts`, `ReaderPanel.svelte` all clean).
- Verified no `bookmarks.ts` / `slab.bookmarks` namespace exists today (greps clean).
- `src-tauri/src/keymap/action.rs` exposes the right pattern — added new `BookmarksToggle` variant + ACTIONS row (matches the 19 existing entries' shape).
- Existing `<aside class="outline-sidebar">` + `class="thumbs"` + `class="info-sidebar"` + `class="annot-sidebar"` give the precedent for the new `<aside class="bookmarks-sidebar">` — same flex row, same toggle pattern.
- `ReaderPanel.svelte` is 2577 lines — added explicit "search for X near line Y" anchors throughout the plan rather than absolute line numbers, so the plan survives drift from concurrent v2.0.4 work.
- i18n: namespaces `bookmarks.*` + `palette.bookmark.*` chosen specifically to NOT collide with v2.0.4's `reader.progress.*` / `palette.resume.*` / `onboarding.memory.*`.

## Scheduling note (critical, documented in the plan)
**Do not start v2.0.5 Slice 1 until v2.0.4 ships and is tagged.** Both versions touch `ReaderPanel.svelte`, `CommandPalette.svelte`, `OnboardingTour.svelte`, and `i18n/en.json`. Strict pipeline order: v2.0.2 → v2.0.3 → v2.0.4 → v2.0.5.

## File-size estimate (baked into the plan)
~980 LOC across 6 commits, comfortably above the 600 LOC / 4 commit hard minimums.

## Quality gates
- N/A this tick — the deliverable is a markdown plan, not running code. No Rust/TS edits to compile.
- Sanity: `wc -l` on the plan = 1932 lines, file size 62.5 KB.

## Commits this tick (target)
- 1 on `main`: `docs(plans): v2.0.5 "Bookmarks" implementation plan ★`
- 1 follow-up: `chore(cron): STATE.md + session log — v2.0.5 plan promoted`

## What I did NOT update
- `LAST_WOW_TICK_AT` — this tick is a planning tick, not a wow tick. The plan *contains* a wow (Slice 4 drag-to-reorder), but the wow lands when v2.0.5 ships, not when the plan ships.

## Next tick options (in priority order)
1. **Execute** — switch from planning to building. Cut `feature/v2.0.2-workshop-marketplace`, dispatch a subagent against Slice 0 (the LF gitattributes hotfix on main). Backlog is now 4 plans deep — sufficient.
2. **Plan v2.0.6** — if Sanjay wants the backlog deeper. Candidates: per-document highlights & notes sidebar; Pinboard view (all bookmarks across all docs in one grid); bookmark export to Markdown/JSON.
3. **Wait for Sanjay's call** on the plan/execute switchover.

## Mood note
Pattern is rock-solid now: read STATE → identify next pipeline release → discover codebase surfaces → write a 1500-1900 line plan with copy-pasteable code, i18n, gates, and a MODE A merge stanza. Each plan deliberately tied to the prior one (DRY surfaces, disjoint i18n namespaces). Four plans deep, no conflicts, no waste.
