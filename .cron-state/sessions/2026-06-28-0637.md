# Round 49 — 2026-06-28 06:37 PT (Cake cron BATCH)

Shipped 5/5 frontend/UX capabilities, 5 feature commits, pushed to main.
Top demo-value items off the round-48 refill roadmap across FOUR surfaces,
each on a tested pure core. Frontend-focus override honoured (all TS/Svelte;
the only non-view-core file is recent.ts — the store field + mutation that
slice 5 genuinely needs, kept minimal and wired straight to the UI).

## Features (commit / what / tests)

1. a968670 — feat(library-search): sort toggle on the recent-search chip strip
   - sortRecentChips(chips,mode) + RECENT_CHIP_SORT_MODES + recentChipSortLabel.
   - Recent (newest ts, default) vs Results (biggest resultCount); non-mutating,
     stable arrival tie-break, finite-number coercion.
   - Panel: cursor/delete/run key off the VISIBLE sortedRecents; toggle hidden
     under 2 chips. librarySearchView 199->214 (+15).

2. 38fa4ff — feat(ocr-queue): resume a canceled Run-all with one click
   - planRunRemaining(batch,alreadyDone)->{remaining,impact} (clamped cut +
     summarizePending) + describeRunRemaining(impact)->"" when empty.
   - Panel: runAllPending takes an optional explicit batch (resume) vs live
     pending (fresh); cancel captures the un-run tail -> accent resume banner
     + dismiss. ocrQueueView 180->196 (+16).

3. ebd7399 — feat(palette): collapse-all / expand-all sections
   - collapseAllGroups(grouped) + isEveryGroupCollapsed(grouped,collapsed)
     (superset-tolerant).
   - CommandPalette: Cmd/Ctrl+E chord + footer toggle, persisted via the
     existing saveCollapsedGroups, browse-mode only. paletteSearch 271->284 (+13).

4. 93ec44c — feat(library-search): suggested-queries recovery row in the empty state
   - suggestEmptyQueries(recents,failedQuery,limit=4): excludes the failed
     query (case/space-insensitive) + dead-end chips (resultCount<=0), ranked
     by hit count biggest-first, stable tie-break.
   - Panel: one-click recovery chips when a search finds nothing.
     librarySearchView 214->225 (+11).

5. f430bfd — feat(recents-home): drag-to-reorder the pinned strip
   - orderPinnedStrip(pinned) (by pinOrder stamp, unstamped follow, stable) +
     movePinned(ordered,from,to)->paths (clamped). recentsHomeView 125->142 (+17).
   - Store: optional pinOrder field + reorderPinned(paths) that stamps it
     WITHOUT touching the global sortRecents — zero ripple to hero / palette /
     eviction. HTML5 drag + Alt+Left/Right keyboard nudge, gated on the
     default-sort/no-filter resting state. role=list/listitem + aria-label.

## Gates (run ONCE at batch end)

- tsx ALL GREEN: librarySearchView 225/225, ocrQueueView 196/196,
  paletteSearch 284/284, recentsHomeView 142/142, paletteCollapsed 10/10;
  shared cores intact (beaconCacheView 112, readerFindView 86, toastStack 254).
- pnpm check: 0 errors / 104 warnings — rounds 32-48 baseline EXACT, zero new.
  (The draggable div initially added 1 a11y warning -> fixed with
  role=list/listitem + aria-label, back to 104.)
- cargo: ZERO Rust changed -> round-32 lib baseline (clippy clean, 2620 tests)
  carries forward. Full Tauri binary build never run (wedges the disk).

## Notes / deferrals

- recent.progress.test.ts errors under Node 25: its polyfill guard
  (typeof localStorage === "undefined") is skipped because Node 25 now ships a
  built-in localStorage global that LACKS .clear(). PRE-EXISTING +
  environmental, fails identically on the baseline, NOT part of the formal
  gate. The new store path (reorderPinned + pinOrder round-trip) was verified
  separately with a proper localStorage stub — correct.
- None deferred — all 5 were genuinely solid, clean vertical slices.
- Slice 5 was the riskiest (touches the store). Mitigated by NOT changing the
  global sortRecents: pinOrder is read only by the RecentsHome strip via
  orderPinnedStrip, so the hero card, palette file list, and eviction are
  provably unaffected.
- Refilled the roadmap with 9 fresh frontend-first candidates (round-49 block).
