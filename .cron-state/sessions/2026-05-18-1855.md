# Tick 2026-05-18 18:55 PT — v1.9.2 SHIPPED + v2.0.0 proposal authored

## Mode
MODE A (merge) + MODE C (next-version pre-flight) in one tick.

## Time
Started: 18:55 PT (5min into off-hours window — blackout ended 18:00 PT)

## Decision
Descope v1.9.2 T6 (Windows-native cpal recorder) to v1.9.3 per plan §1327 explicit escape hatch. Ship Tasks 1-5 as v1.9.2 NOW. Reasoning:
- T6 is perf-only delta on Windows (PowerShell shell-out from v1.9.1 still works fine)
- Holding v1.9.2 for T6 means Sanjay doesn't get cancel/voice-to-send/model-picker on macOS+Linux either
- One more "scaffolding only" commit doesn't qualify as "big" per Sanjay's directive
- Better to ship the polish slice now, ship native Windows recorder as the headline of v1.9.3 with the real WASAPI implementation (not a `todo!()` scaffold)

## What shipped this tick

**On `feature/v1.9.2-voice-polish`:**
- `aad0b61` fix(clippy): drop useless into_iter() in list_whisper_models (uncommitted from last tick)
- `8f5d1df` chore(release): v1.9.2 — version bumps + release notes

**Merge to main:**
- `cc51f72` Merge v1.9.2 'Voice Mode: Polish' 🎙 — cancel + voice-to-send + Whisper model picker

**Tagged + pushed:**
- `v1.9.2` annotated tag at `cc51f72`
- `main` pushed with `--follow-tags`
- CI run `26071609861` started, in_progress at tick end

**v2.0.0 pre-flight (BIG ROADMAP work):**
- `.cron-state/proposals/v2.0.0-workshop.md` — full v2.0.0 "Workshop" spec (TypeScript Plugins via QuickJS/rquickjs). 12 slices, ~48 commits, ~70 backend test delta. Architecture, capability model, host API surface, risk register, all documented. Resolves STATE.md "v2.0.0 needs a real spec doc first" gate.

## Quality gates (on main post-merge)
- `cargo fmt --all -- --check` — clean
- `cargo clippy --all-targets -- -D warnings` — clean
- `cargo test --lib` — **736 passed / 0 failed**
- `pnpm check` — 0 errors, 23 pre-existing warnings

## RELEASE_PENDING
- v1.9.2 — merge SHA `cc51f72`, tag `v1.9.2`, CI run `26071609861`
- Finalize next tick (MODE B): `gh run download` + `gh release create` with 6 platform bundles.

## Next tick
1. **MODE B finalize v1.9.2** — poll CI run `26071609861`. If green, `gh release create v1.9.2` with 6 bundles (macOS arm64+x64 dmg, Linux deb+AppImage, Windows msi+nsis). Release notes at `docs/release-notes/v1.9.2.md`.
2. **Then MODE C** — start `feature/v2.0.0-workshop` from `main`, ship Slice 1 (QuickJS embedding + sandboxed console.log). Plan promotes from `.cron-state/proposals/v2.0.0-workshop.md` → `docs/plans/2026-05-XX-v2.0.0-workshop.md` first.

## STATE.md updated: yes
