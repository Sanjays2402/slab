# Session 2026-05-23 01:10 PT — v3.1.0 Loom codegen pipeline

**Cake (cron). Tick mode: MODE C — DEVELOP. 4 commits, +1684 net LOC.**

## Tick context

- Off-hours (Sat 01:10 PT). Blackout gate clear (weekday 6).
- Disk: 523 MiB free, target/ at 2.5 GB → no Rust compile this tick (per
  STATE.md plan).
- Previous tick (00:50 PT) shipped Slice 0 docs: ADR + JSON registry +
  accessibility.html marketing page.
- Strategy: ship the docs/script-only Slice 1 alternative — generate the
  Rust validate engine from JSON, wire the module skeleton in, surface
  audit trail on landing page.

## Work

1. **JSON Schema for the registry**. Draft 2020-12 schema in
   `docs/specs/matterhorn-1.1.schema.json`. Pins ids, verdicts, totals,
   uniqueness constraints. Editors with JSON-schema awareness now flag
   typos in matterhorn-1.1.json on save.

2. **Codegen script**. `scripts/loom/codegen-matterhorn.mjs`:
   - Reads + validates `matterhorn-1.1.json` against expected invariants
     (cross-checks totals vs counted conditions, verifies condition-id
     prefix matches section id, etc).
   - Emits a Rust module with Verdict enum, FailureCondition/Section/Totals
     structs, 31 per-section const arrays totalling 91 conditions, helper
     iterators, 6 in-module tests.
   - Pipes through `rustfmt --edition 2021` so output is byte-identical to
     what `cargo fmt --check` accepts.
   - `--check` mode = CI gate; fails with SHA-256 diff if matterhorn.rs is
     stale relative to JSON.
   - Wired into package.json as `loom:codegen` / `loom:codegen:check`.

3. **Generated matterhorn.rs + module skeleton**.
   - `src-tauri/src/pdf/loom/matterhorn.rs` (934 lines, @generated).
   - `src-tauri/src/pdf/loom/mod.rs` (174 lines) — pipeline docstring with
     7-stage architecture, `CoverageSnapshot::from_registry()` (bridge to
     marketing copy), 7 cross-cutting tests.
   - Wired into `src-tauri/src/pdf.rs` via `pub mod loom;`.
   - Both files verified rustfmt-clean.

4. **Landing-page transparency hook**.
   - `/accessibility.html` coverage cards now report actual counts:
     Auto 48 / Human 33 / OutOfScope 10 alongside projections.
   - Footnote links to all four artefacts: JSON registry, JSON Schema,
     generated Rust, codegen script. Procurement-officer audit trail.
   - Replaced curly quotes/dashes with HTML entities.

## Buy-Button test

Passes 4/4:
- Pay-for-it: procurement officer comparing to CommonLook ($1,800/seat)
  can audit the conformance claim end-to-end.
- Pick-us: no competitor publishes a CI-verified conformance matrix.
- Notice-it: concrete numbers replace hand-wavy projections.
- Tell-a-friend: the 4-link audit trail is screenshot material for
  accessibility consultants.

## Wow

The codegen pipeline as a unit: 333 lines of JSON → 934 lines of Rust,
byte-identically, with CI guarantee that the registry, engine, and
marketing copy can never lie to each other. That's a class of integrity
guarantee Adobe doesn't ship.

## Quality gates

Rust gates intentionally skipped — no Rust compile attempted (disk
pressure). All four new files verified rustfmt-clean via standalone
`rustfmt --edition 2021 --check`. pnpm check not run (no Svelte/TS
touched). `node scripts/loom/codegen-matterhorn.mjs --check` ran cleanly
post-write.

## Commits + push

- 45c6993 — docs(specs): JSON Schema
- bc3a48d — feat(loom): codegen script + pnpm scripts
- 7da265d — feat(loom): generated matterhorn.rs + mod.rs + wire-up
- 8c003d0 — docs(landing): coverage cards cite generated registry

Pushed to `main` (`f36e85e..8c003d0`). CI run 26327918051 queued.

## Next tick

Rust tick — Slice 1 LayoutTree. MUST start with `cd src-tauri && cargo
clean`. Plan in `docs/plans/2026-05-22-v3.1.0-loom-pdf-ua.md` line 144.
