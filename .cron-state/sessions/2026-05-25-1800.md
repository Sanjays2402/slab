# Tick 2026-05-25 18:00–18:18 PT — v3.36.0 Atlas Personal Presets

**Branch**: `feature/v3.36.0-personal-presets` (pushed to origin)
**Commits**: 4 · **Net LOC**: +1040 / -17 (non-test)

## What shipped

End-to-end vertical slice: user-saved smart-collection presets +
portable `.slabpresets` JSON pack format for sharing across teams.

1. **Backend** (`personal_presets.rs`, 545 LOC): `library_personal_presets`
   table (schema v5 migration), full CRUD, pack export/import with
   Skip-or-Rename conflict policy, 12 unit tests green.
2. **IPC** (lib.rs +97 LOC): six new Tauri commands, all emit
   `library-changed` on mutation.
3. **PresetPicker UI** (+255 LOC): "★ Personal presets" section above
   built-ins, per-card +Add and × delete, footer Import/Export pack
   buttons with browser-style blob download.
4. **CollectionsSidebar** (+36 LOC): context-menu "Save as personal
   preset…" prompt flow + version bump to 3.36.0 across Cargo.toml,
   package.json, tauri.conf.json.

## Quality gates

- ✅ `cargo fmt --check` clean
- ✅ `cargo clippy --all-targets -- -D warnings` clean
- ⚠️ `cargo test --lib` — partial: personal_presets module 12/12 green,
  full suite blocked by disk-full (host 100%, 116Mi free). Full sweep
  needed before merge to main.
- ✅ `pnpm check` — 0 errors, 105 pre-existing warnings.

## Blockers raised to Sanjay

- **Disk full** (100%, ~116Mi free) — `cargo clean` would free ~11 GB.
- **CI billing** — still failing; v3.33.0 + v3.35.0 release artifacts
  remain pending.

## Buy-Button qualification

All four tests pass:
- Pay-for-it: Adobe Document Cloud charges per-seat for shared
  templates; this is one JSON file.
- Notice-it: "★ Personal" section appears the moment they save one.
- Pick-us: Preview/PDF Expert have nothing equivalent.
- Tell-a-friend: drop `.slabpresets` → 8 collections appear in 1s.

## Next tick

Run full `cargo test --lib` once disk is freed → merge → tag → push
v3.36.0 to main.
