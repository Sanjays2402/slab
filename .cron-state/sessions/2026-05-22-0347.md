# TICK 2026-05-22 03:47 PT — writing-plans skill — v2.2.0 "Atlas" plan promoted

User invoked `writing-plans` this tick.

**State of the world**:
- v2.1.2 "Arranger" shipped end-to-end last tick (a0e37f4 merge, tag pushed, release published).
- Build CI runs `26282803602` (build) still in_progress for the bundle (linux/win/mac-x64 jobs queued; mac-arm64 + all cargo-test jobs already green).
- `gh release view v2.1.2` shows the release exists but with 0 assets — artifact upload pending next tick once CI completes.
- All 5 override issues (#23-#27) closed (per STATE.md). `gh issue list` returns empty.
- No issues to drive a feature tick — fall through to roadmap fill.

**Plan written**: `docs/plans/2026-05-22-v2.2.0-atlas-search.md` (~30 KB,
30+ tasks across 6 slices + pre-flight + final tick, ~880 LOC, 28 new
tests at execution).

**Why v2.2.0 Atlas?** The enterprise paralegal moment. After v2.0.3
(self-install) + v2.1.2 (page ops) closed the per-document gaps, the
biggest remaining paid-tier gap is **cross-document search**. Acrobat
$239/yr "Search Multiple PDFs", Foxit paid-tier catalog, Everlaw/
Relativity at $50/user/mo — Slab ships it free, offline, cross-platform.

**Architecture**: SQLite FTS5 virtual table colocated in `~/.slab/
library.sqlite` (no new deps — rusqlite bundled-sqlite ships FTS5).
unicode61 tokenizer + bm25 ranking + snippet() with `<mark>` tags.
Page-level rows keyed on `(doc_id, page_index)` so result jumps land on
the matching page directly. Cascade-delete trigger keeps fts in sync.

**WOW × 2** (planning tick — at execution time they'll fire):
1. 180ms cubic-bezier `(0.34, 1.56, 0.64, 1)` accent-tinted result-card
   slide-in cascade w/ 40ms stagger (reduced-motion safe).
2. 240ms-dwell hover thumbnail preview tooltip rendering the snippet's
   page at 160 px w/ query terms gold-highlighted. No competitor ships it.

**Buy-Button 4/4 PASS** — Pay-for-it (Acrobat Pro $239/yr replaced),
Pick-us (only free offline GUI cross-platform), Notice-it (new nav +
shortcut + palette + onboarding step), Tell-a-friend (paralegal demo:
folder-paste → 30 ms results).

**Scheduling**: 6 ticks @ 1 slice each, or 3 BIG ticks (Slice 1+2,
3+4, 5+6). Next tick: MODE B finalize for v2.1.2 (CI should be green by
then) — upload the 6 artifacts, then start v2.2.0 Slice 1+2 on
`feature/v2.2.0-atlas-search`.

**Commits this tick (2 on main)**:
- `<HASH>` docs(plans): v2.2.0 "Atlas" cross-doc search plan
- `<HASH>` chore(cron): STATE.md + session log — v2.2.0 plan promoted
