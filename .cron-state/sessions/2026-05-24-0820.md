# Cron tick — 2026-05-24 08:20 PT (Sunday off-hours)

**Branch**: `feature/v3.20.0-hopper`
**Mode**: C (develop). Shipped Hopper Tasks 4-6 + cross-cutting keymap.

## Commits (4)

- `21070ba` feat(hopper): notify watcher + debounce + parallel pipeline dispatch
- `2d3567a` feat(hopper): 7 Tauri commands + Ollama title provider + setup() bootstrap
- `b22aa46` feat(hopper): HopperPanel.svelte + sidebar entry — end-to-end Hopper UI
- `8c6723c` feat(keymap): bind hopper.open to Mod+Shift+H

## End-to-end loop reachable

Mod+Shift+H → Hopper panel → add watched folder + Atelier recipe → drop a PDF →
notify watcher debounces 1s → parallel `tokio::spawn_blocking` → pipeline runs
Atelier recipe + Ollama title suggestion (4-6 words, sanitized) → rename +
file into output dir → `hopper://run-completed` event → live UI update.
5s polling fallback for resilience.

## Tests

- `cargo test --lib hopper::` → **33 passed / 0 failed** (post Task 5).
- `cargo check --lib` clean post Task 6.
- `cargo clippy --lib --tests -- -D warnings` clean (Tasks 4-5).
- `pnpm check` → 0 errors, 66 pre-existing warnings.
- Full lib test deferred to CI due to disk pressure (100% twice this tick).

## Disk

Started ~646Mi free, dipped to 116Mi, two `cargo clean -p slab-app` cycles
recovered ~2.6 GiB total. Avoided full Tauri rebuilds; used `cargo check`.

## Next tick

Task 7: rate limiting + retry policy + dead-letter queue for failed Hopper
runs. Then Task 8: docs (`docs/hopper.md`), telemetry counters, release notes
draft, version bump 3.19.0 → 3.20.0.

CI to watch: build 26365080908 (b22aa46) currently in_progress.
