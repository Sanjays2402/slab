# Cron tick — 2026-06-22 04:14 PT (round 22)

**5 slices shipped (5/5), one cohesive arc: activity-over-time aggregate for the install log.**

This is the third install-log read-side aggregate, complementary to
the per-event timeline (which/what) and the Top plugins histogram
(which plugins). Together they cover the trio: "what / who / when".

## Slices

103. **activity timeline aggregate primitive** — `bf12da4` (+ tiny doc-
     indent fixup squashed in)
     - `InstallLog::activity_timeline(since, until, granularity)`
       returns `Vec<ActivityBucket>` (sparse, ASC by bucket_start_unix).
     - New `TimeBucketGranularity{Day,Week,Month}` enum (serde lowercase).
     - New `ActivityBucket` struct mirroring `PluginHistogramRow`'s
       per-action shape but keyed by `bucket_start_unix` instead of
       `plugin_id`.
     - New `bucket_floor_unix` helper using chrono for UTC-calendar
       flooring (Day midnight, Week ISO-Monday via
       `weekday().num_days_from_monday()`, Month first-of-month via
       `with_day(1)`).
     - 22 new tests pin: enum round-trip + parse-unknown-is-Day + serde
       lowercase, bucket_floor day/week/month + epoch edge (Week=-259200
       for Thursday 1970-01-01), activity_timeline day/week/month
       bucketing against a hand-built 3-day-spanning-2-month fixture,
       window filters since/until/empty, conservation invariant, sparse
       output (no zero-fill), ASC ordering, serde round-trip.

104. **activity timeline CSV export primitive** — `58d4a71`
     - Pure-data `activity_timeline_to_csv(buckets, granularity,
       include_header) -> String` RFC-4180 serialiser.
     - 8 columns: `granularity, bucket_start_unix, bucket_start_iso,
       installs, updates, uninstalls, failures, total`.
     - `granularity` is the FIRST column for concat-friendly downstream
       pipelines (day.csv + week.csv + month.csv reads cell 0 to
       dispatch).
     - `ACTIVITY_TIMELINE_CSV_HEADER` pub const.
     - 12 new tests pin: header opt-in + column count parity +
       documented order + granularity-on-every-row + ISO matches
       install-log byte-for-byte + preserves input order + zero
       timestamp renders 0 + total verbatim + no None/null leaks + one
       row per input + granularity distinguishes export pairs.

105. **activity timeline JSON envelope primitive** — `19b2254`
     - Pure-data `activity_timeline_to_json(buckets, granularity, since,
       until, grand_total) -> ActivityTimelineExportEnvelope`.
     - Mirrors `InstallLogExportEnvelope` (slice 60) +
       `PluginHistogramExportEnvelope` (slice 99) +
       `DrilldownExportEnvelope` (slice 93) shape.
     - Adds one extra discriminator field — `granularity` — because
       the timeline body carries per-bucket counts whose meaning
       depends on the bucket width (the first three envelopes don't
       need this; their bodies are self-describing).
     - `ACTIVITY_TIMELINE_EXPORT_SCHEMA_VERSION = 1`, parallel-
       versioned with the three sibling envelopes (independent bumps).
     - 14 new tests pin: schema_v1 + granularity field + bucket_count
       invariant + grand_total verbatim + ISO matches install-log +
       window round-trip + only-since + preserves order + owned clones
       + serde + pretty-print + empty input + parallel-versioning +
       granularity serde round-trip.

106. **activity timeline Tauri commands + TS client** — `0424f22`
     - 3 Tauri commands: read + CSV-export + JSON-export. All default
       granularity to Day. All reload via `activity_timeline()` with
       the same default ("export what you're looking at"). Registered
       in invoke_handler between the histogram exports and the
       retention commands.
     - TS surface: `TimeBucketGranularity = "day"|"week"|"month"`,
       `TIME_BUCKET_GRANULARITIES` + `timeBucketLabel`, `ActivityBucket`
       + `ActivityTimelineResult` interfaces, `getActivityTimeline`
       async wrapper.
     - Pure helper `densifyActivityTimeline(buckets, granularity)` zero-
       fills gap days/weeks/months between first + last bucket so the
       UI's bar chart has dense input (server stays sparse, cheap on
       idle corpora).
     - `advanceBucketStart` helper: day = +86_400, week = +7d, month =
       UTC-calendar +1 via `Date.UTC` (handles 28/29/30/31 +
       year-overflow).
     - `exportInstallLogActivityTimeline{Csv,Json}` lazy-import invoke
       wrappers (browser no-op).
     - `suggestActivityTimelineExportFilename` producing
       `marketplace-activity-{day|week|month}_<window>_<YYYY-MM-DD>.<ext>`
       with granularity-in-prefix + identical window-shape to
       `suggestHistogramExportFilename`.
     - 23 new pure-helper tests in marketplace.test.ts.

107. **Activity over time section with bar chart** — `9c0b0b6`
     - Collapsible "Activity over time" section in RecentInstallsDrawer,
       sibling to Top plugins, answers "WHEN was activity happening".
     - State: timelineOpen + timeline + timelineLoading + timelineError +
       timelineGranularity + timelineExport{MenuOpen,ing,Toast,
       ToastTimer}. onMount cleanup adds the timer alongside the
       existing histogram timer.
     - Loader: `refreshTimeline()` via `getActivityTimeline` keyed
       on (open|window|granularity); $effect refetches on any change.
     - Handler `runTimelineExport(kind)` mirrors `runHistogramExport`'s
       shape exactly with in-state semantics.
     - UI: Bucket width selector + Export… popover beside it inside
       `.timeline-controls`. Popover anchors DOWN+RIGHT in
       `.timeline-export-anchor` (dismiss-isolated from histogram +
       footer anchors). 96px vertical-bar chart with densified buckets
       (zero-fill gaps), per-action stacked segments (install green /
       update accent / uninstall amber / failed red, bottom-up via
       column-reverse), 1px hairline + .empty-bar opacity for empty
       buckets, date axis labels (bookends, tabular-nums), legend
       footer.
     - Escape chain: timelineExportMenuOpen FIRST → histogramExport →
       footer export → confirmingPrune → retention → filters → close.

## Gates (final run after rebase --autosquash)

- `cargo fmt --all --check`: clean.
- `cargo clippy --lib -- -D warnings`: clean (one doc-overindented-
  list-items warning fixed in slice 103 via fixup commit, autosquashed).
- `cargo test --lib`: 2393 passed / 0 failed (round-21 baseline 2345 +
  22 from slice 103 + 12 from slice 104 + 14 from slice 105 = 2393).
- `pnpm check`: 0 errors / 104 warnings (round-21 baseline preserved
  exactly).
- `pnpm exec tsx src/lib/marketplace.test.ts`: 102 inline `ok:` lines
  pass (round-21 ~54 + slice 106 sub-expects).

## Push verify

`git fetch -q origin && git rev-parse main && git rev-parse origin/main`
both = `9c0b0b6c8061597d30c91f40869a1fa91793a6ed`. Local == origin.

## Process notes

- Round 21's closing notes listed "histogram time-bucket axis" as the
  lead candidate. Round 22 shipped it as a SIBLING aggregate (not a
  histogram pivot) because the per-bucket data is keyed by
  bucket_start_unix rather than plugin_id — complementary axis, not a
  sort axis.
- Five slices, five commits, one logical subsystem — same canonical
  five-layer cadence as round 19 (drilldown CSV arc 88-91 + composite
  92), round 20 (drilldown JSON 93-96 + sort 97), round 21 (histogram
  export arc 98-102): backend primitive → CSV primitive → JSON
  primitive → commands+client (composite) → demo-able UI.
- Separate CSV-primitive + JSON-primitive slices (rather than one
  combined "exporters" slice) gives each format its own revert point
  + focused test surface.
- The clippy doc-list-overindentation fix in slice 103 was discovered
  by the post-batch quality gate (clippy is stricter than cargo
  check). Folded into slice 103 via `git commit --fixup=c6a7dc0 +
  git rebase -i --autosquash` so the slice stays self-contained on
  log inspection.
- The densifier lives on the TS side (not the Rust side) because the
  primitive should stay sparse (cheap on idle corpora) and the UI's
  row count is bounded by what fits in the chart anyway.

## Next subsystem candidates

- Hopper rule reorder-by-drag in the coverage panel (drag a dead row
  up to fix shadowing in one motion).
- Drilldown row → cross-surface filter (clicking a fall-through
  filename in the popover carries the search query into the document
  inspector).
- Loom-grade tagging explorer.
- Doc-detail metadata editor read/write surface.
- Beacon cache inspector polish (column sort by basename / model
  facet).
- Quill multi-document field-detect queueing.
- Install-log per-plugin retention override.
- Histogram hover-tooltip on bar segments showing per-action breakdown
  without forcing the user to read the legend.
