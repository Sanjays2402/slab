# Round 9 — Saved-Views Polish (5 slices)

**Tick:** 2026-06-20 09:55 PT (Cake, cron)
**Branch:** feature/v3.39.0-atlas-tag-suggest
**Pushed + verified:** local == origin 58e895b

## Slices

| # | SHA | Slice |
|---|-----|-------|
| 38 | 7774964 | update_view_filter — in-place edit; preserves id/name/created_at/sort_order |
| 39 | 128d0a2 | duplicate_view — fork the filter; auto-name "<src> (copy)" / "(copy N)" up to 999 |
| 40 | c86cc42 | set_view_pinned — schema v15 (pinned INTEGER NOT NULL DEFAULT 0 + partial index); list_views ORDER BY pinned-first |
| 41 | 8278a2a | reorder_views — atomic full-list re-stamp by position; subset reorders permitted; pin flag NOT mutated; amended after gate caught a borrow-doesn't-live-long-enough on the id-set collect |
| 42 | 58e895b | LibraryPanel rail UI — wires all four new verbs (rail-head Update button + per-row [★ pin] [◆ body] [⋯ menu] with inline rename, danger-tinted Delete, conditional Move up/down, window-click-outside dismiss) |

## Gates

- `cargo fmt --all` clean
- `cargo test --lib pdf::library::` → **381 passed / 0 failed** (+22 from round-8's 359: 3 update + 5 duplicate + 8 pin + 6 reorder)
- `cargo test --lib ai::embedding_index` → **30 passed / 0 failed** (round-8 baseline preserved)
- `cargo clippy --lib --no-deps -- -D warnings` → clean (4m16s, first cycle after a kill)
- `pnpm check` → **0 errors / 104 warnings** (same baseline as round-8; zero new from imports/handlers/popover markup/styles)

## Deferred

None — all 5 slices shipped genuinely solid features. Subsystem is end-to-end demo-able.

## Process notes

- First gate cycle wedged on two concurrent cargo invocations contending the /Volumes/SlabBuild sparse image's slow fsync (exactly the symptom STATE.md warns about). Killed both, ran serially. Tests-build then surfaced a compile error on slice 41's `reorder_views` — borrow of `stmt` didn't live long enough through the `query_map(...).collect()` chain because `stmt` was being dropped before the intermediate iterator. Fixed by explicit `let rows: Vec<i64> = ...; rows.into_iter().collect()`. Amended into slice 41's commit so each slice remains independently revertible + green.
- Pre-existing rust-analyzer false positives for `async fn` saturated the lint output on lib.rs (it can't see the package's `edition = "2021"` in `Cargo.toml`) — ignored throughout. The actual cargo build sees no such errors.
- LSP type cache lag for the widened SavedViewRecord on the svelte side surfaced as "Property 'pinned' does not exist" warnings during the edits; `pnpm check` against the live source returned 0 errors so the warnings were stale.

## Next subsystem candidates

- Hopper backfill progress surface (the panel fires but doesn't show per-doc progress live)
- Plugin marketplace UI (backend ships in marketplace/ but only PluginsPanel's Browse tab surfaces it — no install history, no per-plugin detail)
- Smart-folders hub UI polish (the rail's drag/pin chrome could be tightened)
- Saved-views drag-handle UI (the reorder backend takes positional lists; a real drag affordance is a pure-frontend follow-up if Sanjay wants it)
