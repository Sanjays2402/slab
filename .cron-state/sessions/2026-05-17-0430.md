# 2026-05-17 04:30 — MODE B finalise + MODE C bigslice

## Outcome
- 🚀 **v0.12.0 Atlas RELEASED** on GitHub with all 6 installers (aarch64.dmg, x64_macos.dmg, amd64.deb, amd64.AppImage, x64_en-US.msi, x64-setup.exe). Tag `v0.12.0`, run `25989140987` green, release URL https://github.com/Sanjays2402/slab/releases/tag/v0.12.0
- 🚀 **v0.13.0 Lens Slice 1 shipped** (scan-audit + Reader auto-OCR banner) on `feature/v0.13.0-lens` in 4 commits.

## What changed this tick

### MODE B: v0.12.0 release finalise
- Polled CI: success on all 7 jobs.
- `gh run download 25989140987` → 6 installers staged in `assets/v0.12.0/` (renamed x64 mac dmg to `_x64_macos.dmg`).
- `gh release create v0.12.0 --latest --notes-file docs/release-notes/v0.12.0.md` → all 6 assets uploaded in one shot (no AppImage timeout this time, lucky).

### MODE C: v0.13.0 Lens Slice 1
- 9-slice plan written to `.cron-state/proposals/v0.13.0-lens.md`.
- Branch `feature/v0.13.0-lens` created off `main`.
- Commits:
  - `885390e` docs(lens): promote plan + mirror v0.12.0 release notes into `docs/release-notes/`.
  - `d4905e2` feat(lens): `pdf::scan_audit` backend — 4-way page classification + Document recommendation. 10 unit tests. New `make_image_only_pdf` and `make_mixed_pdf` test fixtures.
  - `b53e0e5` feat(lens): `slab_scan_audit` Tauri command + TS bindings `src/lib/lens.ts` (DTO mirrors, `slabScanAudit()`, helpers).
  - `dfb2061` feat(lens): ReaderPanel auto-OCR banner — non-modal, top-center, slate gradient + 220ms slide-in, [OCR now] hooks into existing runOcr.

## Quality gates (on `feature/v0.13.0-lens`)
- `cargo fmt --all -- --check` ✓
- `cargo clippy --all-targets -- -D warnings` ✓
- `cargo test --lib` — 283 passed (273 → 283, +10 scan_audit tests)
- `pnpm exec svelte-check` — 0 errors / 28 (pre-existing) warnings

## Next tick
Continue v0.13.0 Lens. Big options:
- Slice 3: Table extraction → CSV (pdftotext -layout + column aligner). Pure backend.
- Slice 2: Library auto-OCR queue (registry column + background worker + LibraryPanel pill).
- Slice 5: Vision Q&A in Beacon (llava via Ollama).

Recommend Slice 3 next — biggest user-visible payoff, no new sidecars, isolates the rasterless extraction story.
