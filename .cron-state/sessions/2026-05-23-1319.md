# 2026-05-23 13:19 PT — Signet Tasks 1+2+3 (foundation tick)

3 commits on `feature/v3.10.0-signet`:
- c051a24 chore(signet): vendor RustCrypto CMS deps + ADR 0011
- f013237 feat(signet): SigningIdentity PEM loader (RSA / P-256 / P-384), 8 tests
- edcdc89 feat(signet): TrustStore + chain status (Trusted/Untrusted/Expired/NotYetValid/ChainBroken/SelfSigned), 9 tests

Net: ~1100 LOC of non-test code + 17 new tests. cargo test --lib: **1346 passing** (+17). cargo fmt + clippy + pnpm check all green.

## Buy-Button — honest verdict: foundation, NOT a ship yet
- Pay-for-it ❌ no end-user capability yet. PEM loader + trust store are plumbing.
- Notice-it ❌ no UI surface.
- Pick-us ❌ Acrobat still wins until sign+verify lands.
- Tell-a-friend ❌ nothing to screenshot.

This tick was deliberately scoped to risk-reduce the CMS work. Tasks 4 (sign pipeline) and 5 (verify) need the `cms` 0.2 builder which has a finicky generic API — better to land identity + trust on solid ground than write broken crypto under time pressure.

## Next tick — Task 4 (sign pipeline, ~600 LOC end-to-end)
- `cms_blob.rs` — `build_pkcs7_detached(digest, identity, time)` using cms::SignedDataBuilder + SignerInfoBuilder. Reference: `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/cms-0.2.3/tests/builder.rs` line 86–156.
- `sign.rs` — prepare_signature_field + sign_pdf with byte-range splice.
- Tauri command + minimal SignetPanel.

Once sign+verify both work (likely tick after next), v3.10.0 hits all 4 buy-button checks and is ready to release.

## Ops
- Disk dropped from 5.2 Gi → 1.7 Gi free during this tick. `target/` rebuilt from scratch.
- LAST_WOW_TICK_AT: 2026-05-23T18:20Z (Quill press-roller, still within 24h).
