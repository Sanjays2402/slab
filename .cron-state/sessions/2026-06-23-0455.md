# 2026-06-23 04:40-04:55 PT — round 28 BATCH (5 slices)

Shipped FIVE slices closing one cohesive arc: Hopper dead-rule
fix-it action (slices 133-137). The natural successor to round
27's diagnose+drill loop — round 27 let a user SEE which rules
are dead and drill into their empty bucket; round 28 lets them
FIX it in one click.

## Slices shipped (5/5)

- `9e7efeb` — slice 133: dead-rule reorder planner primitive
  (Rust). plan_dead_rule_reorder(rules, report) ->
  Vec<ReorderProposal> with EARLIEST-Always target heuristic +
  index-zero fallback + target_index < rule_index invariant +
  stale-row defence + snake-case serde round-trip pin.
  12 tests.

- `4052d75` — slice 134: planner TS mirror + helpers.
  planDeadRuleReorder 1:1 mirror + applyReorderProposal pure
  utility returning NEW array with shared rule object identity
  + no-op guards + formatReorderProposal discriminated copy
  (with/without shadower, plural-aware, zero-recovered,
  empty-name positional fallback). 39 inline tests.

- `37945b9` — slice 135: planner Tauri command + TS wrapper.
  slab_hopper_plan_dead_rule_reorder wired into invoke handler
  + slabHopperPlanDeadRuleReorder async wrapper with browser-
  mode delegation. 6 wrapper-delegation tests pinning all 5
  ReorderProposal fields.

- `55bd526` — slice 136: reorder-proposal confidence
  classifier. Pure helper reorderProposalConfidence(proposal)
  -> "high"|"medium"|"low" composed from proposal alone +
  whitespace-trim contract + filterProposalsByConfidence with
  threshold ranking + describeReorderConfidence discriminated
  copy with tier-discriminative phrasing
  ("Confident"/"Structurally"/"Aggressive") pinned by
  substring tests. 21 inline tests.

- `6383a35` — slice 137: demo-able UI. "Fix it · +N" pill on
  each dead-row chip (sibling of cov-row button to avoid
  nested HTML buttons, negative-margin overlay) + confidence-
  tier color treatment (.conf-high green / .conf-medium
  orange / .conf-low muted) + 280px confirm popover with
  formatted-proposal copy + confidence-tier subline +
  Cancel/Apply + optimistic apply via applyReorderProposal
  then persist via slabHopperSetRules with rollback on
  failure + applied-toast shares cov-export-toast surface +
  Escape chain extended (fix-it > Export menu > drilldown >
  filter clear) + ~150 lines scoped CSS.

## Gates (end-of-batch ONCE)

- cargo fmt --all: clean
- cargo clippy --lib -- -D warnings: PASSED CLEAN in 11.88s
- cargo test --lib: 2567 passed / 0 failed (round-27 baseline
  2555 + 12 planner tests for slice 133 = 2567)
- pnpm check: 0 errors / 104 warnings (round-27 baseline
  preserved EXACTLY)
- tsx src/lib/hopper.test.ts: 256 inline expects pass
  (round-27 baseline 190 + 39 from slice 134 + 6 from slice
  135 + 21 from slice 136 = 256; slice 137 is UI-only)

## Push

Pushed origin/main: `16c7971..6383a35` (5 feature commits).
Verified via `git log --oneline origin/main`.

## Deferred / candidates for next round

- **Reorder-by-drag in the coverage panel** (round 27's
  deferred candidate, now paired with the fix-it pill as a
  richer alternative for multi-rule reorganisations — the
  pill saves a click on the single-fix case, drag wins on
  the multi-rule rearrangement case).
- **Undo the fix-it** ("Undo" button in the toast that pops
  the chain back to its prior state via the captured `prev`
  snapshot — same pattern as the personal-preset duplicate
  toast).
- **Batch fix-it** ("Fix all dead rules" button on the
  chain-health chip that walks proposals in input order and
  applies them one at a time with a debounced refresh between
  each — the planner's per-proposal heuristic IS
  independent-against-the-original-chain so applying multiple
  in sequence may produce unexpected interactions; this
  needs an interleave-with-coverage-refresh design).
- Drilldown row -> cross-surface filter (clicking a
  fall-through filename in the popover carries the search
  query into the document inspector).
- Loom-grade tagging explorer, doc-detail metadata editor
  read/write surface, Beacon cache inspector polish, Quill
  multi-document field-detect queueing, histogram hover-
  tooltip on bar segments, per-plugin "Run prune now"
  (round 25's deferred candidate).
