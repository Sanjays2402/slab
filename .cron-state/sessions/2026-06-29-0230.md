# Round 56 — 2026-06-29 02:30 PT

5/5 frontend/UX slices, 5 feature commits, across FIVE surfaces. From the
round-55 candidates list. Gated once at end; all green; pushed to main.

1. a5ee2c2  feat(reader): keyboard-drive the thumbnail hover-preview
   - classifyThumbPreviewKey + nextPreviewPage in readerThumbView.ts;
     Up/Down step prev/next, Home/End to ends, Esc dismiss; focus follows.
   - readerThumbView 16->34 tests.
2. 8785226  feat(beacon): show-dead-weight chip for mixed-model indexes
   - dominantModel + filterDeadWeight; chip filters to non-dominant PDFs;
     mutually exclusive w/ per-model facet, cleared by Clear-filters.
   - beaconCacheView 145->152 tests.
3. 61610f1  feat(library-search): persist per-pin sweep yield badge
   - new pinYields.ts store + pinYieldBadge/mergeSweepYields; dry pins show
     muted-amber "0" on open; live recent counts still win.
   - librarySearchView 308->319 tests.
4. 373bec8  feat(smart-folders): first-load shimmer skeleton for the hub
   - 6 hub-row-shaped shimmer rows, aria-busy, reduced-motion; matches
     collections pattern. (skeleton-only, no pure core)
5. 94b5235  feat(convert): drop-zone shimmer skeleton while pdf.js loads
   - dashed-frame skeleton replaces dead disabled "Loading" button; reusable
     dz-skel-* in app.css. (skeleton-only, no pure core)

GATES: readerThumbView 34, beaconCacheView 152, librarySearchView 319,
paletteSearch 327, ocrQueueView 247, recentsHomeView 167 (1246 total);
pnpm check 0 errors/104 warnings (baseline exact); cargo fmt clean; zero
Rust changed. Pushed 923f06c..94b5235.

DEFERRED: OCR Queue facet collapse (two render sections, merge risk);
RecentsHome cover-flow zoom (component complexity) — both refilled.
