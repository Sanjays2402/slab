# Tick 2026-05-22 00:20 PT — Docker CI hotfix #3 + issue #23 shake UX

## Gate
`TZ=America/Los_Angeles date '+%u %H'` → `5 00` (Friday midnight PT, off-hours ✅).

## Pre-flight
- `git status` → clean on `main` at `fe4b7a0`.
- `gh run view 26274103726` → Docker CI **red** at gdk-sys build-script (pkg-config could not find gdk-3.0). Tauri pulls these in non-optionally; needed system libs.
- `gh issue list` → #23 (password-prompt) was the highest-priority `v2.0.3 + good-first-issue` ticket. Slab already had `DecryptModal` 80% wired but missing typed error + retry counter from acceptance criteria.

## Work
### Commit 1 — `e10aa1b` Docker base + system deps
- `Dockerfile`: `bookworm` → `trixie` (webkit2gtk-4.1 ABI), added `libgtk-3-dev`, `libwebkit2gtk-4.1-dev`, `libsoup-3.0-dev`, `libjavascriptcoregtk-4.1-dev`, `libayatana-appindicator3-dev`, `librsvg2-dev`, `libxdo-dev`. Runtime stage unchanged.

### Commit 2 — `0bee4e2` Backend WrongPassword
- `src-tauri/src/pdf.rs` — `PdfError::WrongPassword` variant.
- `src-tauri/src/pdf/encrypt.rs` — `is_wrong_password_err()` matches `lopdf::Error::InvalidPassword` typed variant + lowercase fallback. `decrypt()` rewired to surface typed variant on both load and runtime-decrypt failure paths.
- 3 new tests: `decrypt_with_wrong_password_returns_wrong_password_variant`, `decrypt_missing_input_returns_input_missing`, `decrypt_empty_password_on_encrypted_file_is_wrong_password`. All pass.

### Commit 3 — `93020a3` Frontend shake retry
- `src/lib/components/DecryptModal.svelte`:
  - State: `attempts`, `shaking`, `locked`. `MAX_ATTEMPTS = 3`.
  - `triggerShake()` toggles `shaking` via `queueMicrotask` so consecutive wrong attempts re-fire the animation.
  - `submit()` detects wrong-password via new `isWrongPasswordMessage()`, increments attempts, shakes, auto-clears + refocuses, locks at 3.
  - CSS: `@keyframes slab-decrypt-shake` (500ms cubic-bezier 0.36/0.07/0.19/0.97, 3-cycle 6→4→2 px lateral), `.err-input` danger ring, `@media (prefers-reduced-motion)` short-circuit.
  - A11y: `role="alert"` + `aria-live="polite"` on the status div.

### Commit 4 (this) — STATE + session log
- STATE.md status block rewritten with tick summary.

## Quality gates
- `cargo fmt --all -- --check` clean.
- `cargo clippy --all-targets -- -D warnings` clean.
- `cargo test --lib` → **926 passed**, 0 failed (+3 new tests vs prior 923).
- `pnpm check` → 0 errors, 35 warnings (all pre-existing in unrelated files).

## Issue activity
- `gh issue comment 23` — posted acceptance breakdown w/ SHAs.
- Acceptance #3 (re-encrypt on Save) explicitly deferred to a follow-up tick.

## Delivery
ONE tick, 4 commits, ~95 net LOC backend (+57) + ~80 LOC frontend (+71) + 11 LOC Dockerfile + STATE update. Buyer-button: 3/4 PASS (Pick-us, Notice-it, Tell-a-friend). WOW = the shake animation.
