# Tick 2026-05-21 23:48 PT — MODE B finalize v2.1.0 + Docker CI hotfix v2

**Outcome**: v2.1.0 release is now PUBLIC with 6 desktop artifacts. Docker
build CI was still failing on prior SHA (`73ebf45`) — diagnosed and fixed.

## What happened

1. Polled prior tick's CI runs:
   - Desktop build `26272331779` → ✅ all 7 jobs green (cargo tests +
     4 bundle jobs).
   - Docker `26272340751` → ❌ failed. Root cause: `dlopen2_derive 0.4.3`
     entered the dep tree (via tauri → wry → dlopen2) and requires Cargo
     feature `edition2024`, stabilized in Rust 1.85. Our Dockerfile
     pinned `RUST_VERSION=1.83`. GitHub Actions runners ship newer Rust
     so desktop CI didn't notice.
2. Downloaded all 4 bundle artifacts from desktop CI run `26272331779`
   to `/tmp/slab-v2.1.0/`.
3. Uploaded all 6 final installers to GH release `v2.1.0`:
   - `Slab_2.1.0_aarch64.dmg` (17.9 MB)
   - `Slab_2.1.0_x64.dmg` (19.4 MB)
   - `Slab_2.1.0_amd64.deb` (228.7 MB — fat AppImage included)
   - `Slab_2.1.0_amd64.AppImage` (228.7 MB)
   - `Slab_2.1.0_x64-setup.exe` (NSIS)
   - `Slab_2.1.0_x64_en-US.msi`
4. Committed Dockerfile fix `RUST_VERSION 1.83 → 1.85` → `7267cd7`.
5. Pushed main → deleted + re-created tag `v2.1.0` to `7267cd7`.
6. `gh release edit v2.1.0 --draft=false` → release is now PUBLIC.

## CI retry in_progress
- Docker: `26273132933`
- Desktop (re-run from retag): `26273126709`

Both will be polled next tick. The Docker image won't land in GHCR until
the rebuild succeeds; the desktop installers are already live on the
release page, so customers can download Slab v2.1.0 right now.

## Files touched
- `Dockerfile` (1 line)
- `.cron-state/STATE.md` (STATUS rewrite)
- this session log

## Commit
- `7267cd7` fix(v2.1.0): bump Docker Rust 1.83 → 1.85 (dlopen2_derive needs edition2024)
