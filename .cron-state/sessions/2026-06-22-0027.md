# Round-21 — Top plugins histogram audit-export arc

**Tick:** 2026-06-22 00:27-00:50 PT (~23 min)
**Lock:** acquired 2026-06-22 00:27:40 PDT, released at end of tick.
**Result:** 5/5 slices shipped, one cohesive arc. All gates green.
Pushed + verified at origin/main `518f261`.

## What shipped (5 slices)

All five slices close one cohesive arc — the Top plugins histogram
audit-export loop. Same four-layer pattern as round-19 (drilldown
CSV 88-91) + round-20 (drilldown JSON 93-96) plus a UI composite.

| Slice | SHA       | LOC | What                                                                |
|-------|-----------|-----|----------------------------------------------------------------------|
| 98    | `aee2c75` | 245 | `plugin_histogram_to_csv` RFC-4180 primitive + 12 tests             |
| 99    | `743ec95` | 267 | `plugin_histogram_to_json` envelope primitive + 13 tests            |
| 100   | `e624e48` |  95 | Two Tauri commands (CSV + JSON) registered in invoke_handler        |
| 101   | `94ab3ec` | 240 | TS client + `suggestHistogramExportFilename` + 11 tests             |
| 102   | `518f261` | 219 | Demo-able UI: Export… popover beside Sort by + toast + plumbing     |

Total: 5 commits, ~1066 LOC across Rust + TS + Svelte.

## Gates result

- `cargo fmt --all --check`: exit 0 on first run (no fmt fixups
  needed — the patch tool's snake_case suggestions caught one
  before commit, slot-formatted fixups absorbed inline).
- `cargo clippy --lib -- -D warnings`: PASSED CLEAN in 14.68s
  (round-20 baseline 13.23s — pure-data CSV + JSON serialisers +
  thin command wrappers + UI-only popover add no new clippy
  surface).
- `cargo test --lib`: 2345 passed / 0 failed / 0 ignored
  (round-20 baseline 2320 + 12 from slice 98 + 13 from slice 99 =
  2345).
- `pnpm check`: 0 errors / 104 warnings (round-20 baseline
  preserved EXACTLY — zero new warnings from the export wrappers,
  ext-aware suggest helper, button + popover + toast wiring,
  scoped CSS additions).
- `tsx src/lib/marketplace.test.ts`: 54 inline expects pass
  (round-20 ~40 + 11 from slice 101 + 3 implicit from opts
  iteration = 54).

## Deferred

Nothing deferred this tick — all five planned slices landed
cleanly.

## Notes

- Round-20 closing notes listed "histogram time-bucket axis" as
  a next-tick candidate. Round 21 instead closed the audit-export
  symmetry first — the histogram had a SORT axis (round-20 slice
  97) and READ surface (round-18 slice 87) but no SAVE path, so
  it was the most-obvious unfinished symmetry relative to the
  drilldown popover (which already had CSV + JSON since rounds
  19+20).
- The `PluginHistogramExportEnvelope` schema_version=1 matches
  the install-log envelope's v1 today, but they are
  PARALLEL-versioned (a future shape change bumps one independently
  of the other). A test pins the v1==v1 equality so a careless
  joint bump surfaces immediately.
- Two distinct Export… popovers now live in the drawer (footer
  install-log + histogram body). They share the `.export-menu`
  styling but have SEPARATE state cells + separate anchor classes
  (`.export-anchor` vs `.histogram-export-anchor`) so outside-
  click dismiss on one doesn't fire-close the other. Escape chain
  prioritises the histogram popover (more recently opened, closer
  to user attention).
- The `marketplace-top-plugins_` filename prefix groups the
  histogram exports next to the install-log exports (slice 61)
  in a directory listing — sort by name shows
  marketplace-history_*.csv first, then marketplace-top-plugins_*
  (natural reading: events that drove the aggregate, then the
  aggregate itself).
