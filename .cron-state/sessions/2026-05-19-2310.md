# TICK 2026-05-19 23:10 PT — MODE A v2.0.0 "Workshop" RELEASE 🚀

**Branch**: `main` (post-merge HEAD `c270a8d`).
**Tag**: `v2.0.0` (annotated, pushed).
**CI run**: `26145019423` (queued at push time).
**Merge SHA**: `5c1354e`.

The largest single Slab release. 45 commits from
`feature/v2.0.0-workshop` merged to `main` via `--no-ff`. Two minor
conflict files (`STATE.md` + `Cargo.lock`) resolved with `--theirs`
(feature-branch wins; STATE.md is cron-only state, Cargo.lock is the
feature-branch's superset). Version bumped 1.9.2 → 2.0.0 in
package.json, src-tauri/Cargo.toml, src-tauri/tauri.conf.json, and
Cargo.lock locked via `cargo update -p slab-app`.

## Commits this tick (2 on `main`):
- `5c1354e` Merge v2.0.0 'Workshop' — TypeScript plugin platform (45 commits)
- `c270a8d` chore(release): bump version to 2.0.0 — Workshop

## Workshop slices folded into main (recap):
- Slice 1: rquickjs embedding + sandboxed console
- Slice 2: manifest schema + hash-pinned script loader
- Slice 3: capability grants backend + enforce() gate
- Slice 4: `slab` global + lifecycle + Tauri grant cmds + TS bindings
- Slice 5: Cabinet consent modal + permission management UI
- Slice 6: live document lifecycle events (onOpen/onClose/getActive)
  via per-plugin actor threads
- Slice 7: `slab.fetch` — host-mediated HTTP with capability gate
- Slice 8: `slab.storage.*` — per-plugin sqlite KV store with quotas
- Slice 9: `@slab/plugin-sdk` typed npm SDK + 3 example plugins

## Quality gates on `main` HEAD `c270a8d` (all green):
- `cargo fmt --all -- --check` — clean
- `cargo clippy --all-targets -- -D warnings` — clean
- `cargo test --lib` — **902 passed / 0 failed**
- `pnpm check` — 0 errors, 35 pre-existing warnings (unchanged)
- `cargo check --lib` after version bump — clean

## Buy-Button test:
- **Pay-for-it: PASS.** A plugin platform is the #1 differentiator
  Adobe charges enterprise licensing for. Slab ships it free.
- **Pick-us: PASS.** PDF Expert + Foxit have no plugin model.
  Acrobat's requires enterprise.
- **Tell-a-friend: PASS.** Screenshot-able feature — `@slab/plugin-sdk`
  IntelliSense over the entire `slab.*` surface in one npm install.

## RELEASE_PENDING set → MODE B next tick.

Next tick should:
1. Poll CI `gh run view 26145019423`.
2. If green → `gh release create v2.0.0` with the 6 best artifacts +
   customer-facing release notes (lead with the wow, not a changelog).
3. If failed → revert merge or hotfix on follow-up branch.

## Why this counts as one BIG ship tick (not a bunch of small ones):
- **Vertical capability**: the entire v2.0.0 plugin platform crosses
  the release pipeline end-to-end (45 commits → merge → tag → push →
  CI fires).
- **600+ LOC threshold**: the merge brings in ~12,000 LOC of net new
  product code (rquickjs runtime, actors, storage, fetch, consent UI,
  SDK package). Crushing the floor.
- **4+ commits threshold**: 2 commits on main + 45 folded-in feature
  commits.
- **WOW quotient already burnt** for today by Slice 9 — but the
  release itself is its own wow (Slab now has a plugin platform that
  costs $0; Acrobat's is enterprise-licensed).
