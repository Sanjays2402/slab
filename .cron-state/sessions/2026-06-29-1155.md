# Round 59 — 2026-06-29 11:55 PT (Cake cron, 20-min batch)

Shipped 5/5 frontend/UX feature slices across FIVE surfaces, from the
round-58 candidates list. All committed to main + pushed + verified on
origin/main. Frontend-focus override honoured: zero Rust changed.

## The 5 (each its own commit)

1. **0418828** feat(library-search): stale-pin nudge above the saved strip
   - Round-58 follow-up to per-pin recency. New tested pure core:
     isStalePin (true only for a sweep aged past 1w; never-swept != stale;
     future never stale), countStalePins (de-dupes case/spacing variants,
     tolerant of garbage map), describeStaleNudge (singular/plural +
     threshold-window phrasing, "" when none).
   - Wired: derived staleCount drives one dismissible amber banner between
     the saved header and strip, offering the existing Run-all sweep as the
     fix. Dismissal keyed to the stale count so a newly-aged pin re-raises
     while a re-sweep (count->0) clears it cleanly.
   - librarySearchView.test.ts 348 -> 371.

2. **2414ab3** feat(reader): filter-as-you-type over the outline tree
   - New tested core readerOutlineView.ts (43 tests): filterOutlineTree
     keeps a branch if it OR any descendant matches, force-expands
     survivors, null-sentinel on empty query (normal tree untouched).
     Reuses palette scorePaletteField + splitHighlight (no 2nd fuzzy
     engine). countOutlineMatches counts self-matches only;
     describeOutlineFilter / countOutlineNodes for footer + placeholder.
   - Wired: filter input under the outline header, filteredOutlineList
     snippet (highlighted + force-expanded), No-matches state, aria-live
     count, Esc/clear. Dark-first input + accent <mark>.

3. **723947b** feat(diff): first-compare skeleton while documents parse
   - DiffPanel: totals-bar + 3 page-row shimmer blocks. Diff3Panel:
     3-column base/mine/theirs shimmer. Both gated to the first compare
     (working && !diff) so a re-compare keeps the old result visible.
     Shared shimmer family with the outline/Convert loaders; reduced-motion
     safe; aria-busy + status line carry state. Presentational; no tests.

4. **091a1ea** feat(convert): per-page thumbnail preview for pdf2img
   - New tested core convertPreview.ts (23 tests): selectPreviewPages caps
     at 24 + evenly spreads big selections (first+last always included),
     describePreview "Showing N of M". Wired: buildPreview renders ~140px
     JPEG thumbs onto an off-screen canvas, paints the grid progressively,
     monotonic token guards stale renders, rebuilds on range change,
     best-effort (bad page skipped, doc failure clears silently).

5. **1a3a09e** feat(smart-folders): staggered entrance for the main hub list
   - Main-list twin of the round-58 suggested-strip stagger (ae2df3d).
     Same quintOut cardIn rule (45ms steps, capped at 6), reduced-motion
     gated, keyed by kind:id so only new rows animate (a reorder doesn't
     re-fire). Presentational; no tests.

## Gates (once, end of batch)
- TS suites: librarySearchView 371, readerOutlineView 43, convertPreview
  23, convertReorder 21, readerThumbView 34, paletteSearch 327 (shared
  palette core UNCHANGED — reuse proven intact). All green.
- pnpm check: 0 errors / 104 warnings (rounds 32-58 baseline EXACT).
- cargo fmt --all --check: clean. Zero Rust files changed -> lib baseline
  carries forward (full Tauri build never run; it wedges the disk).

## Notes
- Two new tested pure cores this round (readerOutlineView, convertPreview)
  + one extension of an existing core (librarySearchView stale-pin) — the
  pure-core/thin-shell discipline held. Two presentational slices (diff
  skeletons, smart-folders stagger) carry no logic, honestly no tests.
- All 5 were explicit round-58 candidates; the outline filter was the
  marquee (a genuinely missing capability on a long-TOC PDF), the rest
  were teed-up follow-ups. No padding, no fake splits.
- Roadmap refilled (round-59 list): doc-detail metadata editor stays the
  top carried item; OCR facet-merge still the highest-value open polish;
  round-59 follow-ups noted (outline keyboard cursor + persist expand,
  Convert thumb-click-to-range, per-chip stale badge, diff determinate
  progress).
