# Tick 2026-06-18 06:35 PT — roadmap #8 Empty/unused tag cleanup

**Branch:** feature/v3.39.0-atlas-tag-suggest
**Commits:** cd4219a (backend) + ba7a83d (UI) — pushed, local==origin verified.
**Clock at start:** 06:20 PT (before 08:00 stop gate).
**Build env:** /Volumes/SlabBuild mounted, 45Gi free.

## Shipped
Empty/unused tag cleanup — a one-click prune of every tag attached to zero
documents (the residue merges and bulk-removes leave behind).

### Backend (cd4219a)
- `registry::delete_unused_tags() -> usize`: single DELETE over library_tags
  guarded by `NOT EXISTS` against library_doc_tags. tag_id is NOT NULL so
  NOT EXISTS is the clean idiomatic form (vs the NOT IN the roadmap sketched).
  Returns rows removed. In-use tags untouched; empty library a no-op -> 0.
- 1 Tauri command `slab_library_delete_unused_tags` — emits library-changed
  only when removed>0.
- 4 tests: removes-only-unused, no-op-when-all-used, empty-is-zero, and the
  motivating bulk-remove-leaves-residue-at-0 reclaim.

### Frontend (ba7a83d)
- TS `deleteUnusedTags(): Promise<number>`.
- `$derived unusedTagCount` off the existing tagCounts map (count 0 == unused)
  — self-heals on every refreshAll, no bespoke optimistic plumbing.
- "Clean up N" rail-head affordance, shown only when >0, danger-tinted hover,
  disabled while pruning. Confirms with exact count, snapshots doomed ids to
  prune the active filter, toasts via bulkSummary, refreshAll reconciles.

## Gates
- cargo fmt: clean
- cargo test --lib pdf::library::: 228 passed / 0 failed (4 new), 1.74s warm
- cargo clippy --lib -D warnings: clean, 6.48s warm
- pnpm check: 0 errors, 105 warnings (all pre-existing in other panels)

No schema bump (pure delete over existing tables). Cache from the 05:50 tick
still warm. Next undone: roadmap #9 tag filter combinator (AND/OR).
