# Round 36 — 2026-06-25 12:46 PT (Cake cron, 20-min batch)

Shipped 5/5 frontend/UX slices CONTINUING the round-35 toast subsystem
into its interaction layer — full Sonner parity for the global
notification system. All five grow the pure src/lib/toastStack.ts
helper module (254 inline tests, up from 128). Pushed 04c5cb8..4af8ff5.

## Slices (one commit each, all on main)

1. d12b37b feat(toast): inline action button with severity-aware styling
   - NotifyOpts.action {label, onClick, dismissOnClick?}; normalizeToast-
     Action validates non-blank label + callable handler (else null),
     24ch label clamp. Action toasts default sticky. runToastAction
     centralizes run-then-conditional-dismiss. Severity-tinted button.
   - 24 tests (152 total).

2. 65925b1 feat(toast): swipe / drag-to-dismiss with flick velocity
   - Pure ToastSwipe geometry: rightward-clamped dx + flick velocity;
     toastSwipeShouldDismiss fires past 80px OR 0.5px/ms flick (16px
     jitter floor); toastSwipeOpacity fades to 0.25 floor. Pointer
     Events + capture, grab/grabbing, cubic-bezier snap-back (off
     mid-drag + reduced-motion).
   - 23 tests (175 total).

3. a7cc8fd feat(toast): promise lifecycle toast with loading spinner
   - notify.promise(work, {loading, success, error}) sticky spinner
     toast morphs the SAME row in place to success/error. Messages may
     be strings OR functions of the settled value. resolveToastMessage
     (throw/non-string -> fallback), describeToastError, toastFulfil/
     RejectPatch. CSS ring spinner shares .icon footprint, dashed
     reduced-motion fallback.
   - 24 tests (199 total).

4. 6521a74 feat(toast): keyboard focus + dismiss for the stack
   - Alt+T focuses newest toast; Escape/Delete/Backspace dismiss with
     focus sliding to the sibling (pickToastFocusIndex); Enter/Space
     fire the action. Pure resolveToastFocusHotkey + resolveFocusedToast-
     Key classifiers. Rows tabindex=-1 + focus ring; keydown only acts
     when the row itself holds focus (no double-fire with child buttons).
   - 29 tests (228 total).
   - Fixup 4af8ff5: one scoped svelte-ignore for the role=group keydown
     to hold the 104-warning baseline (house style).

5. f1a828b feat(toast): expandable overflow toggle for the +N more pill
   - Round-35's dead "+N more" span is now a real expand/collapse
     toggle. countToastOverflow (stable beyond-cap count), resolveToast-
     StackView (partition + expand -> one render plan), describeOverflow-
     ToggleAria. $effect auto-collapses when overflow drains.
   - 26 tests (254 total).

## Gates (run ONCE at end of batch)
- tsx src/lib/toastStack.test.ts: 254/254 pass (128 + 24+23+24+29+26)
- tsx src/lib/hopper.test.ts: 1189/1189 unchanged
- pnpm check: 0 errors / 104 warnings (rounds 32-35 baseline EXACT —
  slice-4 tripped one a11y_no_noninteractive_element_interactions,
  held at baseline with a scoped svelte-ignore before final gating)
- cargo fmt --all --check: clean
- ZERO Rust files changed -> round-32 lib baseline (clippy clean, 2620
  tests) carries forward unchanged. Full Tauri binary build never run.

## Notes
- Frontend-focus override honoured: all 5 slices TS/Svelte UI. Zero
  backend.
- Deliberate continuation: round 35 built the toast PLUMBING, round 36
  the INTERACTION layer the round-35 notes teed up. Both named "Next
  FRONTEND candidates" (action button, swipe-dismiss) shipped + 3 more
  Sonner-parity caps. Toast system now feature-complete vs Sonner/Linear.
- Pure-core/thin-shell split throughout (every decision a pure fn in
  toastStack.ts; notify.ts + ToastStack.svelte own setTimeout / Pointer
  Events / focus()).
- Deferred nothing — 5 solid slices, no padding.

Lock released at end of tick.
