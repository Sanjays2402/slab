# Round 37 — Command Palette → Raycast grade (2026-06-25 17:55 PT)

Cake (cron) 20-min batch loop. Frontend-focus override active.

## Shipped 5/5 frontend slices — all on `main`, pushed `ac0a0a9..a084b11`

PIVOT: rounds 35-36 took the toast system to full Sonner parity (now
feature-complete). Picked the **Command Palette (Cmd+K)** — the explicit
"Raycast-grade" roadmap candidate, the highest-visibility surface in the
app, and a genuine weak point (untested inline fuzzyScore, no match
highlights, flat title+keyword scoring string).

All five slices backed by a NEW pure module `src/lib/paletteSearch.ts`
(123 inline tests) — same pure-core/thin-shell discipline as toastStack.ts.

1. **c2fd06b** feat(palette): tested search core with match ranges,
   replacing inline fuzzyScore. scorePaletteField (3-tier prefix>>
   substring>subsequence + shortness nudge + match ranges),
   scorePaletteEntry (max(title,keyword), title-only ranges),
   splitHighlight + normalizeRanges. 53 tests.
2. **941ebf5** feat(palette): live accent highlight of matched title
   chars via real <mark> elements (no {@html}); deepens on active row.
   UI-only.
3. **673f22f** feat(palette): wrapping arrows, Home/End, PageUp/Down with
   scroll-into-view. classifyPaletteNav + nextPaletteIndex (wrap arrows,
   clamp paging, guard stale/NaN). 26 tests.
4. **6a2856e** feat(palette): frecency ranking for the empty-query
   Recently-used group. recencyWeight steep buckets * (1+ln count);
   rankFrecency + recordFrecency (capacity eviction, never drops just-
   touched). Migrated cmdMru.ts legacy string[] -> {id,count,lastUsedAt}[]
   with backward-compat read. 28 tests.
5. **a084b11** feat(palette): inline keyboard-shortcut chord hints on
   rows. paletteKeymapId pure mapper (palette row -> keymap ActionId) +
   guard test asserting no typos; rendered as platform-correct key-caps
   via prettyBindingFor, live-reacting to keymapView. 15 tests.

## Gate (run ONCE at end of batch)
- tsx paletteSearch.test.ts — 123/123 pass (NEW suite)
- tsx toastStack.test.ts — 254/254 (regression, unchanged)
- tsx hopper.test.ts — 1189/1189 (regression, unchanged)
- pnpm check — 0 errors / 104 warnings (rounds 32-36 baseline EXACT)
- cargo fmt --all --check — clean
- ZERO Rust files changed -> round-32 lib baseline (clippy clean, 2620
  tests) carries forward; full Tauri build never run (wedges disk).

## Files
- NEW src/lib/paletteSearch.ts (search core + nav + frecency + keymap map)
- NEW src/lib/paletteSearch.test.ts (123 tests)
- src/lib/CommandPalette.svelte (wired all 5 slices + highlight/chord CSS)
- src/lib/cmdMru.ts (migrated to frecency store, backward-compatible)

## Deferred
None — clean 5/5. Next candidates refilled in STATE.md (palette empty-
state polish, group-skip nav, recent-doc thumbnails; plus the standing
drilldown/histogram/Loom items).

## Notes
- Frontend-focus override honoured: all five slices TS/Svelte. Zero backend.
- cmdMru migration is the one non-pure edge worth calling out: legacy MRU
  read once + written forward so upgrading users keep their order.
- Held the 104-warning svelte-check baseline exactly across all 5 slices.
