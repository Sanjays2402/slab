# Tick 2026-06-18 05:35 PT (Cake, cron)

**Window:** overnight (05:35 PT, well before 08:00 stop). Auto-stop gate passed.

**Shipped:** fresh roadmap #7 "Tag usage counts in the rail" — single commit 966db5e.

## What landed
- **Backend** `registry::tag_usage_counts() -> Vec<(i64,i64)>`: one LEFT JOIN +
  GROUP BY (one round-trip, never N). Every tag once; zero-doc tags report 0
  (LEFT JOIN keeps merge/remove residue an INNER JOIN would drop); id-ordered.
- **Tauri** command `slab_library_tag_usage_counts` (registered in handler list).
- **Tests** 6 new in registry.rs: per-doc counts, zero-for-unused, one-row-per-
  tag-id-ordered, empty-with-no-tags, reflects bulk apply/remove, reflects merge
  (distinct union, no double-count, gone source unreported).
- **TS** `tagUsageCounts()` -> `Map<number,number>` (collects the pair array).
- **Svelte** LibraryPanel:
  - `tagCounts` Map state + `tagSort` ("name" | "count") + `sortedTags`
    `$derived` (count desc, name tie-break).
  - `refreshAll` now fetches counts alongside folders/tags -> rail self-heals on
    every `library-changed` poke; no bespoke optimistic plumbing.
  - muted `rail-meta` count beside each tag (mirrors folder rail); rail-head
    `A-Z` / `Most used` sort toggle (shown only when >1 tag); `.rail-sort` CSS.

## Gates
- cargo fmt: clean
- cargo test --lib pdf::library:: -> 224 passed / 0 failed (6 new)
- cargo clippy --lib -D warnings: clean (6.61s warm)
- pnpm check: 0 errors; only the 2 pre-existing LibraryPanel warnings
  (autofocus @1699, webkit line-clamp @2169) — no new ones.

## Build env
- /Volumes/SlabBuild mounted, 45Gi free. No wedge this tick.
- First session test compile ~16s (test profile slightly cold), warm suite 1.72s.

## Git
- Commit 966db5e as `Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`.
- Pushed origin/feature/v3.39.0-atlas-tag-suggest, 48c7fb9..966db5e.
- Verified: local HEAD == origin HEAD == 966db5e.

## Next
- Roadmap #8 "Empty/unused tag cleanup" — `registry::delete_unused_tags()`,
  one-click "Remove N unused tags" on the Tags rail head. tag_usage_counts
  already proves the zero-count detection the cleanup builds on.
