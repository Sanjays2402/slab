# Tick 2026-06-18 04:20 PT — roadmap #5 Recently-used tags

Branch: feature/v3.39.0-atlas-tag-suggest (no merge/PR/tag/release)
Build env: /Volumes/SlabBuild mounted, 45Gi free. Cache warm from 03:25 tick.

## Shipped
Vertical slice for "Recently-used tags":
- Schema v8->v9 migration: nullable `applied_at` on library_doc_tags + a
  (tag_id, applied_at) index. Legacy links carry NULL, sort last, not lost.
- registry::set_doc_tags rewritten from wipe-and-reinsert to a true diff:
  surviving links keep their original applied_at, only newly-attached links
  are stamped now(). Prevents re-saving an unchanged set from shuffling a
  stable tag to the top of the recent list.
- bulk_tag::apply_tag_to_docs now stamps applied_at (added local now_unix()).
- registry::recently_used_tags(limit): each used tag once, MAX(applied_at)
  desc, link rowid tie-break, NULL-last, never-applied excluded, limit-0 = [].
- Tauri command slab_library_recently_used_tags (limit default 8) + registered.
- TS recentlyUsedTags client.
- LibraryPanel: "Recently used" quick-chip row at top of the per-doc tag
  context menu. Lazy-load on menu open, re-rank after each apply/remove,
  $derived list hides tags already on the doc. Dark-first pill CSS.
- Relaxed two schema-version-pinning tests (registry schema_v8 test +
  collections collections_tables_exist) from == 8 to >=, added a dedicated
  v9 applied_at column test. Pre-empts the equality-assert trap that bit an
  earlier tick.

## Commits
- cf62147 feat(library): recently-used tags backend (schema v9 applied_at)
- 3fc663a feat(library): recently-used tag quick-chips in the doc tag menu
Pushed origin/feature/v3.39.0-atlas-tag-suggest; local==origin verified (3fc663a).

## Gates
- cargo fmt --all: clean
- cargo test --lib pdf::library:: 206 passed / 0 failed (9 new)
- cargo clippy --lib -D warnings: clean (9.1s warm)
- pnpm check: 0 errors, 105 warnings (all pre-existing; the 2 in LibraryPanel
  are pre-existing autofocus + webkit-line-clamp, not my chip code)

## Snags
- First cargo test build failed: query_map().collect() temporary outlived
  `stmt` at the end of the current-links block in set_doc_tags (E0597).
  Fixed by draining rows with `let mut rows = stmt.query(...); while let
  Some(row) = rows.next()?` into a HashSet instead. Re-ran green.

## Next
Roadmap #6 "Tag merge" — fold one tag into another (re-point links coalescing
by surviving applied_at, delete source). Natural complement to rename.
