# Round 46 — 2026-06-27 14:55 PT (Cake cron)

Frontend-focus override active. Shipped **5/5** capabilities as **5 separate
independently-revertible commits** — explicit roadmap FOLLOW-UPS across three
surfaces that already have tested pure cores, so each commit compiles green
standalone (unlike rounds 41-45's single-new-surface wires that honestly
had to be 2 commits).

## Shipped (5 commits, all on origin/main 07eb456..bb1594e)

1. `98b7c5f` feat(library-search): refine-highlight inside snippets
   - New tested `buildSnippetSpans(snippet, refine)` parses the server
     `<mark>`-wrapped, entity-encoded snippet into typed render spans
     (match / refine / both), painting a SECOND distinct cyan tint over
     every literal case-insensitive occurrence of the active refine term.
   - Replaced `{@html safeSnippet()}` with plain Svelte text spans split by
     `<mark>` class -> ZERO HTML-injection surface now (safeSnippet removed).
   - Light + dark tints; blended underline for refine-inside-match overlap.
   - librarySearchView 127 -> 144 (+17).

2. `a6262b2` feat(library-search): Cmd/Ctrl+Up/Down jump between doc groups
   - New `searchGroupStarts(groups)` adapter over the tested
     `groupStartIndices`; re-exports the palette `classifyPaletteGroupNav` /
     `nextGroupIndex` through librarySearchView so the chord behaves exactly
     like Cmd+K's group jump. Checked before classifySearchResultKey (which
     bails on modifiers); fires from the search/refine boxes too. Tip added.
   - librarySearchView 144 -> 158 (+14).

3. `4bbaf51` feat(ocr-queue): clickable pending-state facet (image-only/mixed)
   - `groupPendingStates` / `filterByPendingState` /
     `reconcilePendingStateFacet` / `pendingStateLabel` mirror the
     failure-reason facet exactly for the pending list. Clickable count
     pills (shown only when >1 kind), ANDs with search+sort, facet-aware
     empty state, footer narration folds it in, close/clear reset it.
   - ocrQueueView 122 -> 141 (+19).

4. `e048678` feat(recents-home): clear-unpinned footer affordance
   - `countUnpinned` (honest count incl. missing-flag/null-skip) +
     `describeClearUnpinned` ("" when nothing to clear -> hides the button)
     wire the store's `clearRecent` (preserves pinned) with a confirm +
     info toast. Quiet button, danger tint only on hover.
   - Tested `recentProgressBar` core also lands in this view-core commit.
   - recentsHomeView 91 -> 109 (+18).

5. `bb1594e` feat(recents-home): reading-progress overlay on card thumbnails
   - Reusable `progressOverlay` snippet renders a thin accent bar
     (role=progressbar) along the bottom edge of every pinned/others card
     thumbnail, driven by `recentProgressBar` (reuses recentReadingProgress
     so bar/dots/chip never disagree). Green when finished; hidden when no
     position. Resume hero keeps its own body bar (no redundant overlay).
   - recentsHomeView 109 -> 110 (+1 defensive).

## Gates (one batch gate at the end)
- View-core tests: librarySearchView 158/158, ocrQueueView 141/141,
  recentsHomeView 110/110 (69 new tests this round).
- Shared palette suite UNCHANGED (reused core provably intact):
  paletteSearch 253, beaconCacheView 112, readerFindView 86, toastStack 254.
- `pnpm check`: 0 errors / 104 warnings — rounds 32-45 baseline preserved
  EXACTLY (hit one transient `@ts-expect-error`-unused error mid-batch,
  fixed before the gate).
- `cargo fmt --all --check`: clean. ZERO Rust files changed (frontend-only),
  so the round-32 lib baseline (clippy clean, 2620 tests) carries forward.
  Full Tauri binary build never run (wedges the disk).

## Notes
- Commit structure: FIVE separate commits, honestly earned. These were
  additive follow-ups on already-wired surfaces (not one cohesive new wire),
  so each is its own separately-compiling, independently-revertible slice.
- No pivot — deliberate completion of the highest-value named roadmap
  follow-ups across the three surfaces rounds 41-45 built tested cores for.
- Roadmap refilled: 3 fresh frontend candidates added (OCR per-reason
  Retry-all, Library Search recent-chip keyboard nav, RecentsHome pinned-
  strip horizontal scroll).
