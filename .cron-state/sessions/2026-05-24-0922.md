# Tick 2026-05-24 09:22 PT (Sunday off-hours)

**Mode:** C (DEVELOP) → v3.21.0 'Hopper Conditions' Slice 1 shipped.

## What shipped

Branch `feature/v3.21.0-hopper-conditions` (new), 4 commits, **911 net LOC**:

| SHA | Title | LOC |
|-----|-------|-----|
| `ff9743b` | feat(hopper): rules engine — predicates, actions, evaluation chain | 467 |
| `c21bab5` | feat(hopper): persist rules in sqlite — set_rules/get_rules typed API | 210 |
| `07a135c` | feat(hopper): pipeline + watcher honor rules — end-to-end routing works | 149 |
| `05a4165` | feat(hopper): Tauri command surface for rule CRUD + live-test preview | 116 |

## End-to-end capability now working

A user can configure conditional routing rules per watched folder and
PDFs dropped into the inbox route by filename/size/page-count to
different recipes + output directories. Tasks 1–7 of the plan shipped
this tick (skipping the per-task granularity in favor of cohesive
slices). What remains:

- **Task 8:** Frontend `hopper.ts` API client extensions + TS types.
- **Task 9:** `HopperRulesEditor.svelte` + live test preview.
- **Task 10:** Onboarding tooltip + release notes + version bump.

## Quality gates (this tick)

- `cargo fmt --all --check` ✅
- `cargo clippy --lib --all-targets -- -D warnings` ✅
- `cargo test --lib` ✅ **1645 passed** (57 hopper tests, +15 new)
- `pnpm check` ✅ 0 errors, 66 pre-existing warnings

## Disk cleanup

Mac mini hit 100% disk again during `cargo check`. `cargo clean` freed
4.5 GiB. Disk back to 85% used (4.2 GiB free). Future ticks should
budget this — keep `target/` under 4 GiB or clean before any
incremental rebuild.

## Buy-Button verdict

✅ Pay-for-it — Hazel charges $42 for exactly this. Adobe AutoActions
require enterprise licensing.
✅ Pick-us — Without conditional routing, Hopper users had to create
one watched folder per document type. This closes the gap.
✅ Tell-a-friend — Tweetable: "Drop ALL my PDFs in one folder, Slab
routes them automatically by content/filename."

## Next tick

Continue v3.21.0:
- Frontend types + API client (Task 8, ~80 LOC, trivial).
- `HopperRulesEditor.svelte` with live test preview (Task 9, ~280
  LOC) — this is the WOW moment for v3.21.0.
- Version bump + release notes + onboarding (Task 10).

After Task 10 → merge to main + tag v3.21.0 + cut release.
