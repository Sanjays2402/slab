# Tick 2026-06-18 07:50 PT — Cake (cron)

**Shipped:** Round-3 roadmap #11 "Tag filter clear-all" (frontend-only).

**Commit:** f41a6a1 — feat(library): one-click Clear for the tag-rail filter
**Branch:** feature/v3.39.0-atlas-tag-suggest (pushed + verified local==origin)

## What
A "Clear" affordance in the Tags rail head of LibraryPanel.svelte that
resets the entire tag filter in one click.

- `tagFilterActive` $derived = `activeTagIds.size > 0 || untaggedOnly`
  gates visibility (button only shows when a tag filter is engaged).
- `clearTagFilter()`: `activeTagIds = new Set()`, `untaggedOnly = false`,
  `tagMatch = "all"` — three fresh assignments, so the existing reactive
  $effect (watches activeTagIds/untaggedOnly/tagMatch/sort/activeFolder)
  re-queries exactly once. No manual refresh call.
- Match mode is excluded from the visibility test on purpose (it's inert
  with 0 tags selected) but reset anyway for a fully clean slate.
- `.rail-clear` CSS mirrors `.rail-sort`/`.rail-match` (muted uppercase,
  margin-left:auto, neutral hover-to-text). Non-destructive reset, so
  deliberately NO danger tint (unlike `.rail-cleanup`).

No backend, no schema, no cargo. 1 file changed, 48 insertions.

## Gate
- `pnpm check`: 0 errors, 105 warnings (baseline, all pre-existing).
- LibraryPanel: still exactly 2 warnings (line 1807 autofocus + 2334
  webkit) — both pre-existing, none new from this change.

## Why #11 over #10
Tick started 07:44 PT — only ~16 min to the 08:00 hard auto-stop. #10
(saved views) needs a new schema table + full Rust+TS+Svelte slice gated
by a slow cargo test that wouldn't finish in budget. #11 was the seeded
"lowest-risk pick if a tick is tight on build budget", gates on pnpm
check alone, and is genuinely useful now the rail has many tags.

## Build env
/Volumes/SlabBuild mounted (45Gi free). No cargo run this tick (pure
frontend), so the slow-disk wedge risk didn't apply.

## Next undone
#10 saved tag-filter views, #12 tag descriptions/notes.
