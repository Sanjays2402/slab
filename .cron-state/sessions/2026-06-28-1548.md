# Round 52 — 2026-06-28 15:48 PT

Shipped 5/5 frontend/UX feature slices, all on tested pure cores. Theme:
preference persistence (sort/pins survive sessions) + one visual win.

1. OCR Queue persist SORT — 5aa65e2
   isOcrSort guard in ocrQueueView core + ocrSortStore.ts shell (default
   name-asc clears key). Panel seeds sort from load, persists on cycle.
   search/facet stay transient. ocrSortStore 16 tests; core 221->231.
2. Beacon inspector persist SORT — 136a1d1
   isBeaconSort + beaconSortStore.ts (default indexed-desc). 14 tests.
3. Library Search persist SORT mode — 2632899
   isSearchSortMode + librarySortStore.ts (default relevance). 10 tests.
4. Palette PIN-A-COMMAND — d9ef0f7
   isCommandPinned/toggleCommandPin/countPinnedCommands + cmdPins.ts +
   sticky "Pinned" group ahead of Recently-used + per-row star toggle.
   paletteSearch 308->320; cmdPins 8 tests.
5. OCR Queue per-reason SHARE BAR — 69f0595
   reasonShareBars(fraction/scaled/percent) + 2px proportional inset bar
   on each facet pill; dominant cause visibly biggest. core 221->231.

Gates: all 8 tsx suites green; pnpm check 0 errors / 104 warnings
(baseline exact); zero Rust changed.

BLOCKER (resolved): repo volume /Volumes/Projects (15Gi) hit 99-100% full,
writes failed mid-edit. Freed .vitest-tmp (462M stale) + .pnpm-store (1.5G
cache, regenerable). Hit APFS 0-byte deletion deadlock; broke it with a
50x rm retry loop (0Bi -> 7.5Gi free). pnpm install will rehydrate store.
Recurring disk pressure across rounds 50-52 — Sanjay may want to grow the
volume or relocate the repo.

Deferred: doc-detail metadata editor, reader thumbnail-rail match highlight,
histogram tooltip, Signet/Quill skeletons, palette recent-files thumbnails.
