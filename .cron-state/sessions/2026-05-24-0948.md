# Tick 2026-05-24 09:48 PT — v3.21.0 Hopper Conditions Tasks 8-10

**Mode:** C DEVELOP. writing-plans skill invoked; plan already on disk →
executed Tasks 8 + 9 + 10 of `docs/plans/2026-05-24-v3.21.0-hopper-conditions.md`.

## Commits (3, all on `feature/v3.21.0-hopper-conditions`)

| SHA | Subject | Net LOC |
|---|---|---|
| `bbd4f43` | feat(hopper): TS types + API client + predicate helpers | +174 |
| `e38578e` | feat(hopper): rules editor UI with live match preview (wow) | +927 |
| `15b3815` | chore(release): v3.20.0 -> v3.21.0 'Hopper Conditions' | +135/−4 |

**This-tick total:** ~1106 net non-test LOC across 3 commits.
**Branch lifetime (7 commits):** ~2020 net LOC.

## Files

- NEW `src/lib/components/HopperRulesEditor.svelte` (~875 LOC) — the WOW
- NEW `docs/releases/v3.21.0.md` (~110 LOC marketing copy)
- MOD `src/lib/hopper.ts` (+174 LOC TS types + API + helpers)
- MOD `src/lib/panels/HopperPanel.svelte` (+52 LOC expand toggle + mount)
- MOD `src/lib/OnboardingTour.svelte` (+7 LOC new step)
- MOD `README.md` (+19 LOC v3.21.0 lead section, v3.20.0 preserved)
- MOD `package.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`,
  `src-tauri/Cargo.lock` (version 3.20.0 → 3.21.0)

## Quality gates

- `cargo fmt --all --check` — clean (no Rust changes anyway)
- `pnpm check` — **0 errors / 69 warnings (all pre-existing)**
- clippy/test deferred to branch CI (no Rust source changes; lock edit metadata-only)

## CI

Pre-push run on `05a4165` (Task 7) was green:
`26366790680 build succeeded in 10m55s`. Fresh push of `15b3815` kicked
a new run at 09:55 PT, in_progress at tick end. Next tick polls.

## Buy-Button verdict — 4/4 PASS

- **Pay-for-it ✅** — Hazel charges $42 for rules-per-inbox; Adobe AutoActions
  are enterprise-only + cloud round-trip. Slab gives it away local.
- **Notice-it ✅** — Brand-new "▸ Rules" toggle on every watch row, new
  onboarding step, new README lead section.
- **Pick-us ✅** — Without conditional routing Hopper users had to create
  one watched folder per doc type. This closes the gap.
- **Tell-a-friend ✅** — Live preview pane with green chips updating in
  real time as you edit a glob — that's the screenshot.

## Wow

The live preview pane is the moment. Up to 5 candidate files render
with one chip per rule, green ✓ / grey · per match, winning rule
in solid green with `→ <output_dir>` annotation. Re-evaluates
~150ms after every keystroke via `slab_hopper_test_rules` (same Rust
matcher production uses, so preview fidelity = ground truth).

`LAST_WOW_TICK_AT: 2026-05-24T16:55:00Z`

## Disk

`cargo clean` freed 3.6 GB at start. 1.1 GiB → 5.2 GiB free. No
rebuild this tick → headroom preserved for next tick's `cargo test`.

## Next tick plan

1. Poll branch CI on `15b3815`. If green:
2. MODE A — merge `feature/v3.21.0-hopper-conditions` → main (`--no-ff`),
   tag `v3.21.0`, push `origin main --follow-tags`.
3. Tick after that → MODE B finalize: poll build CI, `gh release create v3.21.0`
   with 6 curated artifacts (mac arm64/x64 dmg, linux deb + AppImage, win msi + nsis)
   + `docs/releases/v3.21.0.md` as notes.

If CI failed: fix on branch, do NOT merge.
