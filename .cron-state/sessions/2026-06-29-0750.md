# Round 58 — 2026-06-29 07:50 PT

5 frontend/UX capabilities, 5 feature commits, FIVE surfaces (round-57 candidates list).

## Shipped
1. **Library Search — per-pin sweep recency** (23867a5)
   - Parallel `slab.library.pinSwept.v1` {query:epochMs} store in pinYields.ts
     (loadPinSwept/savePinSwept/stampPinSwept) — kept SEPARATE from the yields
     map so the existing tested contract + every caller stay untouched.
   - Pure `relativeSweptAge` + `describePinSweepBadge` in librarySearchView
     (mirrors recent.ts formatRelTime; `now` injected for testing).
   - Badge now reads "84 · 3h ago" with full title "84 matches last sweep ·
     swept 3h ago"; runAllSaved stamps, clearDry prunes, minute-ticker refreshes.
   - librarySearchView suite 329 -> 348 (19 new: full ladder, future-clamp,
     60m/24h boundaries, singular/plural, dry-pin keeps age, garbage).

2. **RecentsHome — cover-flow hover-zoom on the recents GRID** (ae28def)
   - Round 57 added hover-zoom to the pinned strip only. Hoisted the single
     position:fixed flyout from inside the pinned section up to board level so
     one render serves BOTH strip + grid; wired showCoverPreview/hide onto every
     grid card-body. Reuses tested clampFlyoutTop. Pure UI, no test change.

3. **Convert — real img2pdf drag-to-reorder + flip settle** (e89a4ca)
   - The dropzone always PROMISED "Drag to reorder once added" but only had
     up/down buttons. Added HTML5 drag (dim source + accent drop-ring) +
     animate:flip; stable monotonic `uid` keys (index keys defeat flip).
   - New pure convertReorder.ts (moveItem + isReorder, 21 tests). up/down now
     route through moveItem with disabled end-caps.

4. **Forms — inspect shimmer skeleton** (ef6e86d)
   - Replaced bare "Loading…" with a meta-line shimmer + a full toolbar+table
     skeleton while slab_forms_inspect runs. Mirrors SmartFolders/OCR shimmer
     convention, reduced-motion gated, hidden on report-land or inspect-error.
     Pure presentational.

5. **SmartFolders — suggested-strip staggered fly-in** (ae2df3d)
   - Custom per-card upward fly with capped stagger on first paint; reads
     prefers-reduced-motion once at mount and collapses to a 0ms no-op. Keyed
     by cluster_hash so only new cards animate; existing CSS dismiss-exit kept.

## Gates (run ONCE for the batch)
- TS: librarySearchView 348/348, convertReorder 21/21, readerThumbView 34/34,
  recentsHomeView 167/167, paletteSearch 327/327 — all green.
- pnpm check: 0 errors / 104 warnings (rounds 32-57 baseline preserved EXACTLY;
  zero new warnings from the new badge suffix, grid hover, drag rows + flip,
  forms skeleton, or suggested-strip transition).
- Zero Rust/Cargo files changed -> round-32 lib baseline (clippy clean, 2620
  tests) carries forward untouched; no cargo invocation (full build wedges disk).

## Notes
- All 5 frontend per the standing override. Two carry tested pure cores
  (relativeSweptAge/describePinSweepBadge; moveItem/isReorder); three are
  UI-only polish (grid hover-wire, forms skeleton, suggested fly-in) — all
  explicitly named UX work in the override (loading/skeleton/animation).
- The transient LSP `svelteHTML`/`$state`/"not a module" errors seen mid-edit
  were stale-cache artifacts; svelte-kit sync at the gate resolved everything
  (0 errors). Verified, not assumed.
- Pushed ae2df3d to origin/main; verified landed.

## Deferred
- OCR Queue pending+failure facet merge — still the highest-value open item
  but needs a careful two-section render merge; left for a focused tick.
- Picked 5 across 5 distinct surfaces over going deep on one to keep each slice
  independently revertible and demo-able.
