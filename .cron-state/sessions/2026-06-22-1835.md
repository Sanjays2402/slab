# Round-25 batch — 2026-06-22 18:35 PT

Shipped 5/5 slices closing one cohesive arc: the auto-prune run
history (slices 118-122). Closed the "vanishing attribution"
gap that slice 114 left open — `overrides_applied` +
`overrides_rows_removed` were plumbed through
`AutoPruneOutcome::Pruned` since round 24 but vanished after
each toast. Tonight they get persisted to a four-table audit
trail, surfaced in CSV + JSON exports, and rendered inline in
the toast + a new history sub-block.

## Slices

- **Slice 118** — `0b5fbea` feat(install-log): auto-prune run
  history storage. Schema v3→v4 with
  `install_log_auto_prune_runs(id, ran_at_unix, rows_removed,
  retain_days, cutoff_unix, overrides_applied,
  overrides_rows_removed)` + `ran_at_unix DESC` index. Four
  primitives. `AutoPruneRun` struct re-exported from
  `marketplace::mod`. 8 storage tests.

- **Slice 119** — `82a056e` feat(install-log):
  `auto_prune_if_due` records each run to history. Pruned
  outcome appends one row; Skipped does NOT (debounce-window
  invariant). Zero-row Pruned IS recorded (audit signal, not
  silence). 6 driver tests including the conservation
  invariant (sum of `rows_removed` across history rows ==
  events removed from `install_events`).

- **Slice 120** — `6623281` feat(install-log): auto-prune run
  history CSV export primitive. Pure-data
  `auto_prune_runs_to_csv(rows, include_header) -> String`
  RFC-4180 8-column serialiser. `ran_at_iso` byte-equal to
  install-log CSV. `AUTO_PRUNE_RUNS_CSV_HEADER` pub const.
  11 CSV tests. NB: doc-comment rewording for clippy's
  `doc_lazy_continuation` (tripped on `"X + Y"` pattern in
  the column listing) landed in the slice 122 commit since
  it surfaced post-commit during the gate.

- **Slice 121** — `68a2e96` feat(install-log): auto-prune run
  history JSON envelope primitive. Pure-data
  `auto_prune_runs_to_json(rows) -> AutoPruneRunsExportEnvelope`.
  6 fields with pre-summed envelope-level totals
  (`total_rows_removed` + `total_overrides_rows_removed`)
  so consumers don't re-sum. `AUTO_PRUNE_RUNS_EXPORT_SCHEMA_
  VERSION = 1`, PARALLEL-versioned with all 5 sibling
  envelopes. 11 JSON tests including the all-six parallel-
  versioning equality pin.

- **Slice 122** — `614fc5d` feat(install-log): auto-prune run
  history UI with attribution toast. The demo-able payoff.
  4 Tauri commands. `AutoPruneRunsResult` wire payload with
  denormalised totals. TS surface: 5 async wrappers +
  `suggestAutoPruneRunsExportFilename` +
  `formatAutoPruneAttributionToast` pure helper +
  `InstallLogAutoPruneOutcome` extended with attribution
  fields. UI: nested history sub-block inside Retention
  section with count meta + Export… popover + Clear history
  (gated behind confirm) + per-row 4-column grid with 3-state
  attribution badge (neutral global / accent has-overrides /
  deeper accent all-overrides / muted no-op).
  `runAutoPruneNow` toast upgraded to attribution-aware
  copy. `load()`'s `Promise.all` extended with auto-prune-
  runs read. Escape chain + `onWindowClick` updated. 14
  inline test assertions.

## Gates result

- `cargo fmt --all` clean (no changes needed)
- `cargo clippy --lib -- -D warnings` PASSED CLEAN in 11.22s
  (after one doc-comment reword to satisfy
  `doc_lazy_continuation` on slice 120)
- `cargo test --lib` **2522 passed / 0 failed** (round-24
  baseline 2486 + 36 new = 2522: 8 storage + 6 driver +
  11 CSV + 11 JSON)
- `pnpm check` **0 errors / 104 warnings** (round-24
  baseline preserved EXACTLY — zero new warnings from any
  slice 122 surface)
- `tsx src/lib/marketplace.test.ts` **138 inline expects
  pass** (round-24 baseline 124 + 14 from slice 122 = 138)

## Push

`614fc5d` landed on `origin/main`. Verified with
`git fetch -q origin && git log --oneline origin/main`.

## Process notes

- Round 24's closing-notes candidate "install-log auto-prune
  toast that breaks out the per-plugin vs global attribution
  (slice 114 already plumbs the fields, the UI just doesn't
  render the split yet — short follow-up)" shipped as the
  FULL 5-layer arc rather than the minimum follow-up. The
  history table (118 + 119) is the load-bearing piece —
  without it the attribution split would still be a transient
  toast value lost the next time the user looks.
- Same canonical 5-layer cadence as rounds 19-24: backend
  storage → backend driver-rewrite → CSV primitive → JSON
  primitive → Tauri commands + TS client + filename helper +
  attribution toast helper + demo-able UI.
- Five slices, five commits, ONE logical subsystem. Clean.

## Next subsystem candidates

- Per-plugin "Run prune now" affordance (forces the per-
  plugin pass immediately without waiting for the global
  debounce — natural companion to the per-plugin overrides
  storage from round 24)
- Hopper rule reorder-by-drag in the coverage panel
- Drilldown row → cross-surface filter (clicking a fall-
  through filename in the popover carries the search query
  into the document inspector)
- Loom-grade tagging explorer
- Doc-detail metadata editor read/write surface
- Beacon cache inspector polish (column sort by basename /
  model facet)
- Quill multi-document field-detect queueing
- Histogram hover-tooltip on bar segments
