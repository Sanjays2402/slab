# Session 2026-05-22 22:00 PT — Bedrock Slice 4+5+6: PDF/A end-to-end UI

## What shipped

Buy-button tick. Bedrock is now end-to-end usable from the UI.

Commits on `feature/v3.0.0-bedrock-pdfa`:

1. `cde2b59` feat(pdfa): convert_to_pdfa orchestrator (Slice 4)
   - src-tauri/src/pdf/pdfa/convert.rs (+360 LOC, 6 unit tests)
   - Composes existing slice 1-3 passes into one E2E pipeline:
     load → font_audit (gate) → sanitize → inject XMP+OutputIntent →
     serialize → validate-the-round-trip → atomic_save.
   - Font audit acts as a non-destructive gate. PDFs with unembedded
     fonts (Word "Helvetica via PostScript name", uncommon vendor PDFs)
     are rejected with actionable error naming each font. Callers may
     pass skip_font_check=true to convert anyway. Destructive embedding
     deferred to v3.0.1.
   - Validation runs against the SERIALIZED output (re-loaded from
     buffer) so the report reflects what hits disk.

2. `749bf5a` feat(pdfa): slab_pdfa_convert Tauri command (+14 LOC)
   - Accepts input + output PathBuf + ConvertOpts; returns ConvertReport
     including post-validation findings.

3. `6e7ef37` feat(bedrock): BedrockPanel.svelte UI (+682 LOC)
   - 3-card layout: Source (auto-runs font audit on file pick),
     Options (2b|3b segmented + XMP fields + skip_font_check),
     Output (file picker + Convert button).
   - Hero post-convert card: green ✓ badge with PDF/A-2b label,
     4-stat grid (sanitized, OutputIntent, XMP, unembedded fonts),
     1.4s gold strata-sweep animation cubic-bezier(0.34, 1.56, 0.64, 1),
     prefers-reduced-motion safe.
   - Validation findings list with Error/Warning/Info severity coloring
     + ISO §clause refs.
   - Wired into +page.svelte features[] + main router + detached router.

4. (Slice 6 in commit 3) Keymap:
   - ActionId::BedrockOpen added to src-tauri/src/keymap/action.rs with
     default `Mod+Shift+A` in the "Archive" group.
   - "bedrock.open" added to ActionId union in src/lib/keymap.ts.
   - Global hotkey handler in +page.svelte: Cmd+Shift+A → switches to
     bedrock panel (skips input/textarea/contenteditable focus per the
     same pattern used by theater.start).

Also:
- `docs/release-notes/v3.0.0.md` (3.6 KB marketing copy) ready for the
  Adobe-$239/yr-vs-free pitch + 4-pass pipeline explainer.

## Gates

- cargo fmt --check: clean
- cargo check --lib: clean (4.7s incremental)
- cargo test --lib + clippy + pnpm check: deferred to CI
  (Mac mini at 94% disk; full debug build of slab-app+mockito OOMs).

## What's next

- Push branch end of tick → poll CI.
- If green: MODE A merge → main, bump manifests 2.4.0 → 3.0.0, tag v3.0.0,
  trigger build CI + Docker CI, MODE B finalize release with the
  release-notes file.
- v3.0.1 backlog: destructive font-embedding pass for unembedded
  standard-14 case (currently gated behind skip_font_check).
- v3.1.0 ideas: batch folder convert (drag a folder → all PDFs
  converted), CLI `slab archive` command.

## Buy-Button audit

4/4 PASS:
- Pay-for-it: Adobe Pro DC $239/yr Pro-only feature. 50-lawyer firm:
  $11,950/yr → $0.
- Notice-it: new sidebar entry "Archive (PDF/A)" 📐 + Cmd+Shift+A
  shortcut + auto-palette entry.
- Pick-us: Preview/PDF Expert/free Foxit can't do PDF/A. Ghostscript
  emits invalid output 80% of the time.
- Tell-a-friend: NARA/eIDAS/ISO 14641/IRS mandate PDF/A. 8-sec demo
  bait (drag PDF → green ✓ + strata sweep anim).

LAST_WOW_TICK_AT: 2026-05-22T22:00 PT (gold strata sweep + 4-stat grid).
