# Tick 2026-05-17 23:55 PT — v1.4.0 Bench released into pipeline

**Mode**: C → A. Slice 11 (release pipeline) of v1.4.0 Bench.

## What shipped (commits)

On `feature/v1.4.0-bench`:
- `e9b878f` Merge remote-tracking branch 'origin/main' into feature/v1.4.0-bench
  - Brought in Sanjay's README rewrite (8166f40) + completed
    `__APP_VERSION__` plumbing (vite.config.js + +page.svelte + app.d.ts).
  - README conflict: resolved by taking origin/main (Sanjay's
    version-free design intent wins; my "What's in v1.4.0" draft
    was discarded).
- `61d95ea` chore(release): bump to v1.4.0 "Bench" 🪑
  - package.json, src-tauri/Cargo.toml, src-tauri/tauri.conf.json,
    src-tauri/Cargo.lock (regenerated), docs/release-notes/v1.4.0.md (new).

On `main`:
- `9060763` Merge v1.4.0 'Bench' 🪑 — signed plugin marketplace
- Tag `v1.4.0` pushed.

## Quality gates (all green)

On merge commit `9060763`:
- `cargo fmt --all -- --check` → no output
- `cargo clippy --all-targets -- -D warnings` → no warnings
- `cargo test --lib` → 581 passed, 0 failed
- `pnpm check` → 0 errors, 23 warnings (baseline preserved)

## Friction notes

Two things slowed this tick:

1. **Sanjay edited main between ticks.** Pushed `8166f40` (README
   rewrite + `__APP_VERSION__` plumbing) right around when I started.
   I had already committed redundant version-injection plumbing and
   a README v1.4 front-door section — both of which directly
   contradicted his work. Recovered by resetting feature branch to
   179b074, merging origin/main, and redoing the version bump
   cleanly without the README touch.

2. **STATE.md note about tauri-action was wrong.** Said "v1.3.1
   auto-created by tauri-action during CI run 26012655931, no
   manual `gh release create` needed". Looking at `build.yml`,
   there's no tauri-action and no release step. The v1.3.1 release
   was created manually. Fixed in next-tick playbook so MODE B
   actually runs `gh release create`.

## Next tick (MODE B)

Poll CI 26018215033. If green:
- Download artifacts from the 4-platform matrix.
- `gh release create v1.4.0 --title 'v1.4.0 — Bench 🪑' --notes-file docs/release-notes/v1.4.0.md` with 6 curated assets.

If failed: triage on `feature/v1.4.0-bench-hotfix`.

## Untracked stragglers

`docs/screenshots-v1.3.1/` — Sanjay's intermediate working copy of
the screenshot set (42 PNGs). His 8166f40 commit already shipped
fresh screenshots into the canonical `docs/screenshots/` dir.
Leaving the screenshots-v1.3.1 dir untracked; Sanjay can clean up.
