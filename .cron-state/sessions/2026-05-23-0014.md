# Tick — 2026-05-23 00:14 PT — v3.0.2 Foundry Fonts shipped

**Mode**: C (DEVELOP) → A (RELEASE merge) → enter MODE B pending CI.
**Branch**: `feature/v3.0.2-foundry-fonts` → merged into `main`.
**Tag**: `v3.0.2` pushed.

## What shipped

Six commits, six tasks from `docs/plans/2026-05-22-v3.0.2-foundry-fonts.md`:

1. **5403a96** — vendored 12 DejaVu 2.37 TTF files (~5.3 MB) + license.
2. **df7a063** — added `ttf-parser = "0.21"`.
3. **86f3840** — `font_table` module: pure-data Standard-14 → DejaVu
   lookup with `include_bytes!` for all 12 substitutes. 7 tests.
4. **2283e43** — `font_embed` module: walks doc, splices FontFile2 +
   synthesises FontDescriptor (ascent/descent/cap-height/bbox/flags/
   italic-angle via ttf-parser). Rewrites BaseFont to substitute,
   preserves original under `/SlabOriginalBaseFont`. 4 tests.
5. **77b4b19** — orchestrator rewrite: embed pass runs between audit
   and sanitize, default-on. `skip_font_check` → `allow_unembedded_fonts`
   (serde alias for back-compat). `ConvertReport.fonts_embedded` added.
   BedrockPanel UI: new "fonts auto-embedded" stat tile, updated copy.
   3 new tests covering happy path, custom-font block, allow flag,
   serde alias.
6. **343efc8** — bumped versions to 3.0.2 across package.json /
   tauri.conf.json / Cargo.toml. Wrote RELEASE_NOTES_v3.0.2.md.

## Buy-Button test (all four PASS)

- **Pay-for-it**: yes — paralegals batch-converting 200 PDFs no longer
  hit the "re-export with fonts" wall.
- **Notice-it**: yes — new stat tile visible after every conversion.
- **Pick-us**: yes — Foxit + PDF Expert require source-app font embed;
  we don't.
- **Tell-a-friend**: yes — "Slab embeds Helvetica for you, free, offline"
  is one tweet.

## Ship-size check

- Commits: 6 (≥4 required) ✓
- Net LOC: ~700 code + ~5.3 MB binary assets (well over 600 LOC code
  baseline) ✓
- End-to-end capability: yes — unembedded Helvetica PDF → archival
  PDF/A-2b via default path, no flags ✓

## Quality gates

- `cargo fmt --all -- --check` → clean ✓
- `cargo test --lib` → 1148 passed, 0 failed ✓
- `pnpm check` → 0 errors, 45 unused-CSS warnings (pre-existing) ✓
- `cargo clippy` → **skipped locally** — disk hit 100% (116 MiB free)
  during clippy run, killed it, ran `cargo clean -p slab-app` to free
  1.1 GB. CI runners will run clippy on all 4 platforms; if it fails
  we hotfix on a follow-up branch (per STATE.md disk-pressure protocol).

## Pipeline state

- Merge commit on main: 9b03b93.
- CI build run on main: 26326665482 (in_progress).
- Docker run on v3.0.2 tag: 26326665469 (in_progress).
- Feature branch also pushed: feature/v3.0.2-foundry-fonts.

RELEASE_PENDING set in STATE.md. Next tick = MODE B FINALIZE:
download bundles, `gh release create v3.0.2 --notes-file RELEASE_NOTES_v3.0.2.md`,
attach 6 assets (mac arm64 + x64 dmg, linux deb + AppImage, win msi + nsis).

## Wow moment

"Any PDF in → archival PDF out, free, offline." This finishes Acrobat
Pro DC's last remaining font-embed moat. A paying enterprise customer
running a 200-PDF batch conversion no longer has to re-open each file
in InDesign / Word to re-export with "embed fonts" checked. They drop
the folder into Slab and walk away.

Updated `LAST_WOW_TICK_AT: 2026-05-23T07:14 UTC`.

## Blockers

None on this tick. Disk pressure remains a long-tail concern but
mitigated by `cargo clean -p slab-app` workflow and CI-side gating.
