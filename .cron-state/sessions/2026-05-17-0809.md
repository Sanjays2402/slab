# 2026-05-17 0809 — v0.13.1 Lens Patch (fix-forward release)

## Mode
- Started: MODE B intent (finalize v0.13.0)
- Pivoted: MODE C → MODE A in one tick (v0.13.0 CI failed, fix-forward to v0.13.1)

## What happened

1. Woke to STATE flagged `RELEASE_PENDING: v0.13.0`. Checked `gh run view 25994119497` → completed with conclusion **failure**.
2. `gh run view --log-failed`: Windows job crashed on `pdf::table_extract::tests::end_to_end_*`. Root cause: xpdf-flavored `pdftotext` (Glyph & Cog v4.00) on PATH, doesn't support `-bbox-layout`. Bundling skipped on all 3 platforms → no v0.13.0 artifacts.
3. Created `feature/v0.13.1-lens-patch` from clean main.

### Commit 1: `dfe752e` — fix Windows pdftotext detection
- Two-step probe in `require_pdftotext`: `-v` then sniff `-h` for `-bbox-layout`.
- Test-side `pdftotext_available()` mirrors prod logic.
- New unit test `require_pdftotext_agrees_with_local_probe`.

### Commit 2: `44529dc` — `slab lens preflight` + `pdf::preflight` module
- ~530 LOC including 9 unit tests.
- 4 probes: pdftoppm, tesseract, Poppler pdftotext (flavor-checked), Ollama (raw TCP-connect, 500ms).
- CLI: `slab lens preflight [--json] [--ollama URL]`, non-zero exit on failure.
- Help text updated.

### Commit 3: `9572795` — version bump + release notes
- Cargo.toml, tauri.conf.json, package.json, Cargo.lock, sidebar pill all 0.13.0 → 0.13.1.
- `docs/release-notes/v0.13.1.md` (~2.9 KB).

### MODE A: merge to main
- Merge SHA `e0bb049`. `--no-ff` with message file.
- All 4 quality gates re-run on main: ✓✓✓✓ (356 lib tests, fmt, clippy `-D warnings`, svelte-check 0 errors).
- Tag `v0.13.1` with "Slab v0.13.1 — Lens Patch 🔧".
- Pushed `main` + tag.
- New CI run: `25994802981` (in_progress).

## Next tick
- MODE B: poll CI `25994802981`, finalize GH release with 6 artifacts when green.
- v0.13.1 will be the **first Lens-line release on GH**.

## Stats
- Tests: 346 → 356 (+10)
- Commits this tick: 3 (+ 1 merge)
- New module: `pdf::preflight`
- LOC added: ~700
