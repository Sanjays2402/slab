# 2026-05-23 07:13 PT — v3.1.0 Loom Slice 3 shipped

Branch: feature/v3.1.0-loom-slice-3 (3 commits + 1 nit, ready to push)

Buy-Button verdict: PASS on Pick-us (only free, offline tool that
does column-aware PDF/UA reading order) and Notice-it (new Reading
order tab visible immediately on next launch).

Commits:
1. 4c8ef2f feat(loom): column-aware reading-order traversal (Slice 3)
2. 99bce70 feat(loom): slab_loom_reading_order_summary Tauri command
3. 3a669d6 feat(loom): Reading order tab in LoomPanel (Slice 3 UI)
4. (this) chore(loom): doc-comment fmt nit + STATE + session log

Quality gates:
- cargo fmt --all -- --check: green
- cargo clippy --lib -- -D warnings: green
- cargo test --lib: 1194 passed
- pnpm check: 0 errors, 46 (pre-existing) warnings

Net LOC ≈ 730 prod (above the 600 bar):
- ~370 reading_order.rs (incl. 9 unit tests)
- ~125 Tauri command + types
- ~245 Svelte UI + CSS

End-to-end capability: open a 2-column research paper -> "Reading
order" tab -> "Compute reading order" -> see the numbered flow walk
left column top-to-bottom then right column, with role-coloured tag
chips (H1, P, Figure, Artifact) and a per-page columns table.

Algorithm highlights:
- Detect page-spanners (width >= page_width * 0.7)
- Cluster remaining nodes by midpoint X with 18pt gap threshold
- Collapse 3+ bands to 2 (most "3-column" PDFs are 2 cols + sidebar)
- Sort narrow bands LTR, top-to-bottom within each
- Artifacts (folio, headers) parked after content; total_reading_nodes
  excludes them so the count matches what a screen reader emits

Determinism guaranteed (pure function, tested). No panics on weird
input — empty pages, missing geometry, single-column docs all sane.

Next tick: Slice 4 (alt-text). Beacon llava with SHA-256 disk cache
for figure descriptions. Wires into the Reading-order flow preview so
Figure entries show their generated alt-text. That'd be the wow
moment for the v3.1.0 marketing message: "Free, offline, AI-generated
alt-text — Acrobat Pro is $239/yr AND uploads your files."
