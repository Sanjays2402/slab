# Tick 2026-05-21 01:35 PT — MODE C writing-plans — v2.0.12 "Sign" ✒️

**Skill invoked**: `writing-plans` (10th consecutive tick of this pattern).

**Time gate**: `TZ=America/Los_Angeles date '+%u %H'` → `4 01` = Thursday 1:35 AM
PT. Off-hours. Greenlit.

**Decision**: Promote next release plan in the pipeline. v2.0.2–v2.0.11 already
on disk. Slot v2.0.12.

**Why "Sign" ✒️ next?**
1. Single biggest paid-tier gap still uncovered. Adobe Sign $180/yr, DocuSign
   Personal $120/yr, Acrobat Pro $239/yr partly for signatures, PDF Expert
   $79/yr ships sigs + 5 stamps. macOS Preview ships exactly one signature
   workflow (trackpad-only, no library, no stamps). Slab today: zero.
2. Every primitive needed is now in place post-v2.0.11:
   - v2.0.6 sidecar JSON → reuse for signature/stamp persistence
   - v2.0.7 RDP ink capture → reuse for drawn signatures (extract to shared
     `inkCapture.ts`, refactor `DrawLayer.svelte` to import)
   - v2.0.10 edit mode → drag, resize, undo work for free
   - v2.0.11 rotation + multi-select → tilt-on-place, batch-rotate signatures
3. Zero new Tauri commands. Zero new Rust deps. One new frontend dep family
   (`@fontsource/{caveat,dancing-script,great-vibes}`, all OFL, self-hosted,
   ~75KB total woff2).
4. Cleanly defers cryptographic PKCS#7 PDF signing to v2.1.0 "Notary" — the
   $49 Pro-tier hook. v2.0.12 ships the *visual* half (which already replaces
   $120-180/yr of competing SaaS for internal sign-offs).

**Buy-Button 4/4 PASS**:
- Pick-us: Adobe / DocuSign / PDF Expert / Foxit all ship sigs. Launch-blocker.
- Notice-it: new Sidebar panel, `s` shortcut, `Cmd+Shift+S` palette,
  Settings → Signatures section, 5 palette entries, OnboardingTour step #16.
- Tell-a-friend: **trackpad capture modal w/ pressure ink + live RDP smoothing**
  + **320ms rubber-stamp drop animation (scale-1.5→1.0 + rotate-3° + 12
  ink-spatter droplets, cubic-bezier `(0.34, 1.56, 0.64, 1)`)**. Screenshot bait.
- Pay-for-it: Adobe Sign $180/yr → v2.0.12 replaces internal-sign-off use.

**Plan structure** — `docs/plans/2026-05-21-v2.0.12-sign.md` (~34 KB):
- Slice 0 (pre-flight): 11 verification checks
- Slice 1: `signatures.ts` + `stamps.ts` + 24 vitest cases
- Slice 2: Rust `Annotation::Signature` + `Annotation::Stamp` + 6 tests
- Slice 3: `SignatureCaptureModal.svelte` w/ Draw/Type/Import tabs + extracted
  `inkCapture.ts` (8 tests)
- Slice 4 ⭐ WOW: single-key `s` + stamp palette + 320ms rubber-stamp animation
- Slice 5: Settings + Command Palette + 32 new i18n keys
- Slice 6: OnboardingTour step #16 + version bump + customer release notes +
  MODE A merge + tag + push

**Commits this tick (2 on main)**:
- `<HASH>` docs(plans): v2.0.12 "Sign" implementation plan ✒️
- `<HASH>` chore(cron): STATE.md + session log — v2.0.12 plan promoted

**Scheduling**: do not start v2.0.12 Slice 1 until v2.0.11 ships + tags.
Strict order: v2.0.2 → … → v2.0.11 → v2.0.12.

**Open questions baked into the plan**:
- Cryptographic PKCS#7 signing → v2.1.0 "Notary"
- Cloud signature sync → v2.x Pro tier (paid-tier hook)
- Signature request workflow (server-mediated) → maybe v3.x
- Multi-party signing → v2.2.0 "Form" candidate
- Initials stamp → v2.0.13 quick-win patch
- **Eleven-plan backlog now on disk** — ~10-11 weeks of execution ticks
  front-loaded.
