# Round 45 — 2026-06-27 ~09:36-10:01 PT

BATCH: 5 frontend/UX capabilities. OCR Queue Panel -> "Atlas VI" (Beacon grade).

## Surface picked
`src/lib/panels/OcrQueuePanel.svelte` — full modal (cmd palette / Cmd+Shift+O /
`slab:open-ocr-queue`). Structurally identical to Beacon Cache before round 40:
two flat lists (failure inbox + pending preview), bare "Loading…" (flagged on
roadmap), local toast, no filter/sort/keyboard, zero pure-core tests. Deliberate
pivot off round-44 RecentsHome.

## Shipped (2 commits)
- 58ea2d3 — feat(ocr-queue): tested pure view-core (new `src/lib/ocrQueueView.ts`,
  122 inline tests). All five capabilities' logic, green standalone.
- 2f8eda5 — feat(ocr-queue): cohesive component wire (OcrQueuePanel.svelte,
  +556/-54). Search bar + mark highlight, segmented sort, reason-facet pills,
  virtual cursor across both lists, aria-live footer + run-all preview.

Five capabilities:
1. searchOcrDocs — filter over both lists, basename highlighted, silent matches
   on folder/state/error reason.
2. cycleOcrSort/sortOcrDocs — Name/Folder/Pages/State segmented sort + caret,
   numeric-aware, both lists.
3. canonicalizeOcrError/groupFailureReasons/filterByReason/reconcileReasonFacet
   /describeDominantReason — collapse noisy ocr_error to short buckets, clickable
   facet pills + dominant lede, stale-facet auto-clear.
4. flattenOcrRows/classifyOcrTableKey/nextOcrCursor/clampOcrCursor — one virtual
   cursor failures-then-pending, arrows wrap / Enter run-or-retry by section /
   O open / Esc park, ring only no focus move.
5. describeOcrView/summarizePending/describeOcrImpact — aria-live context footer
   + run-all workload preview (docs/pages).

Also: pending cap 20->60 with actionable overflow; local basename consolidated
onto view-core ocrBasename.

## Gates (once, end of batch)
- ocrQueueView.test.ts 122/122 pass
- Siblings unchanged: paletteSearch 253, librarySearchView 127, beaconCacheView
  112, readerFindView 86, recentsHomeView 91 (shared palette core provably intact)
- pnpm check: 0 errors / 104 warnings (rounds 32-44 baseline held EXACTLY)
- cargo fmt --all --check clean; ZERO Rust files changed
- Pushed e52a382..2f8eda5 -> origin/main, verified

## Commit-structure note
2 commits, not 5 — same honest round-42 rationale. The component wire interleaves
all five capabilities into shared derived state + one template; can't split into
5 separately-COMPILING commits without pushing red intermediates. Core is its own
commit; wire is one cohesive commit.

## Deferred / follow-ups (logged to roadmap)
- per-reason "Retry all <reason>" (retry only the faceted bucket)
- real progress bar while Run-all is in flight (currently fire-and-refresh)
- pending image-only/mixed state as a clickable facet
