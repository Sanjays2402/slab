# Round 29 — Hopper batch fix-all action

**Tick:** 2026-06-23 09:15 PT (cron lock taken, batch built, lock released).
**Active branch:** `main`. **Push verified** against `origin/main`.

## Shipped (5/5)

One cohesive arc: batch fix-it for dead rules. Round 28 shipped
per-row "Fix it" pills that fix one dead rule per click; round 29
adds the batch path — one click applies every planner proposal.

- **138 — batch reorder applier primitive** (`5faa433`)
  `apply_reorder_proposals_batch(rules, proposals) -> BatchReorderOutcome`
  with by-name source resolution (indices drift after every splice,
  so we MUST resolve by name) + by-name shadower target with target=0
  fallback + RuleNotFound/AlreadyEarlier skip discriminator +
  conservation invariant applied+skipped == input + saturating_add
  on u64 + 14 tests.
- **139 — batch applier TS mirror + summary** (`e1c840e`)
  `applyReorderProposalsBatch` 1:1 with shared per-rule object identity
  + RULE_NOT_FOUND/ALREADY_EARLIER stable singletons +
  `summarizeBatchReorderOutcome` five-branch discriminated copy with
  plural-aware nouns + `describeSkipReason`. 58 inline tests including
  end-to-end planDeadRuleReorder → applyReorderProposalsBatch.
- **140 — batch applier Tauri command + TS wrapper** (`7e7267d`)
  `slab_hopper_batch_reorder_dead_rules` wired into the invoke
  handler + `slabHopperBatchReorderDeadRules` async wrapper with
  browser-mode delegation. 13 wrapper-delegation tests pinning every
  BatchReorderOutcome field on a mixed batch + empty-proposals path.
- **141 — batch-confidence bridge helper** (`be3f801`)
  `worstReorderConfidence(proposals) -> "high"|"medium"|"low"|null`
  with worst-tier-wins short-circuit + order-independence +
  `summarizeProposalTierBreakdown` with total-invariant +
  `describeProposalBatch` discriminated copy with single-tier
  shortcut + order-independent enumeration + plural-aware noun. 32
  inline tests.
- **142 — Fix all (N) batch fix-it button + popover** (`dab4715`)
  Button anchored as sibling of chain-health chip. Worst-tier color
  treatment (.conf-high green / .conf-medium orange / .conf-low
  muted). 380px confirm popover with breakdown header + tone subline
  + per-proposal preview list (tier-colored dots, scrollable max-height
  180px). Optimistic apply via applyReorderProposalsBatch then
  persist via slabHopperSetRules with rollback on failure. applied=0
  fast path skips no-op persist. Outcome toast shares cov-export-toast
  surface. Skipped proposals log to console.info for audit. Escape
  chain extended: Fix-all > per-row fix-it > Export menu > drilldown
  > filter clear. ~160 lines scoped CSS.

## Gates (single batch gate at end)

- `cargo fmt --all` clean (no changes needed)
- `cargo clippy --lib -- -D warnings` PASSED in 11.52s
- `cargo test --lib` **2581 passed / 0 failed** (round-28 baseline
  2567 + 14 batch primitive tests from slice 138)
- `pnpm check` **0 errors / 104 warnings** (round-28 baseline
  preserved EXACTLY — zero new warnings from any of round 29's
  markup, $derived blocks, or scoped CSS)
- `tsx src/lib/hopper.test.ts` **359 inline expects pass** (256 +
  58 + 13 + 32 = 359)
- `tsx src/lib/marketplace.test.ts` 138 unchanged

## Deferred

None — all five slices solid and demo-able. The "what / now what /
how / all at once" four-step loop now closes end-to-end for the
dead-rule diagnostic.

## Closing-notes carryover (future rounds)

- **Reorder-by-drag** still on the candidates list but now LESS
  critical: a 3-dead-rule chain fixes in one "Fix all" click rather
  than three drags. Still a richer interaction model worth a
  dedicated round.
- **Undo-the-fix-all** — "Undo" button in the toast that pops the
  chain back to its prior state via a captured `prev` snapshot
  (same pattern as the personal-preset duplicate toast).
  Particularly useful for the batch path where reverting a
  five-rule reorder by hand is tedious. Natural follow-up to round
  29 — the `prev = rules` snapshot already exists in `applyFixAll`,
  the toast just needs an Undo button to invoke `slabHopperSetRules(watchId, prev)`.
- Drilldown row → cross-surface filter; Loom-grade tagging
  explorer; doc-detail metadata editor; Beacon cache inspector
  polish; Quill multi-document field-detect queueing; histogram
  hover-tooltip; per-plugin "Run prune now" affordance.
