# TICK 2026-05-21 00:42 PT — MODE C writing-plans skill — v2.0.10 plan promoted 🔧

User invoked the `writing-plans` skill an **eighth** consecutive tick.
This tick: **v2.0.10 "Lathe" 🔧** — annotation edit mode (selection, drag,
resize, undo/redo, copy-paste). Redeems FIVE deferred items stacked
across v2.0.6–v2.0.9 open-questions blocks.

**Why v2.0.10 next?**
1. STATE.md line 11 explicitly pre-claimed the codename "Lathe" for
   the next release post-v2.0.9 ("v2.0.10 ('Lathe') can pivot to
   edit-mode polish (selection handles, drag-to-move, undo/redo)").
   Plan redeems that pledge.
2. After v2.0.9 ships, Slab has 5 annotation primitives but ZERO
   editability — clicking an existing annotation does nothing. That
   is a true launch-blocker for any reviewer who tries the markup
   toolkit for >30 seconds.
3. The buy-button verdict passes 4/4: Acrobat / PDF Expert / Foxit /
   Drawboard all ship select+move+resize+undo as table stakes.
4. Wow moment is double-stacked: Figma-style snap guides on drag +
   220ms dissolve/restore animation + cross-document copy-paste —
   none of the paid competitors ship snap-guides on annotation move.

**Commit this tick (1 + STATE chore = 2 on main):**
- `<HASH>` docs(plans): v2.0.10 "Lathe" implementation plan 🔧
- `<HASH>` chore(cron): STATE.md + session log — v2.0.10 plan promoted
- Plan file: `docs/plans/2026-05-21-v2.0.10-lathe.md` (~51 KB, 6 slices + pre-flight, ~2200 LOC, 8 commits, 66 new tests).

**Plan highlights baked in:**
- New `selection.ts` + `history.ts` pure-TS modules (in-memory only).
- New `SelectionLayer.svelte` over pdfjs canvas — 8 handles, marching-ants SVG border, `prefers-reduced-motion` respected.
- Live drag via CSS `transform` (zero pdfjs re-renders mid-drag).
- Command-stack undo/redo per-document, 200-deep cap, branch-rewrite on new apply (matches every editor).
- 8 new `ActionId` variants — j/k/Delete/Mod+D/Mod+Z/Mod+Shift+Z/Mod+C/Mod+V.
- Mod+D collision with v2.0.5 bookmark resolved via context guard (selection!=null → duplicate; else → bookmark).
- Clipboard uses custom MIME `application/x-slab-annot-v1+json` with plain-text fallback for Linux/Wayland.
- Cross-document paste works (new ID assigned; placed on current page).
- Figma-style snap-to-edge guides at 4px threshold (accent-tinted dashed lines).
- 22 new i18n keys under `annotations.edit.*` / `palette.edit.*` / `settings.annotations.edit.*` / `onboarding.edit.*` — all disjoint with v2.0.2–v2.0.9.
- OnboardingTour step #14 ("Annotations are editable").

**Comparison table baked into plan** vs Adobe / PDF Expert / Foxit on 12 markup capabilities — Slab wins on snap-guides, keyboard-first, sidecar JSON, free/offline/no-telemetry; ties on the other 9 after v2.0.10 lands.

**Scheduling note (critical):**
Do not start v2.0.10 Slice 1 until v2.0.9 ships and is tagged.
Strict order: v2.0.2 → 3 → 4 → 5 → 6 → 7 → 8 → 9 → 10.

**Open questions:**
- Group/lasso multi-select → v2.0.11
- Rotate handle → v2.0.11
- Cross-page drag → deferred (use copy-paste)
- Touch gestures → deferred (Tauri mobile lands)
- Undo across documents → out of scope (per-doc matches every editor)

**Buy-Button verdict at release level: 4/4 PASS.**

**Strategic framing:** After v2.0.10 lands, Slab's annotation toolkit
hits feature parity with Acrobat's Comment workspace + WINS on three
differentiators (snap-guides, keyboard-first, sidecar JSON). That
parity is the v1.0.0 launch narrative.

