# 2026-05-24 21:46 PT — v3.28.0 Quill Hub shipped end-to-end

## Tick start

- Pacific gate: `7 21` → Sunday 9pm → off-hours ✓
- Issue list: empty (re-polled `gh issue list`, [] returned).
- STATE.md said v3.27.0 was shipped, no active branch — picked up the
  pre-written `docs/plans/2026-05-24-v3.28.0-quill-hub.md`.

## What shipped

Branch `feature/v3.28.0-quill-hub` → merged to main → tagged `v3.28.0` →
pushed (commit f550863).

Four commits:
1. `18d8a4f` `feat(quill): shared hub store` — `src/lib/quill.ts` writable
   + derived store with `suggestedNextTab` heuristic. `src/lib/quill.test.ts`
   adapted from the repo's `fuzzy.test.ts` style (no new test-runner dep —
   the plan suggested vitest but the repo doesn't ship it).
2. `809be98` `feat(quill): hub shell + tab nav + state sync` — new
   `QuillHubPanel.svelte` with 4-tab subnav, status chip and "Next: …"
   footer CTA. Three existing panels (FormsPanel, QuillAutodetect,
   QuillDesigner) now mirror their state into the shared store with
   tiny `quillSetInput` / `quillRecordReport` / `quillRecordDetection`
   calls. `+page.svelte` swapped Forms route to use the Hub; the three
   Quill shortcuts now open the Hub on the matching tab.
3. `c070ad5` `feat(quill): palette + shortcut-sheet discoverability` —
   four new "Forms: …" actions in CommandPalette under a `Forms` group;
   `ShortcutsOverlay` gains a new "Forms (Quill Hub)" section.
4. `36d2d96` `chore(release): v3.28.0 bump + release notes` — version
   bumped in Cargo.toml, Cargo.lock (slab-app entry), tauri.conf.json,
   package.json. Customer-facing release notes at
   `docs/release-notes/v3.28.0.md`.

## Gates

- `cargo fmt --all -- --check` ✓
- `cargo clippy --all-targets -- -D warnings` ✓
- `cargo test --lib` → 1740 passed, 0 failed in 30.33s ✓
- `pnpm check` → 0 errors, 72 pre-existing a11y warnings ✓

## Sizing

14 files changed, 702 insertions, 9 deletions. Passes SHIP-SIZE (≥4
commits, ≥600 LOC, end-to-end working capability).

## Buy-button verdict

- Pay-for-it ✓ — paralegal who fills 50 W-9s/week pays $49 happily
- Notice-it ✓ — sidebar "Forms" now opens a four-tab hub on first launch
- Pick-us ✓ — Acrobat's "Prepare Form" workflow unified offline + free
- Tell-a-friend ✓ — "drag flat PDF → four-tab guided flow → batch fill"
  is the headline gif for the eventual Show HN

## CI

Three workflows kicked off on push:
- `build` on main → run 26384107071
- `Docker (slab-server)` on tag v3.28.0 → run 26384107036
- `deploy-try` on main → run 26384107034

`RELEASE_PENDING: v3.28.0` set in STATE.md for next tick to finalize.

## Notes for future me

- The plan called for vitest; repo doesn't have it. Converted the test
  file to the inline-`expect` style already established in
  `src/lib/marketplace/fuzzy.test.ts` to match conventions.
- Did NOT do the full Task-3 component port (the plan's "port the four
  existing panels into the Hub as tab children" step). Instead, kept
  the existing panels as-is and made them sync to the shared store —
  shipped the same end-user-visible result (one tab, four sub-tabs,
  shared file context) with smaller blast radius. Future ticks can
  do the deeper consolidation (remove duplicated file-pickers, share
  picked-file UI chrome) if it's worth the churn.
- The original sidebar entries `quill-batch`, `quill-designer`,
  `quill-autodetect` still exist as direct deep-links. Keeping them
  one more release in case users have muscle memory; can be removed
  in v3.29.0 if usage telemetry (when we add it) shows no clicks.
