# Round 39 — 2026-06-26 03:35 PT (Cake cron, 20-min batch)

## Arc: "Lexicon" — the "?" keyboard cheat-sheet overlay

PIVOTED off the 3-round Command Palette streak (rounds 37-38, now
power-user-complete) to the explicit roadmap candidate "keyboard-shortcut
cheat-sheet overlay (? key)". The ShortcutsOverlay existed but was a
~120-row hand-maintained literal array silently DRIFTING from the live
keymap, with no search, no keyboard nav, no conflict detection.

Rebuilt on a new tested pure core `src/lib/shortcutsOverlay.ts` (95
inline tests), reusing the tested palette core (scorePaletteField,
splitHighlight, classifyPaletteNav, nextPaletteIndex) so there is no
second fuzzy/nav engine.

## Shipped 5/5

1. **Live-keymap grouping** (92a5d4f) — buildShortcutGroups / shortcutGroupRank
   / countShortcutRows. Bindable rows off keymapView; curated rank-then-alpha
   order; "custom" pill on rebinds; INFO_HINTS for un-bindable hints. 30 tests.
2. **Filter-as-you-type** (b0648c1) — filterShortcutGroups reuses palette
   scorePaletteField; matches label/keys/group-title; live <mark>; autofocus;
   Esc-clears-then-closes; shown/total count; empty state. 17 tests.
3. **Keyboard nav** (1b87b92) — flattenShortcutRows -> one index space; reuses
   classifyPaletteNav + nextPaletteIndex; accent focus ring + scroll-into-view;
   cursor snaps to first match. 10 tests.
4. **Conflict detection** (48ba0e9) — canonicalizeBinding (order-independent,
   alias-normalized) + detectShortcutConflicts + conflictingActionIds. Surfaces
   the REAL library.search/forms.open Mod+Shift+F collision. Amber banner +
   per-row "conflict" pill (#ffb648). 24 tests.
5. **Copy-as-text** (36bfac2) — formatShortcutsText column-aligned export,
   filter-aware, clipboard + success/error toast, "copied" flip. 14 tests.

## Gates (run ONCE at end of batch)

- tsx shortcutsOverlay.test.ts: 95/95 pass (30+17+10+24+14)
- tsx paletteSearch.test.ts: 253/253 unchanged
- tsx toastStack.test.ts: 254/254 unchanged
- tsx hopper.test.ts: 1189/1189 unchanged
- pnpm check: 0 errors / 104 warnings (rounds 32-38 baseline EXACT, zero new)
- Zero Rust files changed -> round-32 lib baseline (clippy clean, 2620 tests)
  carries forward. Full Tauri binary build never run (wedges disk).

## Notes

- All 5 frontend/UX per the standing override. Zero backend.
- Deliberate pivot; the palette is power-user-complete after rounds 37-38.
- Reuse over reinvention: imported the palette core rather than rolling a
  second fuzzy/nav engine.
- Caught + fixed one real svelte-check error early (Array.isArray widening
  the generic flatten callback to any[]) before final gating — never red.
- Pushed 92a5d4f..36bfac2 to origin/main, verified landed.

## Deferred / next candidates

- palette: group COLLAPSE (click header to fold) — still open.
- palette: recent-files thumbnails (getRecentThumb data URLs already stored).
- shortcuts: a "rebind" deep-link from the conflict banner straight into
  Settings -> Keyboard shortcuts (would need a cross-panel nav event).
- drilldown row -> cross-surface filter; histogram hover-tooltip; Beacon
  cache inspector polish; doc-detail metadata editor; Loom tagging explorer.
- configurable toast position; responsive Hopper editor; focus-trap polish;
  reduced-motion pass; touch/pointer drag-reorder fallback.
