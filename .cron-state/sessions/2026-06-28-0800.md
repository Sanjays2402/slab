# Round 50 — 2026-06-28 08:00-08:55 PT (Cake cron, 20-min batch loop)

## DISK EMERGENCY (recovered)
Tick opened with both build/repo volumes detached AND the repo image corrupt:
- `/Volumes/Projects` (repo) and `/Volumes/SlabBuild` both missing at start.
- `hdiutil attach SanjayProjects.sparseimage` -> "no mountable file systems".
- First `fsck_apfs -n /dev/disk6s1` -> "container superblock is invalid" (EIO
  on block 0) — looked like real corruption.
- ROOT CAUSE: stale/busy attach state, not actual corruption. Fix sequence:
  `hdiutil detach -force` both stale disks -> clean `hdiutil attach -nomount
  SanjayProjects.sparseimage` -> `fsck_apfs -y /dev/disk6` (the CONTAINER, ran
  ~6 min in background, passed clean: "volume appears to be OK / container
  appears to be OK", exit 0) -> `diskutil mount disk6s1` -> repo back.
- SlabBuild re-attached cleanly (`hdiutil attach` -> auto-mounted disk8s1).
- ~10 min lost to recovery; still shipped all 5.

## Shipped 5/5 (all frontend, the full round-49 refill roadmap)
1. 15d1351 feat(ocr-queue): cancellable Requeue-all + Retry-remaining resume
   - ocrQueueView: describeRequeueRemaining + describeRequeueAllOutcome (+16
     tests, 212 total). requeueAllFailed loops ocrQueueRequeue per-doc over a
     snapshot, Cancel flag breaks between docs, planRunRemaining carves the
     un-run tail -> "Retry remaining (N)" banner + inline determinate bar.
2. e5d6fa2 feat(palette): legible bulk-collapse state in the footer
   - paletteSearch: describeCollapseState (+14 tests, 298 total). Live
     "N of M sections open" / "All M collapsed" beside the fold-all button.
3. 0f1c4c1 feat(recents-home): reset the pinned-strip drag order
   - recentsHomeView: anyPinOrder + describeResetPinOrder (+13 tests, 155).
   - recent.ts: clearPinOrder() (inverse of reorderPinned). "Reset order (N)"
     button in the Pinned header, shown only once a manual order exists.
4. 4d99d59 feat(library-search): pin a search to a sticky saved-search strip
   - librarySearchView: normalizePinnedQuery + isPinnedSearch +
     togglePinnedSearch(cap 32) + describePinnedSearches (+26 tests, 251).
   - NEW savedSearches.ts localStorage shell (mirrors paletteCollapsed.ts).
   - Per-chip pin toggle + a distinct accent "Saved searches" strip; kept
     isolated from the recents keyboard cursor (its own simple list).
5. 7b11748 feat(ocr-queue): first-load skeleton instead of a bare spinner
   - firstLoad gate renders shimmer placeholders (stats strip + a row
     section) on the very first fetch; aria-busy + sr-only live label;
     honors prefers-reduced-motion.

## Gates (run ONCE at end of batch)
- view-core tsx: ocrQueueView 212, paletteSearch 298, recentsHomeView 155,
  librarySearchView 251 = 916 pass, 0 fail.
- pnpm check: 0 errors / 104 warnings (rounds 32-49 baseline preserved
  EXACTLY — zero new a11y warnings across all 5 surfaces).
- cargo fmt --all --check: clean. ZERO Rust changed -> round-32 lib baseline
  (clippy clean, 2620 tests) carries forward. Full Tauri build never run.

## Process notes
- Pushed in TWO batches: the first 4 features pushed mid-tick (b6bcfbb..7b11748)
  to protect them against another disk event, then feature 5 (..4d99d59). Both
  on main, both verified landed via git fetch + log.
- Deferred nothing — all 5 roadmap items shipped despite the disk recovery.
- Refilled STATE.md roadmap with 12 round-50 frontend candidates.
