# Round 44 — 2026-06-27 06:10 PT (Cake cron)

WIRED the tested RecentsHome view-core ("Atlas V") into RecentsHome.svelte
— the first-launch hero, the single highest-visibility surface. Round 43
shipped the tested pure core (recentsHomeView.ts, 91 tests) but never
wired it; this is the "core then wire" second half (round-42 pattern).

5 frontend slices, all on main, all green, pushed (8ab126f..191eb37):

- fffc618  slice 1: tested partitionRecents() replaces inline $derived +
           monochrome chrome (pushpin/x emoji -> inline-SVG snippets)
- 1e1f667  slice 2: filter-as-you-type (filterRecents + highlightRecentName
           live <mark>, hero collapses, no-match empty state, Esc-clear)
- 9928658  slice 3: sort modes (sortRecentView Recent/Name/Progress/Pages
           segmented pill, both grids)
- 7fa4862  slice 4: keyboard nav (window keydown -> virtual cursor via
           flattenRecentCards/classifyRecentKey/moveRecentCursor/
           clampRecentCursor; Enter open / P pin / Backspace remove / Esc
           park; no Enter double-fire; filter box keeps text semantics)
- 191eb37  slice 5: aria-live summary footer (summarizeRecents +
           countInProgress) + keycap chord hint

Gates (one batch gate at end):
- recentsHomeView 91/91 (core already tested; this round wired it)
- shared palette suites ALL unchanged: paletteSearch 253, librarySearchView
  127, beaconCacheView 112, readerFindView 86, toastStack 254
- pnpm check 0 errors / 104 warnings (rounds 32-43 baseline EXACT)
- cargo fmt --all --check clean; ZERO Rust files changed

Notes:
- Frontend-focus override honoured: all 5 slices TS/Svelte. Zero backend.
- Reuse over reinvention: every capability routes through the view-core's
  reuse of the palette core — no second fuzzy/nav/progress engine.
- Fixed a standing chrome defect: emoji (pushpin + heavy-x) in RecentsHome
  violated Slab's icon-only monochrome rule; replaced with currentColor
  inline-SVG snippets.
- Virtual-cursor keyboard model (ring only, no focus move) chosen so Enter
  can't double-fire on a focused card <button>.
- Deferred (RecentsHome follow-ups, logged to candidates): tiny progress
  overlay on thumbnails; "clear all unpinned" footer affordance; pinned-
  strip horizontal keyboard scroll on overflow.

Next candidate: Library Search snippet refine-highlight (second-color
<mark> over the client refine term), or the RecentsHome follow-ups above.
