# Round 27 — Hopper coverage diagnostic filter

**When:** 2026-06-23 01:30 PT
**Branch:** main
**Tick result:** 5/5 shipped, gates green, pushed.

## Slices

- **128** `050f16e` — backend `filter_coverage_by_diagnostic` primitive + `CoverageFilter` enum (All|Dead|Zero|Shadowed|Healthy) + `slug()` helper + private `rule_matches_filter` composed from `coverage_diagnostic_str` + priority chain dead>zero>shadowed>healthy preserved end-to-end + totals (fallthrough + total_samples) preserved verbatim. 11 tests.
- **129** `83ddbe3` — TS mirror `filterCoverageByDiagnostic` + `ruleMatchesCoverageFilter` + `CoverageDiagnosticFilter` type + `COVERAGE_FILTER_KINDS` readonly array + private `coverageRuleBucket` classifier + `formatCoverageFilterSummary` discriminated copy + identity transform returns NEW shallow-clone rules array. 53 inline tests.
- **130** `8a0ecaa` — `slab_hopper_filter_coverage` Tauri command (registered in lib.rs) + `slabHopperFilterCoverage` async wrapper with browser-mode delegation + `suggestCoverageExportFilename` optional `filter` parameter inserting slug between watch + date with "all"/unset omitting slot entirely (round-26 back-compat byte-for-byte). 10 inline tests.
- **131** `522b1ad` — `coverageHealthClickTarget` bridge composing chain-health priority (critical->dead, warn+shadowed->shadowed, warn+zero->zero, warn+high-fallthrough->null, healthy/empty/null->null) matching `summarizeCoverageHealth` EXACTLY pinned by cross-helper-agreement test + never-returns-"all" pin. 26 inline tests.
- **132** `84f4784` — demo UI clickable chain-health chip (conditional <button> vs <span>) + 5-chip diagnostic filter row with active state + aria-pressed + shared `setCoverageFilter` handler + "Showing X of Y rules — dead" aria-live sub-line + right-anchored Clear filter button + filtered rule list with accent rail + fall-through synthetic row HIDES while filtered + no-matches empty cell with inline link-style clear-filter button + filtered exports ship displayedCoverage + toast suffix + Escape chain extended (filter clears LAST) + ~130 lines scoped CSS.

## Gates

- `cargo fmt --all` clean
- `cargo clippy --lib -- -D warnings` clean in 12.79s
- `cargo test --lib` 2555 passed / 0 failed (round-26 baseline 2544 + 11 from slice 128 = 2555)
- `pnpm check` 0 errors / 104 warnings (round-26 baseline preserved EXACTLY)
- `tsx src/lib/hopper.test.ts` 190 inline expects pass (round-26 101 + 53 + 10 + 26 = 190)
- `tsx src/lib/marketplace.test.ts` 138 unchanged

## Push

```
29a4ce9..84f4784  main -> main
```

Verified `origin/main` has all 5 commits.

## Deferred

None — all 5 slices landed cleanly. The candidates list grew rather than shrank: rule reorder-by-drag (round 26's deferred, now the most-natural follow-up since the filter lets the user SEE the dead rules but not yet FIX them in one motion); drilldown row -> cross-surface filter; coverage panel "Show only X" pairing with reorder mode; per-plugin "Run prune now" affordance (round 25's deferred).

## Notes

The bridge helper (slice 131) is the load-bearing piece. By composing the priority chain ONCE in `coverageHealthClickTarget`, the UI never needs to know dead > shadowed > zero — that knowledge lives in the helper. A future change to either `summarizeCoverageHealth` or `filterCoverageByDiagnostic` that reorders the chain bumps both helpers + this bridge together; the cross-helper-agreement test pins the three-way contract.

The chain-health chip is now a TWO-affordance surface: read the copy + click to drill in. Conditional <button vs span> preserves the chip's accent color (`.healthy`/`.warn`/`.critical` class:directives apply to both elements) while gating the pointer cursor + hover lift + focus ring to the clickable variant.

The filter slot in `suggestCoverageExportFilename` is the second TWO-slot extension to the canonical filename shape (the drilldown helper has bucket; coverage has filter). Both optional, both omitted on the chain-wide / unfiltered default path, both inserting between the watch slot and the date so the date stays the most-recent-thing-on-the-right anchor across the family.

A paralegal building a 6-rule chain who clicks the chain-health chip's "2 dead rules" now sees: chip lights up (active ring) → filter row's "Dead" chip lights up → "Showing 2 of 6 rules — dead" sub-line → cov-list narrows to those 2 rows with accent rail → Export… popover writes `hopper-coverage_watch-7_dead_<date>.csv` with the toast confirming "Exported 2 rules as CSV (0.2 KB) (filtered: dead)". Two Escape keystrokes returns to the full chain view.
