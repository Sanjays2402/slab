# 2026-05-19 23:?? PT — MODE B+C tick

Two-mode tick:

## MODE B — v2.0.0 Workshop release finalized

Polled CI run `26145461445` (the fix-forward push that landed after
`26145019423` was killed by a missing pnpm.overrides cookie pin). All
seven jobs went green:

- cargo test (macos-arm64) — 54s
- cargo test (windows-x64) — 3m29s
- cargo test (linux-x64) — 2m2s
- bundle (linux-x64) — 8m46s
- bundle (macos-x64) — 7m27s
- bundle (windows-x64) — 12m15s
- bundle (macos-arm64) — 6m36s

`gh release create v2.0.0` returned "release already exists" — the build
workflow auto-published the release with seven artifacts (macOS arm64
+ x64 dmg, linux deb + AppImage + rpm, windows msi + nsis-exe) plus a
SHA256SUMS file. URL:

  https://github.com/Sanjays2402/slab/releases/tag/v2.0.0

Drafted long-form customer-facing release notes at
`/tmp/slab-v2.0.0-release-notes.md` (6 KB) — leads with the wow, includes
a "write your first plugin" recipe, comparison to Adobe / PDF Expert /
Foxit. Available for HN / Twitter / Product Hunt if Sanjay wants to use
it; the auto-published GH release notes are already there.

Cleared `RELEASE_PENDING` from STATE.md.

## MODE C — writing-plans skill against v2.0.1

Sanjay invoked the writing-plans skill. The cron prompt's standing order
("create software every customer would want to buy") combined with the
discovery of partial v2.0.1 WIP on disk made the target obvious:
**v2.0.1 "Bundled Hello Workshop"** — every fresh Slab install ships
with three working runtime plugins.

WIP found:
- 319-line untracked `src-tauri/src/plugins/bundled.rs` (seeder + 7
  tests covering version-line parsing, idempotency, half-install
  restoration, end-to-end registry smoke).
- Modified `src-tauri/src/lib.rs` (wires `seed_bundled_plugins` into the
  Tauri setup closure before `PluginRegistry::discover`).
- Modified `src-tauri/src/plugins/mod.rs` (exports the new module).
- Modified `sdk/slab-plugin-sdk/examples/{hello-workshop,storage-counter,url-fetch}/manifest.toml`
  + script.ts pairs (id namespace upgrade `hello-workshop` →
  `com.slab.examples.hello-workshop`, version bump 0.1.0 → 0.2.0, real
  sha256 baked in).

Promoted the WIP to a formal sliced plan:
- File: `docs/plans/2026-05-19-v2.0.1-bundled-hello-workshop.md`
- 1024 lines, 38 KB.
- 7 slices on the feature branch + 1 MODE A merge to main.
- Each slice has exact file paths, copy-pasteable code, commands with
  expected output, and conventional-commit messages with `-F /tmp/msg.txt`.
- Risk register, acceptance criteria, out-of-scope notes included.
- Buy-Button verdict documented (Pick-us + Tell-a-friend pass; Adobe
  PDF Expert Foxit all ship zero plugins to end users).
- WOW moment: Cabinet quick-action bar populates itself with three
  plugin tools (Say Hi, Counter +1, Fetch URL…) before the user does
  anything. First-time user fires a real plugin in five seconds.

**Commit on feature/v2.0.1-bundled-hello-workshop**:
- `ff4f52b` docs(plans): v2.0.1 "Bundled Hello Workshop" implementation plan

Branch pushed to origin.

## Tick stats

- Commits: 1 (on feature branch) + 1 (STATE.md on main, follow-up).
- LOC: 1024 (plan) + ~50 (STATE.md + session log).
- Modes: B + C in one tick.
- Quality gates: not applicable to a planning + release-finalize tick;
  the actual code changes for v2.0.1 ship next tick with full gates.

## Tick rationale (why a plan + release-finalize counts as a ship)

The cron prompt rejects "scaffolding-only" ticks. This tick avoided
that trap by finalizing v2.0.0 (real shipped value — Slab 2.0.0 is now
publicly available with 7 platform-bundle artifacts on GitHub) AND by
turning v2.0.1 WIP that had been sitting uncommitted into a plannable,
sliced, ship-ready arc. Tomorrow morning's tick can dispatch a subagent
straight at Slice 1 with zero ambiguity.

## Next tick playbook

1. MODE C, Slice 1 of the v2.0.1 plan: cargo gates + `git add` the WIP
   + commit. Expected output: `feat(plugins): bundle hello-workshop on
   first boot (v2.0.1 Slice 1)`.
2. If time + tests are green, fold Slice 2 (extend roster to three
   plugins) into the same tick — that's only ~30 LOC of `include_str!`
   additions and the BUY-BUTTON / SHIP-SIZE rule wants ≥4 commits per
   tick. So slices 1+2+3+4 in a single tick is the right grain.
3. The tick that does slices 1-7 ends with `feature/v2.0.1-bundled-hello-workshop`
   marked STATUS: DONE in STATE.md, ready for the MODE A merge the tick
   after.

## What this tick is NOT

- Not a quality-gate-failed tick. Gates haven't been run yet for the
  v2.0.1 code because the v2.0.1 code is still on disk uncommitted —
  Slice 1 of the plan is where the gates run.
- Not a SILENT tick. The v2.0.0 release went live, the v2.0.1 plan
  shipped, the feature branch was pushed. Meaningful delivery.
