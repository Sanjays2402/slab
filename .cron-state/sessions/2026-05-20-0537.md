# TICK 2026-05-20 05:37 PT — MODE C writing-plans → v2.0.4 "Memory" plan

## Invocation
User invoked `writing-plans` skill (third consecutive tick using it).

## Time gate
`TZ=America/Los_Angeles date '+%u %H'` → `3 05`. Wednesday 5am PT.
**Off-hours — proceeding with normal tick.**

## Decision
Pattern from last two ticks: each writing-plans invocation promotes the
next release in the pipeline.
- Two ticks ago: v2.0.2 "Workshop Marketplace" plan promoted.
- Last tick: v2.0.3 "Beacon Settings" plan promoted.
- This tick: v2.0.4 "Memory" plan promoted.

## Why v2.0.4 "Memory"?

**The gap:** Slab today opens every PDF on page 1. Adobe Acrobat, PDF
Expert, and Foxit all remember last page. That's a **table-stakes
Pick-us PASS** gap — no serious PDF-reader review forgives it. Closing
it is a launch-blocking prerequisite for v1.0.0.

**Why now (vs v0.10.0 Beacon launch or v2.0.5 bookmarks)?**
1. Both v2.0.2 and v2.0.3 plans are already on disk. The user clearly
   wants the plan-stockpile pattern continued.
2. v2.0.4 is *cheap* (frontend-only, no Rust, no Tauri commands) AND
   high-impact (table-stakes gap closed).
3. Surfaces are clean — no collision with v2.0.2 (marketplace) or
   v2.0.3 (Settings Beacon section). Documented the audit in Slice 0.4.

## Codebase discovery this tick
- Read `src/lib/recent.ts` (217 LOC) — confirmed `RecentFile` shape,
  `recordRecent` signature, thumb-store quota handling pattern (used
  the same quota-crash recovery in the new `readingPosition.ts`).
- Read `src/lib/panels/ReaderPanel.svelte` around event-bus handlers
  (lines 690-715), `loadBytes` flow (line 404+), `tearDownDoc`,
  recents-card markup (line 1220+), toolbar markup (line 1281+).
  Confirmed pdfjs `pagesinit` / `pagechanging` / `scalechanging`
  events are exactly what we need.
- Read `src/lib/CommandPalette.svelte` around line 240-300 to find the
  insertion point for the new "Resume reading…" entries.
- Read `src/lib/types.ts` to confirm `CmdResult` shape (not used —
  v2.0.4 has zero Tauri commands).
- Verified version stamps: `package.json`, `src-tauri/Cargo.toml`,
  `src-tauri/tauri.conf.json` all at `2.0.1`.
- Verified no in-flight branches touching `recent.ts` or
  `ReaderPanel.svelte`.

## Plan written

**File:** `docs/plans/2026-05-20-v2.0.4-memory.md`
**Size:** 51 KB, 1482 lines.
**Structure:** Header + 6 slices + pre-flight Slice 0.
**Code budget:** ~580 LOC non-test, ~700 with optional vitest unit tests.
**Commit budget:** 6 commits (7 if tests ship as a separate Slice 1 commit).

### Slice-by-slice
- **Slice 0** — pre-flight verification (4 grep checks).
- **Slice 1** — `src/lib/readingPosition.ts` store + 9 vitest tests
  (~270 LOC total).
- **Slice 2** — Save/restore wiring in ReaderPanel (~120 LOC).
- **Slice 3** — Live "Reader %" toolbar chip (~30 LOC + 2 i18n strings).
- **Slice 4 ⭐ WOW** — SVG progress rings on Recents grid (~60 LOC CSS
  + 25 LOC markup + 2 i18n strings).
- **Slice 5** — Command-palette "Resume reading…" entries (~40 LOC +
  3 i18n strings).
- **Slice 6** — Onboarding step + version bump + CHANGELOG + customer
  release notes + MODE A merge instructions.

## Buy-Button verdict baked into the plan
- **Pick-us PASS** — closes the Adobe / PDF Expert / Foxit
  table-stakes gap.
- **Notice-it PASS** — three visible new surfaces per upgrade.
- **Tell-a-friend PASS** — progress-ring grid is the WOW screenshot.

## Quality gates this tick
- `pnpm check` → 0 errors, 35 pre-existing CSS warnings. ✅
- No Rust touched this tick (plan only).

## Commit
- `<HASH>` docs(plans): v2.0.4 "Memory" implementation plan
- Plan file: `docs/plans/2026-05-20-v2.0.4-memory.md` (51 KB)
- STATE.md updated with new STATUS line + new TICK entry.
- Session log at `.cron-state/sessions/2026-05-20-0537.md`.

## Push
Pushed to `origin/main` via gh-token credential helper.

## Scheduling note for future ticks
Strict order: v2.0.2 → v2.0.3 → v2.0.4. All three touch
`src/lib/i18n/en.json` in disjoint namespaces but git's textual merge
doesn't know JSON. Serialize the releases — don't parallelize.

## Open question for Sanjay
Three plans now on disk and ready. Next tick's mode?
- (a) Start *executing* v2.0.2 Slice 1 (Fuse.js marketplace search).
- (b) Continue planning (v2.0.5+).
- (c) MODE B finalize v2.0.1 GitHub release if CI is green.

Default if Sanjay doesn't weigh in: option (c) if CI is green
(v2.0.1 has been waiting on Windows since the LF hotfix), else (a)
because the plan backlog is now three deep and stockpiling without
shipping is its own anti-pattern.
