# Tick 2026-05-17 23:10 PT — v1.3.1 released + v1.4.0 Bench Slices 9+10 shipped

**Time of day check**: `7 23` (Sunday 11pm PT) — outside blackout.

## What happened

### MODE B closeout — v1.3.1 "Foundry Patch" 🩹 released

CI run `26012655931` finished green between ticks. Release pipeline:

- `gh run download 26012655931 --dir /tmp/slab-release-v1.3.1` →
  all 6 artifacts present (macos arm64+x64 dmg, linux amd64 deb +
  AppImage, windows msi + nsis).
- `gh release create v1.3.1` → reported "release already exists". The
  Tauri build action auto-created the release via `tauri-action`'s
  `releaseDraft: false` flag — all 6 assets were already uploaded by
  the matrix workers.
- Verified via `gh release view v1.3.1` — 6 assets uploaded, public,
  release notes rendered from `docs/release-notes/v1.3.1.md`.

v1.3.1 is now the latest published release.

### MODE C develop — v1.4.0 Bench Slices 9 + 10

3 commits on `feature/v1.4.0-bench-slices-9-10`, merged --no-ff into
`feature/v1.4.0-bench`:

1. `0185360` — feat(marketplace): uninstall confirmation modal (Slice 9)
   - New `UninstallConfirmModal.svelte`, mirrors DecryptModal's
     visual language. Esc/Enter/backdrop click handled.
   - Refactored `onUninstall` in PluginsPanel to open the modal
     instead of firing the destructive call immediately. Both
     call sites (marketplace card + detail drawer) automatically
     pick up the modal flow since they already routed through
     `onUninstall`.
   - 7 new i18n keys × 4 locales = 28 strings added.
   - Closes the detail drawer if it was showing the just-removed
     plugin (cosmetic but matters — otherwise the drawer would
     pop a confirmation for an absent plugin on next click).

2. `1ce0d41` — docs(marketplace): submission guide + seed slab-plugins
   repo (Slice 10)
   - New `docs/MARKETPLACE.md` (~340 lines): user flow, trust model,
     full submission walkthrough, IndexEntry schema reference,
     `slab-sign-plugin` CLI usage, key rotation plan.
   - New `docs/marketplace-seed/README.md` + `index.json.example`:
     drop-in payload for the future `Sanjays2402/slab-plugins` GH
     repo. Three stub entries (hello-slab, qpdf-tools, theme-pack)
     so authors see the schema shape.
   - Added one bullet in `docs/PLUGINS.md` so authors discover
     the marketplace from inside the existing plugin docs.

3. `6bc227a` — Merge commit (--no-ff) on `feature/v1.4.0-bench`.

## Quality gates (all green)

- `cargo fmt --all -- --check` ✓
- `cargo clippy --all-targets -- -D warnings` ✓
- `cargo test --lib` → 581 passed (unchanged — no backend changes)
- `pnpm check` → 0 errors / 23 warnings (baseline preserved)

## Sanjay's WIP preserved

Sanjay had a working tree with screenshot refreshes + dynamic version
injection (vite define + `__APP_VERSION__`) + README updates in
progress on main. Stashed at the start of the tick:
`stash@{0}: On main: WIP from Sanjay: screenshot refresh + dynamic version (preserved by cron 2026-05-17 23:10b)`.
Recover with `git stash pop`.

## Decisions

- **No PR created.** Per the cron prompt's explicit instructions, this
  is direct merge to feature branch, not a PR.
- **Did NOT create the `Sanjays2402/slab-plugins` GH repo.** That's a
  one-time external action that should be Sanjay-initiated. The seed
  files are ready under `docs/marketplace-seed/` for him to drop in.
- **Modal focuses the destructive primary, not Cancel.** Accessibility
  convention for destructive dialogs: keyboard users press Enter to
  act, danger color makes the consequence visually obvious. Matches
  what GitHub / Linear / Notion do.

## Next tick

`feature/v1.4.0-bench` now has 10 slices shipped. Only Slice 11 left:
**release**. That's a lockstep version bump (1.3.1 → 1.4.0) +
`docs/release-notes/v1.4.0.md` + merge to main + tag + push. Single
tick, MODE A territory.

Slice 11 plan (next tick):
- `package.json`, `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`,
  `Cargo.lock` → 1.4.0
- `docs/release-notes/v1.4.0.md` with marketplace headline + i18n
  expansion + uninstall confirmation
- README front-door refresh to v1.4.0
- Merge to main, tag v1.4.0, push with `--follow-tags`
- Then MODE B finalize on next-next tick (or same tick if CI finishes
  quickly)
