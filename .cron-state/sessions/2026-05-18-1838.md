# Tick 2026-05-18 18:38 PT — v1.9.2 Tasks 4 + 5

**Mode:** C (DEVELOP)
**Branch:** `feature/v1.9.2-voice-polish`
**Commits this tick:** 3 (`f2f5d09`, `cb924e6`, `c64ab60`)

## Shipped

### T4 — whisper.cpp model picker (backend)

- `ai/stt.rs::list_whisper_models()` — readdir on `$SLAB_MODELS_DIR` / `~/.slab/models/`, merge with built-ins (tiny.en, base.en, small.en). Stem normalisation: `ggml-base.en.bin` → `base.en`.
- `ai/stt_session.rs::build_whisper_cmd()` — pure argv builder. Precedence: explicit `model` arg → `$WHISPER_MODEL` env → omit `-m`.
- `SttSession::start(engine, model: Option<String>)` — per-recording override captured in `InFlight.model`. `transcribe_whisper_cpp()` now takes `Option<&str> model`.
- `slab_beacon_voice_stt_start` IPC: optional `model` param with precedence `arg → BeaconConfig.voice.stt_model → env`.
- New IPC `slab_beacon_voice_stt_list_models` for the Settings dropdown.
- **+7 tests**, mutex-serialised env-touching ones (`WHISPER_ENV_LOCK`, `MODELS_DIR_LOCK`).

### T5 — Listen settings UI

- BeaconVoicePanel "Listen settings" cluster: model dropdown + send-phrase input + auto-send checkbox + scoped Save button. CSS card styled to match existing panels.
- VoiceCfg type extended with 5 STT fields (`stt_engine`, `stt_model`, `stt_language`, `stt_trigger_word`, `stt_send_on_trigger`).
- Fixed a pre-existing buggy invoke shape: panel was casting `slab_beacon_config_read` to bare `BeaconCfg` and reading `cfg.voice`, but real shape is `CmdResult<SlabConfig>` so `cfg.voice` was always `undefined`. **Panel never pre-populated from disk before this commit.** Now properly unwraps `res.value.beacon` and writes via `cfg.beacon.voice = …`.

## Gates (batched)

- `cargo fmt --all -- --check` ✓
- `cargo clippy --all-targets -- -D warnings` ✓ (one `useless_conversion` fix on `into_iter()`)
- `cargo test --lib` — **736 passed / 0 failed** (was 711 before v1.9.2; +25 tests across T1–T4)
- `pnpm check` — 0 errors, 23 pre-existing warnings

## Status

- Branch: 7 commits ahead of base, pushed to origin.
- Tasks 1–5 of plan complete. Task 6 (Windows-native cpal recorder cargo feature) remains.

## Next tick

Default: ship T6, mark branch DONE, MODE A merge to main + tag + push, MODE B finalize release.
Alternative: descope T6 to v1.9.3 and ship v1.9.2 now with PowerShell Windows fallback.
