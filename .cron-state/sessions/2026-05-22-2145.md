# Tick 2026-05-22 21:45 PT — Slice 3 XMP + OutputIntent

Off-hours (Friday 21:00 PT). Blackout clear. STATE pointed to Slice 2/2.5
done, next = Slice 3 (XMP + OutputIntent) or Slice 3 mutating font embed.
Issue list empty. Branch `feature/v3.0.0-bedrock-pdfa` at `2a418f3`, **CI
on that SHA failed** (clippy::manual_contains -D warnings on all 3 platforms
in `font_audit.rs:181`).

## Shipped

1. **`3aee658` clippy rescue** — STANDARD_14.iter().any(...) → .contains(&...).
2. **`655bdd8` xmp module** — 220 LOC + 15 tests. Hand-rolled RDF/XML.
   Chrono added with no-default + clock + std only.
3. **`7a2fa89` output_intent module** — 195 LOC + 9 tests. Injects /Metadata
   catalog ref + /OutputIntents array w/ flate-compressed sRGB v4 ICC stream.

## Gates

- `cargo fmt --all -- --check` ✅
- `cargo check --lib` ✅ (36s)
- `cargo test --lib pdf::pdfa` ❌ OOM (disk full — 205 MiB free). Test build
  needs ~3 GB more. Deferred to CI. Cleared `target/debug` (-1.2 GB).
- Clippy + tests delegated to CI.

## Buy-Button audit

4/4. Pay-for-it (Adobe Pro), Pick-us (Preview/PDF Expert/Foxit free can't),
Notice-it (Bedrock UI is next slice), Tell-a-friend (compliance mandates).

## Next tick

Slice 4 (font embedding mutator) OR Slice 5 orchestrator. Or wait for CI:
if clippy fix lands green, attack the mutating font embed pass; that's the
last pure-Rust module before the orchestrator + UI ticks.

## Disk emergency redux

Mac mini disk fluctuates between 95% and 100% within a single tick — every
test build drains the remaining buffer. Sanjay action item: weekly cargo
clean across worktrees + Downloads triage.
