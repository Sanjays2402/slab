# v1.0.0 "Glass" Release Prep Implementation Plan

> **For Hermes:** Execute this in a single tick — release prep is mostly
> mechanical version-bumping + release-notes-writing + a MODE A merge.
> Verify quality gates after the bump and after the merge.

**Goal:** Bump 0.15.0 → 1.0.0, write comprehensive `docs/releases/v1.0.0.md`
release notes covering all 7 Glass slices, merge `feature/v1.0.0-glass`
to `main` with `--no-ff`, tag `v1.0.0`, push.

**Architecture:** Same lockstep version bump as every prior release —
`src-tauri/Cargo.toml`, `package.json`, `src-tauri/tauri.conf.json`,
sidebar pill in `src/routes/+page.svelte`, and `Cargo.lock` (via
`cargo check`). Then release notes. Then MODE A merge.

**Tech Stack:** No code changes — file edits + git + cargo.

---

### Task 1: Bump version 0.15.0 → 1.0.0 in all four manifests + sidebar pill

**Objective:** Lockstep version bump.

**Files:**
- Modify: `src-tauri/Cargo.toml:3`
- Modify: `package.json:3`
- Modify: `src-tauri/tauri.conf.json:4`
- Modify: `src/routes/+page.svelte:326`

**Step 1:** Edit `src-tauri/Cargo.toml` line 3: `version = "0.15.0"` → `version = "1.0.0"`.

**Step 2:** Edit `package.json` line 3: `"version": "0.15.0"` → `"version": "1.0.0"`.

**Step 3:** Edit `src-tauri/tauri.conf.json` line 4: `"version": "0.15.0"` → `"version": "1.0.0"`.

**Step 4:** Edit `src/routes/+page.svelte` line 326: `v0.15.0` → `v1.0.0`.

**Step 5:** Refresh `Cargo.lock` via `cd src-tauri && cargo check --quiet`. Expected: clean, lock updated, `slab-app` version bumps to `1.0.0`.

---

### Task 2: Write comprehensive `docs/releases/v1.0.0.md`

**Objective:** Tell the v1.0.0 story across all 7 Glass slices.

**Files:**
- Create: `docs/releases/v1.0.0.md`

Cover: headline, what's new (7 slices: Settings/Theme, Shortcuts overlay,
Toasts, Recent pinning, Palette MRU, Onboarding tour, Customizable keymap),
deferred to v1.0.1 (floating panels, perf pass), upgrade notes, stats,
the v1.0 stable-API promise.

---

### Task 3: Commit version bump + release notes

**Step 1:** `git add -A`

**Step 2:** Commit via `/tmp/msg.txt`:

```
chore(release): bump 0.15.0 → 1.0.0 + v1.0.0 "Glass" release notes

The first stable Slab release. Locks in 7 Glass polish slices on top of
the 14 prior feature releases: a stable PDF tool with offline AI, library,
diff, OCR, presenter mode, and a fully customisable command-driven UI.
```

---

### Task 4: Quality gates on `feature/v1.0.0-glass`

From `src-tauri/`:
- `cargo fmt --all -- --check` ✓
- `cargo clippy --all-targets -- -D warnings` ✓
- `cargo test --lib` ✓ (expect 451 passed, unchanged)

From repo root:
- `pnpm exec svelte-check` ✓ (expect 0 errors / 28 baseline warnings)

If any fail → STOP, fix on branch, do not merge.

---

### Task 5: Push branch

```bash
TOK=$(gh auth token)
git -c credential.helper="!f() { printf 'username=x-access-token\npassword=%s\n' '$TOK'; }; f" \
  push origin feature/v1.0.0-glass
```

---

### Task 6: MODE A — merge to main + tag + push

**Step 1:** Switch to main and pull.

```bash
git checkout main && git pull --ff-only
```

**Step 2:** Merge `feature/v1.0.0-glass` with `--no-ff` using a rich merge message at `/tmp/merge-msg-v1.0.0.md`.

```bash
git -c user.email='51058514+Sanjays2402@users.noreply.github.com' \
    -c user.name='Cake (cron)' \
    merge --no-ff feature/v1.0.0-glass -F /tmp/merge-msg-v1.0.0.md
```

**Step 3:** Re-run all 4 quality gates ON MAIN. If any fail → `git reset --hard ORIG_HEAD` and abort.

**Step 4:** Tag `v1.0.0`.

```bash
git -c user.email='51058514+Sanjays2402@users.noreply.github.com' \
    -c user.name='Cake (cron)' \
    tag -a v1.0.0 -m "Slab v1.0.0 — Glass 🪟"
```

**Step 5:** Push main + tag.

```bash
TOK=$(gh auth token)
git -c credential.helper="!f() { printf 'username=x-access-token\npassword=%s\n' '$TOK'; }; f" \
  push origin main --follow-tags
```

---

### Task 7: Update STATE.md with `RELEASE_PENDING: v1.0.0`

Record merge SHA, tag, CI run id (from `gh run list -L 1`). Roll the
prior tick block to PRIOR.

---

### Task 8: Session log + final commit on main (state)

Write `.cron-state/sessions/2026-05-17-1240.md` summarising the tick.
Commit STATE.md + session log to main. Push.
